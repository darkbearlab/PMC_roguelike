# 複製人 PMC 戰術 Roguelike — MVP 實作規格
> **文件版本**：v0.5
> **文件用途**：交付給 Claude Code 實作的技術規格。範圍嚴格鎖定在「最小可玩版本」（MVP）。
> **語言慣例**：文件為繁體中文，程式碼識別字一律使用英文。

## 版本沿革

| 版本 | 內容 |
|---|---|
| v0.1 | 初版規格 |
| v0.2 | 介面浮動化（地圖滿版，HUD 與控制列半透明浮在上面） |
| v0.3 | **操作介面重構**：取消斜向移動、距離改用曼哈頓、統一的兩段式地圖點擊文法、移除開火鍵、鎖定機制、相鄰格互動、按鈕重新配置、鏡頭邊界夾制 |
| v0.4 | **數值規模 ×10 與戰場回饋層**：生命值／傷害／護甲放大十倍（距離與次數不動）、保底傷害資料化、`CombatEvent` 事件流、傷害數字與彈道畫在戰場上、目標資訊加上護甲與傷害區間 |
| v0.5 | **戰鬥手感成形**：開啟命中率擲骰、掩蔽（獨立於遮蔽的三級鄰格掃描）、姿勢的完整效果（含視野代價）、浮動傷害與浮動護甲、`penetration` 欄位、防禦狀態指示與掩蔽成因標示 |

> v0.4 不改任何戰鬥規則：命中率仍固定 1.0、護甲仍是確定值、沒有掩體或姿勢的命中修正、傷害公式不變。放大數值與回饋層都是下一版（命中率、掩體、浮動傷害與護甲）的前置條件——個位數的整數承載不了少量浮動，而「打中但被擋下」和「沒打中」若在畫面上長得一樣，新的戰鬥數學就只能盲調。

> v0.3 的動機：原規格把射擊設計成「點敵人 → 彈出預覽面板 → 再點確認」。實測後確認這是錯誤的——射擊是全遊戲最高頻的動作，卻被配上了最重的互動形式。v0.3 把**所有地圖互動統一成同一條文法**，並縮減按鈕數量。

---

## 0. 給實作者的重要說明

**請嚴格遵守第 2 節的範圍界線。** 本文件第 16 節列出了大量已經設計好、但**這一版明確不要實作**的系統。它們寫在這裡的唯一目的，是讓你在設計資料結構與架構時不要把路擋死。看到它們不代表要做它們。

本專案的成敗完全取決於**單場戰鬥好不好玩**。所有的合成、經濟、多人系統在戰鬥手感確立之前都沒有意義。請把工程品質集中在 `core/` 規則層。

---

## 1. 設計核心

三句話說明這個 MVP 要驗證什麼：

1. **回合制但打起來要爽。** 玩家每回合有 2 AP，可以組合出「移動→開火」「開火→開火」「換槍→開火」等短句，而不是一次只做一件事。
2. **掩體是二元的、可預測的。** 是否被打到取決於視線與姿勢，玩家看得到、算得出來。隨機性優先放在「局面」而不是「結果」。
   *MVP 階段命中率固定為 100%，但命中解算管線必須完整實作（§8.1），日後要能只改一個函式就打開擲骰。*
3. **死亡是支出而非失敗。** 士兵死了就從名冊丟下一個到最近的空投點，屍體與裝備留在原地。任務只有在名冊耗盡或玩家主動止損時才結束。

---

## 2. MVP 範圍

### 2.1 這一版要做的

- 單一手工繪製的地圖（一張，從檔案讀取）
- 單一玩家單位在場上（回合制、格子、**四方向移動**）
- 2 AP 行動點系統
- 站／蹲兩種姿勢，與半身掩體的視線互動
- 兩把武器：一把輕武器、一把重武器
- 三種敵人：衝鋒型、裝甲型、射手型
- 敵人 AI：IDLE / ALERT / SEARCH 三態
- 開火噪音警戒機制
- 死亡與空投點增援（名冊 4 人）
- 屍體與裝備遺留、可回收
- 主目標 1 個、次要目標 2 個
- 撤離與止損、任務結算畫面
- 觸控操作介面（統一的兩段式地圖點擊 + 方向鍵 + 動作鍵）

### 2.2 這一版明確不要做的

- 任何合成、DNA、世代、經驗值系統
- 任何經濟、貨幣、商店、副業、掛機收益
- 任何伺服器、帳號、連線、多人、大型合約
- 程序化地圖生成（地圖是手工資料檔）
- 音效、配樂、粒子特效、過場動畫
- 存檔／讀檔（單場任務打完就結束）
- 士兵之間的差異化（名冊 4 人數值完全相同）
- 任何技能內容（v0.3 只做技能鍵的展開／收合鉤子）

---

## 3. 技術規格

| 項目 | 規格 |
|---|---|
| 目標平台 | **手機瀏覽器**（iOS Safari / Android Chrome），直向 |
| 次要平台 | 桌面瀏覽器（鍵盤操作可選做，不強制） |
| 語言 | TypeScript |
| 渲染 | HTML5 Canvas 2D（不使用遊戲引擎） |
| 建置 | Vite |
| 測試 | Vitest |
| 外部相依 | 盡量為零。不使用 React／Phaser／Pixi。 |
| 效能目標 | 中階手機上 60fps；單一回合解算 < 16ms |

### 3.1 架構要求（重要）

```
src/
  core/          # 純函式規則層。不得 import 任何 DOM / Canvas / 瀏覽器 API。
    state.ts     #   GameState 型別定義
    commands.ts  #   Command 型別與 applyCommand()
    los.ts       #   視線計算
    combat.ts    #   傷害解算
    ai.ts        #   敵人決策
    rng.ts       #   可播種亂數
  render/        # Canvas 繪製，唯讀 GameState
  ui/            # 觸控輸入、HUD、按鈕
  data/          # 地圖、武器、敵人的資料檔（JSON）
  main.ts
```

**硬性要求：**

1. `core/` 必須是**純的、決定性的**。`applyCommand(state, command) => newState`。相同的初始狀態 + 相同的指令序列，必須產生完全相同的結果。
2. 所有亂數必須經過 `core/rng.ts` 的可播種產生器（xorshift128 或類似）。**禁止直接呼叫 `Math.random()`**。
3. 渲染層與 UI 層唯讀 `GameState`，不得直接改寫狀態，一律透過發出 Command。
4. 遊戲狀態必須可完整序列化為 JSON。

理由：這一版用不到，但之後的合約計分需要在伺服器端重跑同一套規則來驗證分數。現在把規則層弄髒，之後要重寫。

> **鎖定（§8.4）刻意不放進 `GameState`**。它純粹是介面狀態，不影響規則，放進去只會污染規則層。

---

## 4. 資料模型

```ts
type Vec2 = { x: number; y: number };
type Stance = 'STAND' | 'CROUCH';
type Facing = 'N' | 'NE' | 'E' | 'SE' | 'S' | 'SW' | 'W' | 'NW';
type TileType =
  | 'FLOOR'       // 可通行，不擋視線
  | 'WALL'        // 不可通行，永遠擋視線
  | 'HALF_COVER'  // 不可通行，視線規則見 §7
  | 'DROP_POINT'  // 可通行，增援落點
  | 'TERMINAL'    // 可通行，主目標互動點
  | 'SUPPLY';     // 可通行，次要目標互動點

interface Weapon {
  id: string;
  name: string;
  class: 'LIGHT' | 'HEAVY';
  damage: number;
  range: number;          // 最大射程（格，曼哈頓距離）
  magazine: number;       // 彈匣容量
  ammo: number;           // 目前彈藥
  fireCost: number;       // 開火 AP 成本
  reloadCost: number;     // 裝填 AP 成本
  endsTurn: boolean;      // 開火後是否強制結束回合
  noiseRadius: number;    // 開火噪音半徑（格，曼哈頓距離）
  splash: number;         // 濺射半徑（曼哈頓距離），0 = 無
  // --- 命中相關：MVP 不生效，但欄位與管線必須存在，見 §8.1 ---
  accuracy: number;       // 基礎命中率 0..1
  optimalRange: number;   // 此距離內不衰減
  falloffPerTile: number; // 超出 optimalRange 每格扣除的命中率
}

interface Unit {
  id: string;
  faction: 'PLAYER' | 'ENEMY';
  archetype: string;       // 'SOLDIER' | 'RUNNER' | 'HULK' | 'SHOOTER'
  name: string;            // UI 與戰鬥紀錄顯示用
  pos: Vec2;
  hp: number;
  maxHp: number;
  armor: number;
  aim: number;             // 命中修正，MVP 一律 0，見 §8.1
  evasion: number;         // 迴避修正，MVP 一律 0，見 §8.1
  maxAp: number;
  ap: number;
  stance: Stance;
  facing: Facing;          // 八向，純美術朝向；移動只有四向
  sightRange: number;      // 曼哈頓距離
  equipped: Weapon | null;
  stowed: Weapon | null;
  aiState: 'IDLE' | 'ALERT' | 'SEARCH';
  lastKnownTarget: Vec2 | null;
  searchTimer: number;
  shotsThisTurn: number;   // 射手型每回合上限 1 次（§9）
  attacksPerTurn: number;
}

interface Corpse {
  id: string;
  pos: Vec2;
  unitId: string;            // 原本的士兵編號，用於 UI 顯示
  weapons: Weapon[];         // 死亡時攜帶的所有武器
}

interface Objective {
  id: string;
  pos: Vec2;                 // 互動時要比對座標，所以必須帶位置
  done: boolean;
}

interface GameState {
  turn: number;
  phase: 'PLAYER' | 'ENEMY' | 'MISSION_END';
  map: MapData;
  units: Unit[];
  corpses: Corpse[];
  roster: string[];          // 尚未投入的士兵 id
  activePlayerUnitId: string | null;
  objectives: {
    main: Objective;
    secondary: Objective[];
  };
  casualties: number;
  deployed: number;          // 累計投入士兵數（結算畫面用）
  rngSeed: number;
  rng: RngState;             // 產生器完整內部狀態，隨 state 序列化（§14）
  result: 'ONGOING' | 'SUCCESS' | 'ABORTED' | 'WIPED';
  enemyQueue: string[];      // 敵人回合剩餘待行動者（逐步播放用）
  pendingReinforcement: { deathPos: Vec2; deadUnitId: string } | null;
  nextEntitySerial: number;  // 決定性的 id 產生器，不用亂數
  log: LogEntry[];           // 戰鬥紀錄（§8.1 要求能顯示未命中）
}
```

---

## 5. 回合與 AP 規則

### 5.1 基本流程

1. 玩家回合開始：目前的士兵 AP 補滿至 `maxAp`（2）。
2. 玩家下指令，每個指令扣除對應 AP。
3. **AP 歸零時自動結束玩家回合**，不需要按結束鍵。
4. 敵人回合：所有敵人依 `id` 字典序依次行動，各自使用自己的 AP。
5. `turn++`，回到 1。

另外提供一個明確的 **「待機」** 指令（消耗全部剩餘 AP 並結束回合），供玩家主動跳過。

### 5.2 AP 成本表

| 動作 | AP 成本 | 是否結束回合 |
|---|---|---|
| 移動 1 格（四方向） | 1 | 否 |
| 輕武器開火 | 1 | **否** |
| 輕武器裝填 | 1 | 否 |
| 重武器開火 | 2 | 否（AP 歸零自然結束） |
| 重武器裝填 | 2 | 否（同上） |
| 換裝為輕武器 | 1 | 否 |
| 換裝為重武器 | 2 | 否 |
| **改變姿勢（站↔蹲）** | **0** | 否 |
| **改變面向** | **0** | 否 |
| 從屍體拾取武器 | 1 | 否 |
| 與目標物互動（終端／補給箱／撤離） | 1 | 否 |
| 待機 | 全部剩餘 | 是 |

**設計註記給實作者**：姿勢與面向免費是刻意的。它們的代價在能力限制（§7），不在 AP。不要「順手」給它們加上 AP 成本。

> v0.3 起取消斜向移動，原 §5.3「斜向切角」整節刪除。該節正文（兩側皆為阻擋物才禁止）與括號註記（禁止切角）互相矛盾；取消斜向後此問題自然消失。

---

## 6. 移動與地形

- **四方向移動**（上下左右），一次一格，每格 1 AP。
- `WALL` 與 `HALF_COVER` 不可通行。
- 單位不可重疊。
- 屍體不阻擋移動，可以走上去。
- 地圖邊界視同 `WALL`。

> **所有「距離」一律指曼哈頓距離 `|dx| + |dy|`**：移動、射程、視野、噪音半徑、濺射半徑、AI 判斷全部同一套標準。取消斜向之後，走過去要幾步，距離就算幾格。

---

## 7. 視線、掩體與姿勢（本作的戰術核心）

這一節是整個遊戲的心臟，請務必照實作。

> **「多遠」與「有沒有被擋住」是兩個不同的檢查，不是不一致：**
>
> - **距離**回答「夠不夠得到」→ **曼哈頓距離**（§6）
> - **遮擋**回答「中間有沒有東西擋著」→ **幾何直線**（下述的對稱 Bresenham）
>
> **不要**把遮擋的射線改成沿正交路徑折線行走。折線有多條走法、無法唯一決定，會讓半身掩體的雙向阻擋規則產生不確定的結果。

### 7.1 視線演算法

使用**對稱**的視線判定。實作 Bresenham 直線，並要求「A 看得到 B」與「B 看得到 A」永遠一致（若兩方向結果不同則判定為不可見）。非對稱視線是這類遊戲最常見的 bug 來源。

### 7.2 遮蔽：看不看得見（射線，二元）

> **「遮蔽」與「掩蔽」是兩個互不相干的檢查，不要混在一起。**
>
> | | 回答的問題 | 演算法 | 結果 |
> |---|---|---|---|
> | **遮蔽**（§7.2） | 看不看得見 | 幾何射線 | 二元 |
> | **掩蔽**（§7.2b） | 好不好打中 | 目標鄰格掃描 | 三級 |
>
> 掩蔽刻意不走射線：現有模型裡射線通過 `WALL` 就直接阻擋，不存在「射線切到牆但仍看得見」的中間狀態，所以要讓牆提供掩蔽，來源必須是另一個檢查。

| 地形 | 視線效果 |
|---|---|
| `WALL` | 永遠阻擋視線 |
| `HALF_COVER` | 依兩端單位的姿勢決定，見下 |

**半身掩體規則：**

> 一條視線若通過某個 `HALF_COVER` 格，且**線段任一端的單位正緊鄰（8 鄰域）該掩體格並處於 `CROUCH` 狀態**，則該視線被阻擋。

這裡的「8 鄰域」是**幾何遮擋判定的一部分**，不是距離比較，因此不受 §6 的曼哈頓規則影響。

推論出來的實際效果（這正是我們要的）：

- **蹲在半身掩體後 → 你看不見外面，外面也看不見你。**（雙向阻擋）
- **站起來 → 你可以越過掩體開火，但敵人回合中你是暴露的。**
- 因為改變姿勢免費，玩家每回合的真正決策變成：**這回合結束時，我要站著還是蹲著？**

### 7.2b 掩蔽：好不好打中（鄰格掃描，三級）

對目標 T 與射手 S：

1. `dx = S.x − T.x`、`dy = S.y − T.y`
2. 找出 T 的正交鄰格中**朝向射手**的那些（最多兩格）：`dx > 0` 取東鄰、`dx < 0` 取西鄰、`dx == 0` 水平方向無候選；`dy` 同理
3. 數其中地形為 `WALL` 或 `HALF_COVER` 的格數。**地圖界外視同 `WALL`**

| 阻擋鄰格數 | 掩蔽等級 | 命中率修正 |
|---|---|---|
| 0 | 無掩蔽 | 0 |
| 1 | 部分掩蔽 | −0.25 |
| 2 | 良好掩蔽（牆角） | −0.40 |

`WALL` 與 `HALF_COVER` 在掩蔽上**一視同仁**。兩者的差別只存在於遮蔽：蹲在半身掩體後會整個消失，蹲在牆邊不會。

**為什麼是三級列舉，不是布林也不是純疊加：** 純布林讓縮在牆角與躲在單一掩體後完全相同，地圖設計少掉一個維度；純疊加沒有上界，掩蔽加蹲姿加射程衰減會把命中率壓到下限、雙方互相打不中、回合空轉——這是戰術遊戲變得拖沓的典型死法。三級列舉保留角落的價值，上界由結構本身保證。**不要**改成累加每一格鄰接掩體的實作。

**側翼是掩蔽的解法。** 掩蔽取決於掩體位於目標朝向射手的哪一側，因此射手換一個角度就能消除或降低目標的掩蔽。這是本機制最重要的戰術產出。

兩個預期中的推論（不是 bug）：

1. **正交直線對射時通常沒有掩蔽。** 若目標朝向射手那一側的鄰格是阻擋物，那格必然在射線上，結果是遮蔽（完全看不見）而非掩蔽。走廊裡的正面對射因此非常致命——這是設計意圖，也讓繞到正交線上成為一種進攻手段。
2. **掩蔽與蹲姿會疊加。** 上界由命中率下限保證（§8.1）。

### 7.3 姿勢的完整效果

| | 站 | 蹲 |
|---|---|---|
| 被命中率 | 基準 | **降低** −0.15 |
| 自己射擊命中率 | 基準 | **提高** +0.10（穩定度） |
| 視野範圍 | 基準 | **縮短為 0.6 倍** |
| 射線上有半身掩體時 | 有掩蔽，可被射擊 | 完全看不見（§7.2） |

- 姿勢**不影響**傷害與移動成本。
- 姿勢改變仍然**不消耗 AP**。
- 移動不會自動改變姿勢。蹲著也可以移動（成本相同）。

**蹲姿視野縮短是刻意的代價。** 若蹲下同時降低被命中率、提高自身命中率卻沒有任何代價，玩家會永遠蹲著。實測：士兵在中央大廳站立可見 95 格，蹲下降至 32 格。

> **待觀察（§17）**：在走廊與掩體密集區域，玩家可能本來就看得到所有相關目標，視野縮短形同無代價。若實測發現「永遠蹲著」仍是最優解，預留的下一個調整槓桿是**蹲姿移動改為 2 AP 一格**（尚未實作）。

### 7.4 偵測

敵人在自己的回合開始時（以及移動的每一步後）檢查：若對玩家單位有視線且**曼哈頓距離** ≤ `sightRange`，則進入 `ALERT` 並更新 `lastKnownTarget`。

面向**不影響**視野（360 度視野）。面向在 MVP 只用於美術朝向。
*（保留 `facing` 欄位是為了日後可能加入的背擊機制，這一版不要實作。）*

---

## 8. 武器與戰鬥解算

### 8.1 命中解算管線

v0.5 起**已啟用擲骰**。開關在 `data/rules.json` 的 `combat.enableToHitRoll`，設為 `false` 可退回「一律必中」做對照測試。

#### 合法性檢查（先於解算）

攻擊必須同時滿足，否則為非法指令，UI 應事前禁用而非讓玩家打空：

1. 目標在射程內（**曼哈頓距離** ≤ `weapon.range`）
2. 攻擊者對目標有視線（§7）
3. 彈藥足夠、AP 足夠、本回合攻擊次數未達上限

#### 命中率函式

```
命中率 = clamp(
    weapon.accuracy
  + attacker.aim
  + 射手姿勢修正          （蹲 +0.10）
  - target.evasion
  - 目標姿勢修正          （蹲 −0.15）
  - 掩蔽修正              （部分 −0.25 / 良好 −0.40，§7.2b）
  - 射程衰減              （max(0, 曼哈頓距離 − optimalRange) × falloffPerTile）
, 下限, 1.0)
```

**命中率下限訂在 0.15 而非 0.05 是刻意的。** 掩蔽與蹲姿會疊加，最壞情況是良好掩蔽加蹲姿加斜向射程衰減。命中率觸底時的正確解法是換位置，但如果玩家當下換不了位置，5% 等於要他空轉二十回合。0.15 讓僵局仍然會被打破，只是很慢。

所有修正項都在 `data/rules.json` 的 `combat.stance` 與 `combat.cover` 底下。

#### 解算流程（必須照這個順序實作）

```ts
export function resolveAttack(state, attackerId, targetPos, weapon): AttackResult {
  const chance = toHitChance(...);
  // 無論 chance 是多少，都必須抽一個亂數。
  // 這讓 MVP 與日後啟用擲骰時的 RNG 序列長度一致，重播不會錯位。
  const roll = rng.next(state);          // 0..1
  const hit = roll < chance;
  ...
}
```

**「即使必中也要抽亂數」是刻意的。**

#### 解算結果型別

```ts
interface AttackResult {
  hit: boolean;
  roll: number;          // 保留，供除錯與戰鬥紀錄顯示
  chance: number;        // 保留，同上
  impactPos: Vec2;       // 實際彈著點。命中時 = 目標格；MVP 階段永遠等於目標格
  damageByUnit: { unitId: string; amount: number }[];
}
```

#### 未命中路徑

未命中時：不造成傷害、照常消耗 AP 與彈藥、照常產生噪音、戰鬥紀錄與 UI 必須能正確顯示「未命中」。

驗收時會以測試強制命中率回傳 0，驗證整條未命中路徑走得通（見 §15）。**不要用 `if (hit)` 把未命中寫成什麼都不做的空分支。**

### 8.2 傷害計算

```
傷害擲值 = weapon.damage ± weapon.damageSpread      （均勻整數）
護甲擲值 = target.armor  ± target.armorSpread       （均勻整數，**每一發各自擲一次**）
實際傷害 = max(minDamage, 傷害擲值 − max(0, 護甲擲值 − weapon.penetration))
```

**護甲每一發都擲，不是單位生成時擲一次。** 這讓「防護穩定」與「品質參差」成為兩種可以區分的敵人特質——同樣平均 20 的護甲，`20 ± 2` 與 `20 ± 8` 打起來的感覺完全不同。生成時擲一次會把這個維度壓成一個數字。

**保底傷害是資料值**（`combat.minDamage`，目前為 `10`），不得寫死。它與生命值同單位：生命值規模變動時保底也要跟著調，否則等於實質取消保底。

**`penetration` 本次一律為 0**，只留欄位與公式。穿甲彈藥將來會做成**稀有消耗品**而非武器固定屬性——否則輕武器可以繞過換重武器的決策，裝甲型敵人的設計任務就失效了。

濺射武器：對中心格全額計算，對**曼哈頓半徑**內其他單位造成 `floor(傷害擲值 / 2)`，各自再擲一次護甲，同樣扣減、同樣保底。濺射會傷到玩家自己的單位，**不要**加入友軍傷害豁免。

### 8.3 噪音

任何開火動作會在 `noiseRadius`（曼哈頓）內產生噪音。所有在半徑內且處於 `IDLE` 的敵人轉為 `SEARCH`，並將開火位置設為 `lastKnownTarget`。噪音不受牆壁阻擋（刻意簡化）。

### 8.4 鎖定（v0.3）

射擊沒有專屬按鈕，一律透過地圖點擊執行（§12）。介面層維護一個「目前鎖定的目標」：

- 第一次點擊一個敵人 → **鎖定**。鎖定**不消耗任何資源**（不花 AP、不花彈藥、不推進回合）
- 第二次點擊**同一個**目標 → 開火，**且鎖定保留**
- 點擊**另一個**敵人 → 改變鎖定，不開火、不消耗任何資源
- **鎖定跨回合保留**：若目標仍存活、仍在視線與射程內，回合開始時維持鎖定，該目標的第一次點擊即為開火
- 目標失效（死亡、脫離視線或脫離射程）時解除鎖定，並清除瞄準線

鎖定是介面狀態，**不放進 `GameState`**（§3.1）。

### 8.5 MVP 武器資料

放在 `data/weapons.json`：

| 欄位 | AR-9 制式步槍 | RR-4 無後座力砲 |
|---|---|---|
| `class` | LIGHT | HEAVY |
| `damage` | 30 | 120 |
| `damageSpread` | 5 | 20 |
| `penetration` | 0 | 0 |
| `range` | 8 | 12 |
| `magazine` | 6 | 1 |
| `fireCost` | 1 | 2 |
| `reloadCost` | 1 | 2 |
| `endsTurn` | false | false |
| `noiseRadius` | 6 | 14 |
| `splash` | 0 | 1 |
| `accuracy` | 0.85 | 0.75 |
| `optimalRange` | 5 | 8 |
| `falloffPerTile` | 0.08 | 0.10 |

> 射程是曼哈頓距離。射程 8 的武器在正前方可打 8 格，在斜 45 度方向只剩 4 格——這是取消斜向移動的預期結果，不是 bug。也因此**會出現「看得到但打不到」的敵人**：UI 不給它合法目標框，但仍然看得見它。

士兵初始狀態：手持 AR-9（滿彈），收納 RR-4（滿彈）。

### 8.6 CombatEvent：規則層與回饋層的介面

`applyCommand` 除了回傳新的 `GameState`，另外回傳一份**決定性的事件清單**，描述這次指令產生了哪些事件：

```ts
export function applyCommand(state: GameState, cmd: Command): CommandResult;
export interface CommandResult { state: GameState; events: CombatEvent[] }
```

非法指令回傳 `{ state: 原物件, events: [] }`，因此 `result.state === state` 的 identity 比對照樣成立。

事件種類：`SHOT`（彈道）、`IMPACT`（命中傷害，含 `blocked` 被護甲吃掉的量與 `lethal`）、`MISS`、`KILL`、`NOISE`、`AI_STATE`、`AMMO_OUT`、`RELOAD`、`OBJECTIVE`、`DEPLOY`。

**硬性界線：**

1. 事件是純資料、可序列化，完全由狀態轉換推導而來，因此完全決定性。
2. **動畫的時間軸與播放狀態不進 `GameState`**，只活在 `render/effects.ts`。
3. `core/` 不知道有動畫這回事，也不引入任何瀏覽器 API。
4. 回饋層只是每幀被畫出來，**不阻塞輸入、不延後回合推進**。

### 8.7 亂數順序紀律

一次攻擊有三個擲值。順序固定，而且**無論該項是否生效一律照抽**：

1. 命中擲值
2. 傷害擲值
3. 主目標護甲擲值

即使 `enableToHitRoll` 為關閉、即使目標護甲為 0、即使目標格根本沒有單位、即使該發未命中，這三個亂數都必須照抽。濺射的每個額外受害者再各自多抽一次護甲（依 `state.units` 順序，仍然完全決定性）。

**理由**：亂數序列的長度必須與設定無關，否則切換開關或調整數值會使所有以種子重現的紀錄與測試失效。

---

## 9. 敵人

放在 `data/actors.json`。敵人也只能四方向移動。

| 原型 | HP | 護甲 | AP | 視野 | 攻擊 | 說明 |
|---|---|---|---|---|---|---|
| `RUNNER` 衝鋒型 | 40 | 0 ± 0 | **3** | 10 | 近戰，相鄰時 1 AP，傷害 30 ± 5 | 比玩家快，逼玩家不能無限風箏 |
| `HULK` 裝甲型 | 120 | **20 ± 8** | 1 | 8 | 近戰，相鄰時 1 AP，傷害 60 ± 10 | 輕武器每發只造成 10 點（保底），逼玩家換重武器 |
| `SHOOTER` 射手型 | 50 | 0 ± 0 | 2 | 12 | 遠程，射程 7，1 AP，傷害 20 ± 4，每回合上限 1 次 | 逼玩家利用掩體與姿勢 |

> **注意單位**：HP、傷害、護甲是同一組可互相比較的數字，一起縮放。
> 射程、視野、噪音半徑、濺射半徑、彈匣、AP、`searchTimer`、地圖尺寸是**距離、次數或行動點**，
> 與生命值不同單位，**不隨生命值規模縮放**。

敵人不需要裝填、不需要換武器、不需要姿勢（永遠 `STAND`）。所有敵人的 `aim` 與 `evasion` 在 MVP 一律為 `0`，但欄位須存在於資料檔中。

### 9.1 AI 狀態機

```
IDLE
  原地不動（MVP 不做巡邏）。
  取得玩家視線 → ALERT
  聽到噪音 → SEARCH
ALERT
  有視線且在射程／相鄰 → 攻擊，直到 AP 用盡
  有視線但不在射程 → 朝玩家移動
  失去視線 → SEARCH，searchTimer = 3
SEARCH
  朝 lastKnownTarget 移動
  途中取得視線 → ALERT
  抵達 lastKnownTarget 或 searchTimer 歸零 → IDLE
  每回合 searchTimer--
```

AI 必須是決定性的。若有平手情況（多條等長路徑），以固定規則決勝（鄰居展開順序固定為 N, E, S, W），不要用亂數。

> 追擊時的目標格允許被佔據：搜索的 `lastKnownTarget` 往往就是玩家現在站的地方，若不允許，整條路徑會算不出來、敵人會原地發呆。真正的「不能走進去」由下一步的佔據檢查擋下（走到相鄰就停）。

---

## 10. 死亡、增援與屍體

### 10.1 玩家單位死亡

1. 在該格生成 `Corpse`，內含死者身上**所有**武器（含收納的）。
2. 該單位從 `units` 移除，`casualties++`。
3. 若 `roster` 為空 → `result = 'WIPED'`，任務結束。
4. 否則暫停，跳出增援選單：玩家從名冊選一位。
5. 新士兵出現在**距離死亡地點最近的、且該格未被佔據的 `DROP_POINT`**，AP 為 0（該回合不能行動），姿勢 `STAND`。
6. 新士兵**只有預設配備**：一把滿彈的 AR-9，**沒有 RR-4**。

> 第 6 點是刻意的：重武器留在屍體上。要不要冒險回去撿，是玩家自己的選擇。

### 10.2 屍體

- 屍體不阻擋移動與視線。
- 站在屍體所在格，花 1 AP 可拾取其中一把武器（若手上與收納都滿，則需先丟棄，丟棄免費）。
- 屍體永遠不會消失。

> 「丟棄免費」只在「兩個欄位都滿、要撿第三把」時才需要，因此併進拾取指令：被換下來的槍免費留在同一具屍體上，總成本仍是 1 AP。沒有獨立的丟棄指令。

### 10.3 敵人死亡

直接從 `units` 移除，不留屍體、不掉落物品。

---

## 11. 任務目標與結束條件

### 11.1 目標

- **主目標**：走到 `TERMINAL` 格**或其正交相鄰格**，花 1 AP 互動。
- **次要目標** ×2：走到 `SUPPLY` 格**或其正交相鄰格**，花 1 AP 互動。
- 目標可以任意順序完成，次要目標非必要。

> 互動距離為**曼哈頓距離 ≤ 1**（站在該格上或正交相鄰皆可）。斜向相鄰的曼哈頓距離是 2，不算相鄰。

### 11.2 撤離

完成主目標後，回到**初始空投點**（或其相鄰格）並互動，即為成功撤離 → `result = 'SUCCESS'`。

### 11.3 止損

止損**只在當前士兵陣亡的那個當下提供**，出現在增援選單中（「改為止損撤出」）。

- 按下後跳出二次確認，顯示目前戰況損益（見 §12.5）
- 確認後 `result = 'ABORTED'`，任務結束
- 取消則退回增援選單，任務繼續

> 這是刻意的設計決定：止損必須付出一條人命才會浮現，而不是隨時可按的逃生鍵。HUD 上**沒有**止損按鈕。

### 11.4 結算畫面

無論何種結束方式，顯示：回合數、投入士兵數、陣亡數、完成的目標、尚留在戰場上的武器清單（未回收的損失）。附一個「重新開始」按鈕。

---

## 12. 觸控介面

### 12.1 硬性原則

> **手機沒有 hover。** 所有預覽資訊必須靠兩段式互動取得，不得依賴滑鼠懸停。

所有可點擊元素的觸控命中區不得小於 **48×48 CSS px**，且**相鄰按鈕的命中區不得互相重疊**。

地圖是**滿版**底層，HUD 與控制列半透明浮在上面。

### 12.2 統一的地圖點擊文法

這是介面的核心規則，適用於地圖上的**每一種**目標，沒有例外：

> **第一下：選取，並顯示「如果執行會發生什麼」。**
> **第二下：執行。**

**兩條不變式：**

1. **選取永遠不消耗任何資源。** 不花 AP、不花彈藥、不推進回合。因此點錯永遠無害，**不需要「取消」按鈕**。
2. **選取狀態是持續的、可見的。** 不使用任何遮蔽戰場的彈出面板。

| 目標 | 第一下顯示 | 第二下 |
|---|---|---|
| **敵人** | 雷射瞄準線、目標身上紅點準星、名稱與命中率 | 開火 |
| **遠處可通行格** | 路徑與所需 AP | 依路徑移動（見 §12.4） |
| **屍體** | 可拾取的物品清單 | 拾取 |
| **相鄰的終端／補給箱／撤離點** | 互動內容與 AP 消耗 | 執行互動 |
| **自己** | 詳細狀態面板 | —（單次點擊即可） |
| **非法目標** | 無反應 | — |

> 只有「自己」與「屍體」用浮動小卡（兩者本來就需要一份清單，且都是低頻動作）。射擊、移動、互動的預覽全部畫在戰場上。

### 12.3 射擊

**沒有開火按鈕。** 射擊一律透過地圖點擊執行，鎖定的生命週期見 §8.4。

第一次點擊一個敵人時，持續顯示於戰場上：

- 從士兵拉一條**雷射瞄準線**到目標
- 目標身上顯示**小紅點準星**
- 目標附近以行內文字顯示**名稱**、**命中率**（MVP 固定 100%）、**預期傷害區間**與**目標護甲**

傷害一律以區間格式呈現（目前上下限相同，例如 `傷害 30–30`），護甲並排顯示。
這樣下一版改成 `傷害 25–35　裝甲 15–25` 時不需要重排版面。

> AR-9 鎖定裝甲型時會顯示 `傷害 10–10　裝甲 20` ——
> **開槍之前**玩家就看得出這把槍對它沒用。這是 §12.9 的教學能不能成立的前提。

標籤畫在目標**下方**：浮動傷害數字一律往上飄，兩者才不會疊在一起。

**唯一保留的確認對話框**：當濺射範圍會涵蓋玩家自己時，第二次點擊不直接開火，改為跳出明確的確認提示。重武器開火（消耗 2 AP，等於整個回合）**不需要**額外確認——兩段式點擊本身已足夠。

### 12.4 尋路移動

- 點擊遠處的可通行格 → 顯示四方向路徑與所需總 AP
- 再點一次 → 沿路徑逐格移動
- 若目標格不可達，第一次點擊不顯示路徑（視為非法目標）
- 方向鍵仍然保留，供單步微調使用

自動移動在下列任一情況發生時**立即停止**（剩餘 AP 保留）：AP 耗盡、有新的敵人進入視線、玩家單位受到傷害、路徑被阻擋。

### 12.5 目標框：兩種訊息，兩種表現

| 視覺 | 意義 | 適用對象 |
|---|---|---|
| **靜態細框** | **這個目標我現在打得到**（視線、射程、彈藥、AP 皆成立） | 所有合法目標，**包含 IDLE 的敵人** |
| **點滅紅框** | **這個敵人不是 IDLE**（處於 ALERT 或 SEARCH） | 所有非 IDLE 敵人，無論打不打得到 |

兩者可以同時出現在同一個敵人身上。

> **硬性要求：框只表達狀態，不決定可否點擊。IDLE 的敵人必須能夠被選取與攻擊。**
> 偷襲毫無察覺的敵人是核心玩法。若因為 IDLE 敵人沒有點滅框而讓玩家以為不能攻擊，即為設計失敗。

附帶效益：把警戒狀態直接畫在敵人身上，會讓噪音機制變成可見的因果——玩家能看出「我剛剛那一槍驚動了誰」。

### 12.6 HUD（永遠可見，半透明浮動）

AP 指示（兩顆圓點，用掉變空心）、HP 數值、目前武器名稱 + 彈藥、姿勢圖示、名冊剩餘人數、場上存活敵人數，以及**戰況損益**：`投入 2 名 ／ 陣亡 1 名 ／ 主目標 未完成 ／ 次要 1/2 ／ 戰場遺留 1 件裝備`。

日誌的開合按鈕在**畫面右上**。

### 12.7 按鈕配置

**左下：移動區**

```
[ 待機 ]            ← 獨立在方向盤上方，間距 >= 一個按鈕高度

[ 　　 ] [  ↑  ] [ 蹲立 ]
[  ←  ] [  ↓  ] [  →  ]
```

- 左上角留空；**蹲立**放在右上角（每回合都要用，必須常駐好按）
- **待機是這一區唯一不可逆的動作**（消耗全部剩餘 AP 並結束回合），不得與方向鍵相鄰

**右下：動作區**

```
[ 技能 ]   ← 展開摺疊選單。MVP 選單內容為空，只做鉤子與展開／收合
[  彈  ]   ← 裝填
[  換  ]   ← 換武器
```

摺疊選單內的項目順序固定，不得依可用性重排。

**通用規則**

- **不可用的按鈕一律灰掉，不要隱藏。** 按鈕消失會導致其他按鈕位移，位移會破壞肌肉記憶並造成誤觸。
- 按鈕位置固定，任何情況下都不重排。

### 12.9 戰場回饋層

> **玩家不需要打開日誌，就能完整理解剛剛發生了什麼。**
> 日誌是事後查證的紀錄，不是理解戰況的管道。

必須在戰場上直接可見的事件：

| 事件 | 表現 |
|---|---|
| 命中 | 傷害數字從目標身上浮起 |
| 未命中 | 彈道畫成偏移的虛線 + 「未命中」文字，與下一項截然不同 |
| **命中但被護甲擋下** | 顯示實際造成的傷害，旁邊以較小字體顯示「擋下 N」 |
| 擊殺 | 更大的數字 + 「擊殺」，與一般命中可區分 |
| 開火 | 從射手到目標的短暫彈道 |
| 彈藥見底 | 「空倉」提示，不要只在 HUD 上默默歸零 |
| 敵人警戒狀態改變 | 目標框變化 + 該敵人身上一次性的狀態提示 |
| 噪音 | 開火時短暫顯示波及半徑（曼哈頓 ⇒ 畫成菱形） |

**三種結果必須可區分（硬性要求，優先度高於本節其他項目）：**

| 結果 | 玩家應該讀到的意思 | 判準 |
|---|---|---|
| **未命中** | 運氣不好，再開一槍 | 沒有 `IMPACT` 事件 |
| **命中但被擋下大半** | **這把槍對這個敵人沒用，該換武器了** | `IMPACT.blocked >= IMPACT.amount` |
| **命中** | 有效 | `IMPACT.blocked < IMPACT.amount` |

三者必須用**不同的顏色**，被擋下的主數字還要比有效命中**小一號**。

**理由**：裝甲型敵人在設計上的唯一任務，就是教會玩家「該換重武器了」。如果玩家分不出「我沒打中」和「我打中了但沒用」，這個教學就不會發生，裝甲型只會被當成一個很煩的高血量敵人。

**表現要求：**

- 浮動數字從目標上方浮起並淡出，約 0.6～0.8 秒
- **不得阻塞輸入**：動畫播放中玩家仍可繼續操作，動畫不得延後回合推進
- 同一目標短時間內多次受擊時，數字要錯開堆疊；每一階要拉開「主數字 + 副標」的總高，否則兩行字會咬在一起
- 深色描邊 + 亮色字，在淺色地形上也看得清
- 一律以 Canvas 繪製，不新增 DOM 元素

日誌保留在右上，內容與現況相同，但驗收標準是：**全程不開日誌，也能完整理解戰況。**

### 12.10 掩蔽必須看得見

當目標因為掩蔽而難以命中時，戰場上要標示出**是哪些格子造成的**——把那一格或兩格阻擋鄰格框起來。

攻擊預覽同時顯示：實際命中率、傷害區間、護甲區間，以及掩蔽的**等級與幅度**（例如「部分掩蔽 −25%」），不要只給一個變小的數字。

**理由**：側翼是掩蔽的解法（§7.2b）。玩家必須看得出擋住自己的是哪一塊，才會想到繞過去。看不見成因的懲罰只會被當成運氣不好。

### 12.11 玩家自身的防禦狀態

HUD 要顯示玩家當前的防禦狀態：**無掩蔽 / 部分掩蔽 / 良好掩蔽**、是否蹲伏、以及有幾個敵人瞄得到。

玩家必須能在結束回合前，知道自己接下來會以什麼狀態承受攻擊。這是「回合結束時要站著還是蹲著」這個核心決策的資訊基礎。

掩蔽取決於射手的方位，所以「玩家有沒有掩蔽」沒有單一答案。**本作採用最差情況**：在所有現在看得到玩家的敵人之中，取掩蔽最低的那一個。玩家因此永遠不會被自己的 HUD 騙——顯示良好掩蔽就代表每個看得到你的人都難打。這個選擇會在任務開始時寫進戰鬥紀錄。

### 12.8 鏡頭

**邊界夾制**：士兵原則上置中，但接近地圖邊緣時停止捲動，不再嚴格置中。界外區域畫成岩層紋理並標出可作戰範圍的邊界。

> **夾制的邊界是「可觸區域」，不是視窗。** HUD 與控制列浮在地圖上，若照視窗邊界夾制，士兵走到地圖下緣時會被壓到控制列底下——而 §12.3 之後射擊與互動全靠點地圖，被蓋住就等於完全不能操作。因此垂直方向夾在 HUD 底部與控制列頂部之間，士兵置中於「看得到也點得到」的那一段。代價是地圖上下緣外會露出一條界外區，而那條剛好被 UI 蓋住，不佔可視面積。水平方向沒有常駐 UI（控制列只佔最底下一條），維持照視窗夾制。
>
> **硬性要求**：士兵在地圖上任何位置時，**他自己與四個正交鄰格都必須完整落在可觸區域內**，否則移動與相鄰互動會變成點不到。

---

## 13. 地圖格式

`data/maps/mission_01.json`：

```json
{
  "id": "mission_01",
  "name": "廢棄水處理廠",
  "width": 32,
  "height": 24,
  "legend": {
    ".": "FLOOR",
    "#": "WALL",
    "+": "HALF_COVER",
    "D": "DROP_POINT",
    "T": "TERMINAL",
    "S": "SUPPLY"
  },
  "tiles": ["################################", "..."],
  "startDropPoint": { "x": 1, "y": 1 },
  "enemies": [
    { "archetype": "RUNNER",  "pos": { "x": 11, "y": 9 } }
  ]
}
```

### 13.1 MVP 地圖設計要求

- 至少 **3 個 `DROP_POINT`**，分散在地圖三分之一、三分之二處，讓「退回最近空投點」成為有意義的空間懲罰。
- `TERMINAL` 放在離起點最遠的一端。
- 兩個 `SUPPLY` 分別放在偏離主路線的側翼，讓玩家必須決定要不要繞路。
- 半身掩體要**成排配置**，形成可以連續蹲行推進的路線；不要隨機灑點。
- 至少要有一段**開闊地**，讓射手型敵人有發揮空間、逼玩家繞路或衝刺。
- 至少 8～10 個敵人，三種類型都要出現。裝甲型應放在一條無法繞過的路線上，逼玩家面對換重武器的決策。

---

## 14. 亂數與決定論

MVP 的戰鬥結果幾乎是決定性的（命中率固定 1.0、無傷害浮動、AI 決定性），但**每次攻擊仍會消耗一個亂數**（§8.1），以固定亂數序列的長度。

`rng.ts` 必須實作為可播種的產生器，且其內部狀態（含已抽取次數）應納入 `GameState`，使狀態序列化後能完整還原。

`GameState.rngSeed` 應可從網址參數覆寫（例如 `?seed=12345`），方便重現 bug。

---

## 15. 完成定義（驗收清單）

在 320px 寬的手機瀏覽器上，以下每一項都必須成立：

**移動**
- [ ] 士兵與敵人皆無法斜向移動
- [ ] 射程、視野、噪音、濺射全部使用曼哈頓距離，專案中已無殘留的切比雪夫距離判定
- [ ] 斜 45 度方向的可及範圍約為正前方的一半
- [ ] 視線遮擋仍為幾何直線判定，既有的視線測試維持通過
- [ ] 存在「看得見但超出射程」的敵人時，不顯示合法目標框，但該敵人仍然可見
- [ ] 可以用方向鍵移動士兵，AP 正確扣減，歸零時自動換敵人回合
- [ ] 點遠處空格顯示路徑與 AP，再點一次會沿路徑移動
- [ ] 自動移動在敵人進入視線時立即停止，剩餘 AP 保留

**視線與掩體**
- [ ] 蹲在半身掩體後時，掩體另一側的敵人看不見我，我也看不見他們
- [ ] 站起來後可以越過同一個掩體射擊該敵人
- [ ] 掩蔽是獨立的鄰格掃描，**未修改既有的射線遮蔽函式**
- [ ] `WALL` 與 `HALF_COVER` 提供相同的掩蔽效果
- [ ] 朝射手側一格阻擋物 = 部分掩蔽 −25%，兩格 = 良好掩蔽 −40%
- [ ] 地圖界外視同 `WALL`，貼邊界站立可獲得掩蔽
- [ ] 射手移到另一側後，掩蔽等級改變、命中率隨之變動
- [ ] 正交直線對射時，朝向射手的鄰格若為阻擋物，結果是遮蔽而非掩蔽
- [ ] 蹲姿與掩蔽可同時生效，且命中率不低於 0.15

**姿勢**
- [ ] 蹲下時視野縮短至 0.6 倍
- [ ] 蹲下提高自身命中率、降低被命中率
- [ ] 姿勢改變仍不消耗 AP

**命中與傷害**
- [ ] 命中率擲骰可由資料檔開關控制
- [ ] 關掉開關並把 spread 歸零時，行為與 v0.4 完全等價
- [ ] 傷害與護甲皆為 `base ± spread`，護甲每發各擲一次
- [ ] `penetration` 欄位存在且全為 0，公式已就位
- [ ] 三個擲值一律照抽、順序固定；切換開關不改變亂數序列長度

**射擊**
- [ ] 沒有開火按鈕
- [ ] 點敵人一次出現雷射線、準星、名稱與命中率，且**不消耗任何資源**
- [ ] 再點同一個敵人即開火，且鎖定保留
- [ ] 開火後跨回合再點同一目標，一下就開火
- [ ] 點另一個敵人只改變鎖定，不開火
- [ ] 濺射會波及自己時跳出確認；其餘情況不跳任何對話框
- [ ] 輕武器一回合可以開兩槍；重武器一回合只能開一槍
- [ ] 對裝甲型使用輕武器每發只造成 1 點傷害；使用重武器造成 10 點
- [ ] 換武器要花 AP，而且換完重武器就沒 AP 開火了
- [ ] 開火會讓噪音半徑內的閒置敵人轉為搜索狀態並朝開火點移動

**目標框**
- [ ] IDLE 的敵人可以被選取並攻擊
- [ ] 合法目標有靜態細框；非 IDLE 敵人有點滅框；兩者可同時出現

**互動與任務**
- [ ] 站在終端**相鄰格**即可互動，不需站上去
- [ ] 屍體、終端、補給箱全部遵循「點一下顯示、點兩下執行」
- [ ] 衝鋒型敵人移動速度比玩家快，無法靠後退持續拉開距離
- [ ] 士兵死亡後屍體與重武器留在原地，新士兵只帶著步槍從最近空投點出現
- [ ] 可以走回屍體處花 1 AP 撿回重武器
- [ ] 名冊耗盡時任務判定為 WIPED
- [ ] 止損只在當前士兵陣亡時提供，二次確認後正確結算
- [ ] 完成主目標後回到初始空投點可以成功撤離

**介面**
- [ ] 左下為兩列六格配置，左上角留空，蹲立在右上角
- [ ] 待機獨立於方向盤上方，兩者間距 ≥ 一個按鈕高度
- [ ] 右下只有技能、裝填、換武器三顆
- [ ] 技能鍵可展開收合，選單內容為空
- [ ] 日誌開合在右上
- [ ] 不可用按鈕為灰色而非隱藏，且位置不因狀態改變而位移
- [ ] 所有觸控命中區 ≥ 48×48 CSS px 且互不重疊
- [ ] 士兵在地圖任何位置（含四角與四邊）時，自己與四個正交鄰格都沒有被 HUD 或控制列蓋住

**戰場回饋層**
- [ ] 全程不開日誌可以完整理解戰況
- [ ] 未命中、命中被擋下、一般命中三者在畫面上明確可區分（顏色不同、字級不同）
- [ ] 用 AR-9 射擊裝甲型時，玩家能一眼看出「打中了但幾乎沒用」
- [ ] 動畫播放期間仍可輸入，回合推進不被延後
- [ ] 同一目標連續受擊時數字不重疊
- [ ] 在 320px 寬視窗、淺色地形上，浮動數字仍然清晰可讀
- [ ] `GameState` 中不含任何動畫時間軸或播放狀態

**數值規模**
- [ ] 相同種子加相同指令序列，事件序列與放大前完全等價（只有生命值單位的數字大十倍）
- [ ] 射程、視野、噪音、濺射、彈匣、AP、地圖尺寸全部未改變
- [ ] 保底傷害由資料檔控制，目前值為 10
- [ ] 程式碼中已無寫死的戰鬥數值

**規則層**
- [ ] `core/` 有單元測試涵蓋：視線對稱性、AP 扣減、傷害護甲計算、四方向移動
- [ ] **有一個測試把命中率覆寫為回傳 0，驗證未命中路徑完整可用**：不扣血、照扣 AP 與彈藥、照樣產生噪音、戰鬥紀錄顯示未命中、UI 不崩潰
- [ ] 有一個測試驗證：相同種子 + 相同指令序列 → 相同最終狀態
- [ ] 全程沒有直接呼叫 `Math.random()`
- [ ] `core/` 未引入任何 DOM / Canvas / 瀏覽器 API

---

## 16. 已設計但**這一版不要實作**的系統

以下內容僅供架構參考，避免把路擋死。**不要實作。**

- **DNA 與世代**：屍體可抽取 DNA，繼承前一代約 70% 的經驗，逐代遞減形成天花板。士兵編號採「編號-世代」形式（如 `K-441 Gen.7`）。
- **屍體的兩種去處**：抽 DNA 續血脈，或製成溫室蔬菜換現金。互斥。
- **合成系統**：不同資源組合 + DNA 產出不同數值的士兵，無角色設計，純數值。產出應為分佈而非查表，避免配方被社群一週解出。
- **副業與時價**：發電、溫室等被動收入，短期大量出貨會使價格崩跌。**價格回升應綁定「完成的任務數」而非真實時間**，以免養成掛機 check-in 習慣。
- **大型合約**：各打各的、純計分，按完成的主／次目標貢獻分數，分數需依難度加權以避免刷最便宜的目標。
- **屍體池**：他人陣亡士兵的 DNA 進入伺服器池，補員時作為「回收樣本」提供。取代拍賣行，低人口下仍成立。
- **技能系統**：v0.3 已預留技能鍵與摺疊選單的鉤子，但選單內容為空，不要放任何技能進去。
- **調性**：企業諷刺喜劇，越荒謬越好。人格與趣味全部由文字（合約簡報、裝備說明、企業通知、服役紀錄）承擔，零美術成本。

**架構上唯一需要現在預留的**：`core/` 的純粹性與決定性（§3.1），因為日後合約計分需要在伺服器端重跑同一套規則。其餘一律不預留。

---

## 17. 待調整數值一覽

以下數字是為了讓遊戲能跑起來而填的**預設值**，全部預期會在試玩後修改。請把它們集中在 `data/` 的 JSON 中，不要寫死在程式碼裡。

> **兩組不同單位，不要一起縮放。**
> **生命值單位**（HP、傷害、護甲、保底傷害）是一組可互相比較的數字，要縮放就一起縮放。
> **距離與次數**（射程、最佳射程、視野、噪音半徑、濺射半徑、彈匣、AP 成本與上限、`searchTimer`、地圖尺寸、空投點數量）是另一回事，**不隨生命值規模縮放**——放大會直接破壞遊戲。

| 數值 | 目前值 | 調整方向的判斷依據 |
|---|---|---|
| 玩家 AP | 2 | 若一回合能做的事太少、手感綁手綁腳，考慮提高到 3 |
| 士兵 HP | 100 | 應該讓衝鋒型 4 下打死、裝甲型 2 下打死 |
| 保底傷害 | 10 | 與生命值同單位。生命值規模變動時要一起調，否則等於實質取消保底 |
| AR-9 彈匣 | 6 | **這是節奏的主旋鈕。**調它比調傷害有效得多 |
| HULK 護甲 | 20 | 要讓輕武器「打得動但很蠢」，不要真的免疫 |
| RUNNER AP | 3 | 必須嚴格大於玩家 AP，否則風箏流成立 |
| 空投點數量／間距 | 3 個 | **這是死亡懲罰的主旋鈕。**越稀疏死一次越痛 |
| 名冊人數 | 4 | 決定一場任務的容錯上限 |
| 重武器噪音半徑 | 14 | 要讓「開這一砲會引來誰」成為真的顧慮 |
| AR-9 / RR-4 傷害 | 30 / 120 | 與 HP、護甲、保底同一組單位，要一起縮放 |
| 命中率總開關 | 已啟用 | `combat.enableToHitRoll`，設 false 可退回一律必中做對照 |
| 命中率下限 | 0.15 | 掩蔽與蹲姿會疊加；太低會讓玩家覺得被規則耍、只能空轉 |
| 射手蹲姿加成 | +0.10 | |
| 目標蹲姿減免 | −0.15 | |
| 部分／良好掩蔽 | −0.25 / −0.40 | 三級列舉，不要改成每格累加 |
| 蹲姿視野係數 | 0.6 | **蹲姿唯一的代價。**若「永遠蹲著」仍是最優解，下一個槓桿是蹲姿移動改 2 AP |
| 傷害 spread | AR-9 ±5 / RR-4 ±20 | |
| 護甲 spread | HULK ±8 | 每發各擲一次；調它等於調「這個敵人的防護穩不穩定」 |
| `accuracy` / `falloffPerTile` | 見 §8.5 | 啟用擲骰後才有意義；設定射程衰減曲線用 |

> **v0.3 的已知副作用，本次不處理**：四方向移動會使地圖的有效距離變長（原本斜向 3 步的路徑現在要 6 步），任務回合數將上升；曼哈頓射程也會讓現有射程數值在斜向感覺明顯變短。地圖尺寸、空投點間距與射程數值全部是本節的旋鈕，留待試玩後一起調。結算畫面照常記錄總回合數，作為後續調整的依據。
