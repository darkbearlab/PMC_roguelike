# 複製人 PMC 戰術 Roguelike — MVP 實作規格
> **文件版本**：v0.19（於 v0.20 之後補做）
> **文件用途**：交付給 Claude Code 實作的技術規格。範圍嚴格鎖定在「最小可玩版本」（MVP）。
> **語言慣例**：文件為繁體中文，程式碼識別字一律使用英文。

## 版本沿革

| 版本 | 內容 |
|---|---|
| v0.1 | 初版規格 |
| v0.2 | 介面浮動化（地圖滿版，HUD 與控制列半透明浮在上面） |
| v0.3 | **操作介面重構**：取消斜向移動、距離改用曼哈頓、統一的兩段式地圖點擊文法、移除開火鍵、鎖定機制、相鄰格互動、按鈕重新配置、鏡頭邊界夾制 |
| v0.4 | **數值規模 ×10 與戰場回饋層**：生命值／傷害／護甲放大十倍（距離與次數不動）、保底傷害資料化、`CombatEvent` 事件流、傷害數字與彈道畫在戰場上、目標資訊加上護甲與傷害區間 |
| v0.5 | **戰鬥手感成形**：開啟命中率擲骰、掩蔽（獨立於遮蔽的三級鄰格掃描）、姿勢的完整效果（含視野代價）、浮動傷害與浮動護甲、`penetration` 欄位、防禦狀態指示與掩蔽成因標示 |
| v0.6 | **交火張力與敵人回合演出**：基礎命中下修讓掩蔽真正生效、雙方生命值下修、彈匣收緊、兩段式察覺（反應窗口）、敵人回合逐一演出並可跳過 |
| v0.7 | **以行動排程器取代 AP 制**：刪除「回合」與 AP，改成每個單位各自排在時間軸上（`clock` / `nextActAt`）、所有動作改記時間成本、新增系列動作（RR-4 兩步裝填）、敵人狀態轉換也要花時間 |
| v0.8 | **蹲姿面向視野、鏡頭平移與背刺**：`facing` 從美術欄位變成規則（蹲下時只看得見前方半平面）、敵人一律有盲區、姿勢改變開始收費、移除蹲姿視野 ×0.6、背刺（無視線的目標 +0.30 且無視掩蔽）、鏡頭可拖曳平移、點地圖移動可關閉 |
| v0.9 | **彈藥、射擊模式、背包與搜刮**：彈藥變成背包裡的有限資源、單發／點放／連發（同時間、不同耗彈與準度、每發獨立判定）、背包與負重分級、地圖搜刮點與敵人屍體、DNA、隨時可撤離並帶出戰利品 |
| v0.10 | **敵人戰術 AI**：落點評分（掩體、側翼、射線）取代貪婪逼近、射手型會蹲進掩體、IDLE 會警戒巡視、SEARCH 有收尾、敵人喊口令宣告下一個動作且**說了就一定做** |
| v0.11 | **三張手刻地圖、隨機選圖與地圖驗證工具**：走廊密集／開闊地／掩體密集三個對照實驗，任務開始時以可播種亂數選圖，`build_maps.mjs` 同時是驗證器並產出統計摘要 |
| v0.12 | **背包管理與消耗品**：系列動作分成可續行／須重來、重武器裝填進度存在武器上、準備欄與使用按鈕、戰地封合劑、背包介面與丟棄 |
| v0.13 | **地圖收攏與任務長度收斂**：重做 mission_02 與 mission_03、四張圖的耗時收進 1500～3000、驗證器新增「預估完成路徑長度」與「方向性掩蔽差距」兩條約束 |
| v0.19 | **翻越掩體**：撞向半身掩體從「什麼都不會發生」變成一個動作。**不新增按鈕。**一次兩格、花 20、**落地強制站姿**；敵人也會翻，否則掩體列會變成單向膜 |
| v0.20 | **經濟層**：信用點、合約報酬、戰利品出售、遺產武器的現貨稀缺、債務與董事會信件、結算損益表。**取代 v0.16 §21.6 的 0 元補給站佔位** |
| v0.18 附錄 | **自動補給**：撤離帶回來的彈藥併回共用庫存、士兵攜行量歸零 —— 那個模型是對的，但副作用是每一場之後都要手動把彈藥一發一發按回去。補上「補到基準」的一鍵動作與回公司時的自動補給 |
| v0.18 | **任務中的裝備管理**：補上主手／收納欄／背包之間搬武器的動作。這是一個**缺失的動詞** —— 在此之前，空投下來的替補走到屍體旁邊沒有任何動作可以騰出位置，而「要不要冒險走回去撿自己的屍體」是本作最重要的單一決策 |
| v0.17 | **移動演出、攝影機跟隨與選單規則**：格間位移動畫（敵我獨立旋鈕、**可被下一個輸入打斷**）、攝影機跟隨行動中的單位並只在必要時平移、平移期間目標暫停行動、選單通則（不消耗時間的介面可全螢幕）。**純呈現層與介面，`core/` 完全未改動** |
| v0.16 | **持久化、逐人配裝與暫時補給站**：`MetaState` 與 `GameState` 分離、名冊與軍械庫跨場次保留、逐人配裝（武器是實例，同一把只能給一個人）、手動派遣與手動挑替補、0 元補給站。**這一版之後，專案從「單場戰鬥模擬」變成 roguelike** |
| v0.15 | **出擊前配裝、口徑系統與武器批次**：彈藥依口徑分類且可共用、五把新武器（角色分化靠衰減曲線而非傷害）、增量裝填與齊射、武器重量計入負重、重量換算尺（1 單位 = 0.5 公斤）、出擊前配裝畫面 |
| v0.14 | **合約選擇**：任務開始前的合約清單畫面、地形標籤與難度評級**由地圖統計值推導**、手寫的公文簡報、結算可返回重新產生的清單。**局外層的第一塊，刻意不含持久化與經濟** |

> v0.19 的動機：這是一個**缺失的移動動作**。成排掩體目前是絕對屏障 ——
> 只能繞到末端，而那可能要六步。翻越給了「穿過去」這個選項，
> 但它必須付出正確的代價，否則會把 v0.10 的側翼繞行整個架空。
>
> （版本號的順序是刻意的：v0.19 在 v0.20 之後才做，
> 原本標為 v0.19 的自動補給已改記為 v0.18 的附錄。）

> v0.20 的動機：實測發現 —— **玩家全滅之後，免費補滿。**
> 緊張感是真的，**但後果不是**。補給站什麼都 0 元，所以一次全滅的代價只有時間；
> 而搜刮回來的武器、DNA、值錢的東西從 v0.9 到現在一直躺在清單裡沒有用途。
>
> 接上去之後：一次全滅才會真的是一次全滅；
> 「要不要冒險回去撿屍體」第一次有可計算的答案；
> **合約清單的迴路才完整：標籤 → 準備 → 風險 → 報酬。**

> v0.18 附錄的動機：試玩回報 ——「撤離後背包的子彈會全部被收走，
> 導致再次出擊之前還要特別回去補。」
>
> 共用庫存的模型要留（v0.16 §21.3：配裝是**分配資源**，不是選裝備）。
> **要省掉的是重新分配的手工，不是那個決策。**

> v0.18 的動機：實測發現**空投下來的替補士兵沒辦法接手前一個人的裝備。**
> 他走到屍體旁邊，屍體上躺著一把無後座力砲 —— 但他自己的主手與收納欄都滿了，
> 而遊戲裡沒有任何動作可以騰出位置。
>
> 規格 §10.2 與 v0.9 §3.1 其實都預期這件事可行
> （「撿到的第三把武器可以放進背包，要先換到收納欄」），
> **只是這個「換到收納欄」的動作從來沒有被實作。**
>
> 這使本作的核心決策整個失效：玩家走過去之後，做不了任何事。

> v0.17 的動機：v0.16 之後每一場都有後果了，但畫面上仍然是「單位瞬間換格」——
> 玩家看不到動作的開始，只看得到結果。那正是 v0.4 建立回饋層時要解決的同一個問題，
> 只是這次的對象是移動本身。
>
> **同時要守住一條線**：移動動畫毀掉 roguelike 手感最常見的方式，
> 是玩家連按方向鍵趕路時每一步都要等動畫跑完。
> 所以 §22.1 的第一條規則不是「怎麼播」，是「**怎麼被打斷**」。

> v0.16 的動機：**專案至此蓋好的核心機制，在此之前全部沒有作用。**
> 陣亡掉落武器、屍體回收、帶著戰利品提早撤離、止損、名冊耗盡、搜刮與結算清單 ——
> 每一項的實際後果都是「無，下一場全部回來」。
>
> **「要不要冒險走回去撿自己的屍體」是本作的核心決策，而它到目前為止一次都沒有真正發生過。**
> 玩家回去撿，得到的是一個結算畫面上的數字，然後那個數字被丟掉。
>
> 這一版是把電接上去。

> v0.15 的動機：v0.14 的合約清單會告訴玩家「開闊地形、敵方以遠程為主、
> 存在重裝目標」——**然後玩家什麼都不能改變**。
> **有標籤而沒有配裝，比沒有標籤更糟。**這一版是把 v0.14 做完：標籤 → 準備 → 出擊。
>
> AR-9 與 RR-4 的數值一個字都沒動。它們是花了六個版本調出來的參考點，
> 重設計會讓所有既有的手感判斷失效。

> v0.14 的動機：v0.11 起隨機選圖，玩家會**毫無預警**被丟進性格差異極大的地圖。
> 「接哪張合約」應該是一個決策 —— 而決策需要資訊，資訊必須可信。
> 所以這一版最重要的一條規則不是畫面怎麼排，是 §18.2：
> **地形標籤與難度評級由統計值推導，不得手寫。**
> 手寫的標籤會在地圖被修改之後開始說謊，而地圖一定會被修改 ——
> v0.13 才剛把兩張圖整個換掉。統計值不會說謊，它跟著地圖一起重算。
>
> 這個畫面同時是本作的**敘事載體**：主線與所有角色感都由合約簡報、公文與服役紀錄
> 承擔，不做過場動畫。這裡是那些文字第一次有地方放。

> v0.13 的動機：局外層會把任務**串起來**。一場任務長度失控只是煩人，一場接一場就是災難；而合約清單一旦存在，壞掉的地圖會被反覆抽到。**這些修正必須在局外層開始之前完成。**
>
> v0.11 的三個對照實驗有兩個得到了否定的答案，這一版把結果落實回地圖上。

> v0.12 的動機：實測顯示開闊地圖的正確打法是脫離接觸、蹲下、找掩護、逐一孤立敵人。這代表**在火力下移動、承受傷害後退出交火**是會反覆出現的正常情境 —— 既然如此，就需要一個把傷害轉回可用狀態的手段，否則「撐過那一段」只能靠不受傷。
>
> **但恢復必須慢、必須不完全、必須稀有**（§4.1）。如果恢復又快又便宜，「士兵是消耗品」「死亡是支出而非失敗」「屍體回收是最重要的單一決策」這三件事會同時失去意義。慢是最重要的一條：40 時間讓它**無法在交火中進行**，玩家必須先脫離接觸 —— 而那正好就是這一版想支援的那個打法。
>
> 另一半是**丟棄**。v0.9 給了負重代價卻沒給釋放閥：整個指令表裡沒有丟棄，背包唯一會變輕的路徑是裝填時扣彈藥。撿了就再也放不下 —— 負重系統只裝了油門沒裝煞車。

> v0.11 的動機：專案至今所有的數值 —— 掩體密度、射程、視野、空投點間距、負重門檻、AI 權重 —— **全部是在同一組幾何上調出來的**。那是一個樣本。v0.10 新增的掩體運用與側翼繞行，在單一地圖上也測不出真實表現。
>
> **明確不做程序化生成。** 本作的整套戰術系統長在地形上：掩蔽等級、正交直線對射、面向盲區、側翼路線、空投點間距、噪音傳播 —— **地形不是背景，是規則的一部分**。隨機生成容易產出無聊或不公平的地圖，而且無法分辨問題出在規則還是那一張圖。
>
> 更根本的是：**生成器本質上是把「什麼叫好地圖」寫成程式，而那些判準目前還不存在。** 手刻地圖就是發現這些判準的過程；§13.5 的驗證工具會把發現的判準固化成可檢查的條件，將來直接成為生成器的約束。

> v0.10 的動機：戰術層有一整套詞彙，但**只有玩家會用** —— 掩蔽、側翼繞行、蹲姿與面向、背刺、射擊模式，敵人一項都不用。它們永遠站在開闊地、IDLE 時永遠不轉身、永遠不蹲下。
>
> 真正的問題不是太簡單，是**數值已經無法判讀**：基礎命中 0.55、掩蔽減免最高 −0.40，代表玩家躲好時是 15% 對 55%。從那裡開始的每一次調整都是對著壞掉的基準做的。
>
> 這一版刻意**只做 AI、不動地圖**。那張地圖已經被玩過很多次，是最好的對照基準；兩件事一起改就分不出難度變化是誰造成的。

> v0.9 的四件事互相咬合，不能拆開上：**射擊模式若沒有彈藥稀缺就不是決策**（火力大的模式永遠划算）；**搜刮若沒有容量上限就不是決策**（把地圖清空即可）；**戰利品若沒有撤離管道就沒有意義**。
>
> 這一版不做局外層：沒有出擊前配裝、沒有名冊管理、沒有經濟。帶出來的東西只列在結算畫面上 —— 但那兩份清單就是局外層的入口。

> v0.8 的動機：交戰以射擊為主，敵人卻常常因為射程關係落在畫面外。解法是鏡頭可以拖曳平移（§12.15），並趁這個機會把面向變成真正生效的規則 —— `facing` 從第一版就留著，一直沒有啟用。
>
> **面向只在蹲下時對玩家生效**。本作沒有平移（strafe）手段，若面向隨時生效、轉向又要收費，等於對所有移動課稅。限制在蹲姿時，面向只在單位大多靜止的狀態下起作用，移動完全不受影響。

> v0.7 的動機：AP 制把「快」和「慢」壓成同一個節拍——不管衝鋒型多快、重武器多重，一輪就是一輪。排程器讓速度變成一個真正的維度：快的單位在慢的單位動一次的期間會動很多次，這不需要額外的規則，是主迴圈的自然結果。**注意 v0.7 完全不動平衡數值**，所有時間成本都是 v0.6 的 AP 換算（1 AP = 10）。

> v0.6 的四項改動互相依賴，不能拆開上：單獨降命中只會讓戰鬥變長變無聊；單獨收彈匣只會綁手綁腳。**降低基礎命中不是要把遊戲變難，是要讓 v0.5 已經做好的掩蔽系統開始有意義**——修正項必須蓋過基礎值，位置才會成為交火的主題。

> v0.4 不改任何戰鬥規則：命中率仍固定 1.0、護甲仍是確定值、沒有掩體或姿勢的命中修正、傷害公式不變。放大數值與回饋層都是下一版（命中率、掩體、浮動傷害與護甲）的前置條件——個位數的整數承載不了少量浮動，而「打中但被擋下」和「沒打中」若在畫面上長得一樣，新的戰鬥數學就只能盲調。

> v0.3 的動機：原規格把射擊設計成「點敵人 → 彈出預覽面板 → 再點確認」。實測後確認這是錯誤的——射擊是全遊戲最高頻的動作，卻被配上了最重的互動形式。v0.3 把**所有地圖互動統一成同一條文法**，並縮減按鈕數量。

---

## 0. 給實作者的重要說明

**請嚴格遵守第 2 節的範圍界線。** 本文件第 16 節列出了大量已經設計好、但**這一版明確不要實作**的系統。它們寫在這裡的唯一目的，是讓你在設計資料結構與架構時不要把路擋死。看到它們不代表要做它們。

本專案的成敗完全取決於**單場戰鬥好不好玩**。所有的合成、經濟、多人系統在戰鬥手感確立之前都沒有意義。請把工程品質集中在 `core/` 規則層。

---

## 1. 設計核心

三句話說明這個 MVP 要驗證什麼：

1. **輪流下棋，但打起來要爽。** 玩家不必等一整輪：走一格是一次輪到你，開一槍也是一次輪到你（§5）。快的敵人真的會比你多動幾次，慢的重武器真的會讓你被多打幾下。
2. **掩體是二元的、可預測的。** 是否被打到取決於視線與姿勢，玩家看得到、算得出來。隨機性優先放在「局面」而不是「結果」。
   *MVP 階段命中率固定為 100%，但命中解算管線必須完整實作（§8.1），日後要能只改一個函式就打開擲骰。*
3. **死亡是支出而非失敗。** 士兵死了就從名冊丟下一個到最近的空投點，屍體與裝備留在原地。任務只有在名冊耗盡或玩家主動止損時才結束。

---

## 2. MVP 範圍

### 2.1 這一版要做的

- 四張手工繪製的地圖，任務開始時以可播種亂數選一張（§13.1～§13.2）
- 單一玩家單位在場上（格子、**四方向移動**）
- 行動排程器（時間軸，無回合、無 AP）
- 站／蹲兩種姿勢，與半身掩體的視線互動；蹲姿的視野由面向決定（§7.5）
- 兩把武器：一把輕武器、一把重武器；輕武器有三種射擊模式（§8.9）
- 消耗品與準備欄（§4）；背包管理與丟棄（§12.20）
- 有限彈藥、背包與負重（§4.4）
- 搜刮：地圖搜刮點、敵人屍體、己方屍體與 DNA（§10.4）
- 三種敵人：衝鋒型、裝甲型、射手型
- 敵人 AI：IDLE / ALERT / SEARCH 三態；落點評分、警戒巡視、口令宣告（§9.2～§9.5）
- 開火噪音警戒機制
- 死亡與空投點增援（名冊 4 人）
- 屍體與裝備遺留、可回收
- 主目標 1 個、次要目標 2 個
- 撤離與止損、任務結算畫面
- 觸控操作介面（統一的兩段式地圖點擊 + 方向鍵 + 動作鍵）

### 2.2 這一版明確不要做的

- 任何合成、DNA、世代、經驗值系統
- 任何經濟、貨幣、商店、副業、掛機收益
- 任何伺服器、帳號、連線、多人、大型合約
- 程序化地圖生成（地圖是手工資料檔）
- 音效、配樂、粒子特效、過場動畫
- 存檔／讀檔（單場任務打完就結束）
- 士兵之間的差異化（名冊 4 人數值完全相同）
- 任何技能內容（v0.3 只做技能鍵的展開／收合鉤子）

---

## 3. 技術規格

| 項目 | 規格 |
|---|---|
| 目標平台 | **手機瀏覽器**（iOS Safari / Android Chrome），直向 |
| 次要平台 | 桌面瀏覽器（鍵盤操作可選做，不強制） |
| 語言 | TypeScript |
| 渲染 | HTML5 Canvas 2D（不使用遊戲引擎） |
| 建置 | Vite |
| 測試 | Vitest |
| 外部相依 | 盡量為零。不使用 React／Phaser／Pixi。 |
| 效能目標 | 中階手機上 60fps；單一動作解算 < 16ms |

### 3.1 架構要求（重要）

```
src/
  core/          # 純函式規則層。不得 import 任何 DOM / Canvas / 瀏覽器 API。
    state.ts     #   GameState 型別定義
    commands.ts  #   Command 型別與 applyCommand()
    los.ts       #   視線計算
    combat.ts    #   傷害解算
    ai.ts        #   敵人決策
    rng.ts       #   可播種亂數
  render/        # Canvas 繪製，唯讀 GameState
  ui/            # 觸控輸入、HUD、按鈕
  data/          # 地圖、武器、敵人的資料檔（JSON）
  main.ts
```

**硬性要求：**

1. `core/` 必須是**純的、決定性的**。`applyCommand(state, command) => newState`。相同的初始狀態 + 相同的指令序列，必須產生完全相同的結果。
2. 所有亂數必須經過 `core/rng.ts` 的可播種產生器（xorshift128 或類似）。**禁止直接呼叫 `Math.random()`**。
3. 渲染層與 UI 層唯讀 `GameState`，不得直接改寫狀態，一律透過發出 Command。
4. 遊戲狀態必須可完整序列化為 JSON。

理由：這一版用不到，但之後的合約計分需要在伺服器端重跑同一套規則來驗證分數。現在把規則層弄髒，之後要重寫。

> **鎖定（§8.4）刻意不放進 `GameState`**。它純粹是介面狀態，不影響規則，放進去只會污染規則層。

---

## 4. 資料模型

```ts
type Vec2 = { x: number; y: number };
type Stance = 'STAND' | 'CROUCH';
type Facing = 'N' | 'NE' | 'E' | 'SE' | 'S' | 'SW' | 'W' | 'NW';
type TileType =
  | 'FLOOR'       // 可通行，不擋視線
  | 'WALL'        // 不可通行，永遠擋視線
  | 'HALF_COVER'  // 不可通行，視線規則見 §7
  | 'DROP_POINT'  // 可通行，增援落點
  | 'TERMINAL'    // 可通行，主目標互動點
  | 'SUPPLY';     // 可通行，次要目標互動點

interface Weapon {
  id: string;
  name: string;
  class: 'LIGHT' | 'HEAVY';
  damage: number;
  range: number;          // 最大射程（格，曼哈頓距離）
  magazine: number;       // 彈匣容量
  ammo: number;           // 目前彈藥
  fireTime: number;       // 開火時間成本（§5.2）
  reloadTime: number;     // 裝填總時間成本
  reloadSequence: string | null;  // 非 null 則裝填是系列動作，見 §5.5
  /** 可續行序列的進度：已完成幾步（§5.6）。**存在武器上**，不是存在單位上。 */
  reloadProgress: number;
  noiseRadius: number;    // 開火噪音半徑（格，曼哈頓距離）
  splash: number;         // 濺射半徑（曼哈頓距離），0 = 無
  // --- 命中相關：MVP 不生效，但欄位與管線必須存在，見 §8.1 ---
  accuracy: number;       // 基礎命中率 0..1
  optimalRange: number;   // 此距離內不衰減
  falloffPerTile: number; // 超出 optimalRange 每格扣除的命中率
}

interface Unit {
  id: string;
  faction: 'PLAYER' | 'ENEMY';
  archetype: string;       // 'SOLDIER' | 'RUNNER' | 'HULK' | 'SHOOTER'
  name: string;            // UI 與戰鬥紀錄顯示用
  pos: Vec2;
  hp: number;
  maxHp: number;
  armor: number;
  aim: number;             // 命中修正，MVP 一律 0，見 §8.1
  evasion: number;         // 迴避修正，MVP 一律 0，見 §8.1
  stance: Stance;
  facing: Facing;          // 八向。v0.8 起蹲姿與敵人的視野由它決定（§7.5）；移動仍只有四向
  sightRange: number;      // 曼哈頓距離
  equipped: Weapon | null;
  stowed: Weapon | null;
  backpack: Backpack | null;  // 背包（§4.4）。敵人沒有背包，一律 null
  aiState: 'IDLE' | 'ALERT' | 'SEARCH';
  lastKnownTarget: Vec2 | null;
  searchTimer: number;     // 剩餘搜索時間（時間量，不是次數）
  // --- 排程器（§5）---
  nextActAt: number;       // 下次可以行動的時刻
  moveTime: number;        // 移動一格的時間成本
  transitionTime: number;  // AI 狀態轉換的時間成本（§9.1）
  transitioning: boolean;  // 這次輪到它只做狀態轉換（給渲染層看，§9.2）
  pendingSequence: Sequence | null;  // 承諾中的系列動作（§5.5）
  /**
   * 準備欄（§12.19）：現在身上「隨手可用」的那一件消耗品的 item id。
   * 東西**仍然放在背包裡**，這裡只是一個標記 —— 所以重量、陣亡遺留、
   * 撤離帶出全部自動跟著背包走，不需要另一套規則。
   */
  preparedId: string | null;
  // --- v0.10 敵人戰術 AI（§9）---
  declared: Declaration | null;      // 已宣告、下次輪到必定執行的動作（§9.4）
  patrolLeft: number;                // SEARCH 收尾還要巡視幾次（§9.3）
}

/**
 * 敵人宣告的下一個動作（§9.4）。**宣告具有拘束力**：下次輪到它時執行這個，
 * 不重新評估。它是規則狀態（會決定下一次真的做什麼），所以進 GameState；
 * 顯示與動畫狀態不進（§12.18）。
 */
type DeclKind =
  | 'SPOT' | 'ADVANCE' | 'FLANK' | 'TAKE_COVER' | 'FIRE'
  | 'CROUCH' | 'RELOAD' | 'SEARCH_MOVE' | 'PATROL' | 'HOLD' | 'LOST';

interface Declaration {
  kind: DeclKind;              // 同時也是口令的理由碼（§9.5）
  to?: Vec2;                   // 移動類：要走到哪一格
  target?: Vec2;               // 射擊類：要打哪裡
  facing?: Facing;             // 巡視類：要轉到哪個方向
  side?: 'LEFT' | 'RIGHT';     // FLANK 專用：繞畫面的左邊還是右邊
}

interface Sequence {
  id: string;              // 對應 rules.json 的 sequences key
  index: number;           // 目前走到第幾步（0-based）
}

/**
 * 可搜刮的一堆東西（§10.4）。三種來源共用同一個型別、同一套兩段式點擊文法：
 * 己方屍體、敵人屍體、地圖搜刮點。
 * 分成三個型別的話，搜刮面板要寫三份、拾取指令要開三條路 —— 沒有理由。
 */
type LootKind = 'PLAYER_BODY' | 'ENEMY_BODY' | 'CACHE';

interface LootPile {
  id: string;
  kind: LootKind;
  pos: Vec2;
  label: string;             // UI 標題，例如「K-441 的遺體」「補給箱」
  items: Item[];
}

// ---- v0.9 物品與背包（§4.4）----

/**
 * 口徑（v0.15 §19.1）。口徑是**獨立的資料實體**（rules.json 的 calibres），
 * 武器以 calibre 欄位引用它 —— 新增武器只要指定口徑就自動與既有彈藥相容。
 * 共用是刻意的：AR-9 與 LMG-5 同吃 5.56、兩把霰彈槍同吃 12ga。
 */
type Calibre = '5.56' | '7.62' | '12ga' | '9mm' | '84mm';

/** 機構型式（v0.15 附錄 B §20.3）。本版不參與任何判斷。 */
type WeaponAction = 'BREAK' | 'PUMP' | 'BOLT' | 'SEMI' | 'AUTO' | 'BREECH';

/**
 * 彈藥型別（v0.15 附錄 B §20.2）。**彈藥的識別單位是型別，型別引用口徑。**
 * 型別 id 同時就是它在背包裡的 defId。修正欄位 v0.15 一律為預設值。
 */
interface AmmoType {
  name: string;
  calibreId: Calibre;
  weightPerRound: number;
  damageModifier: number;         // v0.15 一律 1.0
  falloffModifier: number;        // v0.15 一律 1.0
  splashRadius: number;           // v0.15 一律 0
  allowedActions: WeaponAction[]; // 空陣列 = 不限制機構；v0.15 一律為空
}
type FireMode = 'SINGLE' | 'BURST' | 'VOLLEY' | 'AUTO';
/** FULL 一次補滿；INCREMENTAL 一次一發，且**不是**系列動作（§8.10）。 */
type ReloadMode = 'FULL' | 'INCREMENTAL';
type ItemKind = 'AMMO' | 'VALUABLE' | 'DNA' | 'WEAPON' | 'CONSUMABLE';

/**
 * 詞條（v0.15 附錄 A）。**v0.15 一律為空，不實作任何效果。**
 * modifiers 的鍵對應 WeaponStats 的數值欄位，值為加法修正。
 */
interface Affix {
  id: string;
  name: string;
  modifiers: Record<string, number>;   // 例如 { accuracy: 0.05, reloadTime: -2 }
}

/**
 * 來歷（v0.15 附錄 A）。**v0.15 一律為空。**
 * actor **只存公司或勢力名稱，不得存放帳號或使用者識別** —— 這是多人時的隱私邊界。
 */
interface Provenance {
  event: string;      // 'MANUFACTURED' | 'RECOVERED' | 'SALVAGED' …
  actor: string;      // 公司或勢力名稱
  note?: string;
}

/** 一把槍的數值。型號與實例共用同一組欄位，差別在來源。 */
interface WeaponStats {
  name: string; class: WeaponClass;
  damage: number; damageSpread: number; penetration: number;
  range: number; magazine: number; calibre: Calibre; weight: number;
  modes: FireMode[]; fireTime: number; reloadTime: number; action: WeaponAction;
  reloadMode: ReloadMode; reloadSequence: string | null; swapTime?: number;
  noiseRadius: number; splash: number;
  accuracy: number; optimalRange: number; falloffPerTile: number;
}

/** 武器**型號**：data/weapons.json 的一筆。不可變，全世界共用一份。 */
interface WeaponType extends WeaponStats {
  id: string;
  ammo: number;    // 出廠預設
  mode: FireMode;  // 出廠預設
}

/**
 * 武器**實例**：世界上實際存在的那一把槍（v0.15 附錄 A）。
 * 玩家持有、掉落、拾取、遺落的都是實例。
 */
interface WeaponInstance extends WeaponStats {
  instanceId: string;   // 決定性來源（狀態流水號），不得用時間或 UUID
  typeId: string;
  ammo: number;
  mode: FireMode;
  reloadProgress: number;
  affixes: Affix[];      // v0.15 一律為空
  provenance: Provenance[];  // v0.15 一律為空
}

/**
 * 一名士兵的配裝（v0.16 §21.3）。
 * **武器欄位引用的是實例 id** —— 同一把槍不可能同時在兩個人身上。
 */
interface Loadout {
  equippedWeaponId: string | null;           // 引用 WeaponInstance.instanceId
  stowedWeaponId: string | null;
  ammo: Record<string, number>;              // **彈藥型別 id** → 發數（附錄 B）
  consumables: Record<string, number>;       // defId → 數量
}

/** 服役紀錄（§21.3）。**這是純數值士兵唯一的人格來源。** */
interface ServiceRecord {
  missions: number;
  kills: number;
  damageTaken: number;
  contracts: string[];
}

interface Soldier {
  id: string;
  designation: string;          // `編號-世代`，例如 K-441 Gen.1
  hp: number; maxHp: number;
  loadout: Loadout;
  serviceRecord: ServiceRecord;
}

/** 局外層的持久狀態（v0.16 §21.1）。**不得混入 GameState。** */
interface MetaState {
  schemaVersion: number;        // 版本不符就重置，不遷移（§21.7）
  instanceCounter: number;      // 實例識別碼的來源，必須一起持久化
  roster: Soldier[];
  armoury: WeaponInstance[];    // 公司持有的所有武器；「未指派」是算出來的
  ammoStock: Record<string, number>;        // ammoTypeId → 數量（共用庫存）
  consumableStock: Record<string, number>;
  salvage: Record<string, number>;          // 帶回來的雜物，本階段無用途
  missionLog: MissionRecord[];
}

/** 派遣快照（§21.1）。**任務唯一看得到的東西。** */
interface DeployedSoldier {
  id: string; designation: string;
  hp: number; maxHp: number;
  equipped: WeaponInstance | null;          // 深複製
  stowed: WeaponInstance | null;
  items: { defId: string; qty: number }[];  // 落地時才展開成 Item
}

/**
 * 消耗品的使用方式（§4）。**資料驅動**：之後新增穿甲彈、興奮劑、煙霧彈
 * 應該只需要改 items.json，不需要改程式。序列 id 直接用該消耗品的 defId。
 */
interface ConsumableUse {
  label: string;
  sequenceType: 'RESUMABLE' | 'RESTART';   // 消耗品一律 RESTART
  steps: { id: string; label: string; time: number }[];
  effects?: { kind: string; amount: number }[];
}

/** 武器也是物品（kind 為 'WEAPON'），這樣搜刮面板只要處理一種東西。 */
interface Item {
  id: string;
  kind: ItemKind;
  defId: string;             // 對應 data/items.json 的鍵；武器一律用 'WEAPON'
  name: string;
  weight: number;            // 每一個的重量。整堆 = weight × qty
  qty: number;
  ammoTypeId?: string;       // kind 為 'AMMO' 時。對彈藥而言 defId === ammoTypeId
  value?: number;            // kind 為 'VALUABLE' 時。局外層還不存在，只列在結算上
  weapon?: Weapon;           // kind 為 'WEAPON' 時的完整武器狀態
}

/**
 * 背包裡的武器不能使用，要先換到手持或收納。
 *
 * **v0.15：手持與收納的武器仍然不佔背包欄位，但開始計入負重**（`carriedWeight`）。
 * 以前不計，於是「多帶一把槍」是完全免費的 —— 那樣的話出擊前配裝就沒有取捨，
 * 只剩「全都帶」。負重級距與攜行上限現在都看 `carriedWeight`。
 */
interface Backpack {
  items: Item[];
}

interface Objective {
  id: string;
  pos: Vec2;                 // 互動時要比對座標，所以必須帶位置
  done: boolean;
}

interface GameState {
  clock: number;             // 世界時刻（§5.1）。沒有 turn，也沒有 phase。
  map: MapData;
  units: Unit[];
  loot: LootPile[];          // 戰場上所有可搜刮的東西（§10.4）
  roster: string[];          // 尚未投入的士兵 id
  activePlayerUnitId: string | null;
  objectives: {
    main: Objective;
    secondary: Objective[];
  };
  casualties: number;
  deployed: number;          // 累計投入士兵數（結算畫面用）
  rngSeed: number;
  rng: RngState;             // 產生器完整內部狀態，隨 state 序列化（§14）
  result: 'ONGOING' | 'SUCCESS' | 'ABORTED' | 'WIPED';
  pendingReinforcement: { deathPos: Vec2; deadUnitId: string } | null;
  /**
   * 撤離時帶出去的東西（§11.2）。只有真的走出撤離點才會有內容；
   * 止損與全滅一律是空的 —— 那兩種情況戰場上的一切都損失。
   */
  extracted: Item[];
  nextEntitySerial: number;  // 決定性的 id 產生器，不用亂數
  log: LogEntry[];           // 戰鬥紀錄（§8.1 要求能顯示未命中）
}
```

---

## 5. 行動排程器（取代回合與 AP）

> **v0.7 起，「回合」與「AP」都不存在了。** 沒有 `turn`、沒有 `phase`、沒有 `ap`／`maxAp`、沒有「敵人回合」這個資料結構。單位不是輪流打完一整輪，而是各自在時間軸上排隊。

### 5.1 主迴圈

`GameState` 只有一個 `clock`（世界時刻，從 0 開始的整數）。每個 `Unit` 有一個 `nextActAt`：它下一次可以行動的時刻。

主迴圈永遠是同一件事：

1. 找出所有存活單位中 `nextActAt` 最小的那一個 —— 這就是 **當前行動者**。
2. 把 `clock` 推進到它的 `nextActAt`。
3. 它執行**一個**動作。
4. `nextActAt = clock + 該動作的時間成本`。
5. 回到 1。

也就是說：**一個動作 = 一次輪到你**。走一格是一次，開一槍是一次。沒有「這回合還剩多少 AP」可以攢，也沒有「等這一輪結束」。快的單位在慢的單位動一次的期間會動很多次，這是排程器唯一的產物，不需要另外寫規則。

**同時刻的先後（決定性 tie-break）**：兩個單位的 `nextActAt` 相同時，依序比較

1. `nextActAt` 小的先；
2. 玩家陣營先於敵方陣營；
3. 都相同則 `units` 陣列索引小的先。

這三層比較完全決定，沒有任何隨機成分——同一個種子必然重播出同一段戰鬥（§14）。玩家優先是刻意的：時間打平時把行動權給玩家，玩家才不會覺得自己被系統偷了一手。

**零成本動作不讓出行動權**：時間成本為 0 的動作（姿勢、面向）執行完之後 `nextActAt` 不變，當前行動者仍然是自己。**AI 絕對不可以選擇 0 成本動作**，否則主迴圈會無限迴圈。

**`ADVANCE` 指令**：當前行動者不是玩家時，UI 送出 `ADVANCE`，規則層讓當前的敵人走一個動作。這取代了 v0.6 的 `ENEMY_STEP`。玩家沒有「結束回合」鍵——因為沒有回合可以結束。

### 5.2 時間成本表

基準單位是 **「移動一格 = 10」**。所有數值來自 `data/rules.json` 的 `time` 與 `data/actors.json`／`data/weapons.json`，程式碼中不得寫死任何時間值。

| 動作 | 時間成本 | 來源 |
|---|---|---|
| 移動 1 格（四方向） | 單位的 `moveTime`（複製兵 10） | `actors.json` |
| **翻越半身掩體**（v0.19） | `time.vault`（**20**），一次兩格、落地強制站姿 | `rules.json` |
| 輕武器開火（AR-9） | 武器的 `fireTime`（10） | `weapons.json` |
| 輕武器裝填（AR-9） | 武器的 `reloadTime`（10） | `weapons.json` |
| 重武器開火（RR-4） | 武器的 `fireTime`（20） | `weapons.json` |
| 重武器裝填（RR-4） | 20，但拆成兩步序列（§5.5） | `rules.json` |
| 換裝（主手 ↔ 收納欄） | 該武器的 `swapTime`，未寫則 `time.swap[class]`（輕 10／重 20） | `weapons.json` |
| **主手 → 背包**（v0.18） | 同上，依**被搬動的那一把** | `weapons.json` |
| **收納欄 → 背包**（v0.18） | 同上 | `weapons.json` |
| **背包 → 收納欄**（v0.18） | 同上 | `weapons.json` |
| 從屍體拾取武器 | `time.pickup`（10） | `rules.json` |
| 與目標物互動（終端／補給箱／撤離） | `time.interact`（10） | `rules.json` |
| 待機 | `time.wait`（10） | `rules.json` |
| **準備物品**（放進準備欄） | `time.prepare`（10） | `rules.json` |
| 使用消耗品 | 該消耗品的序列總和（封合劑 40） | `items.json` |
| **丟棄物品** | `time.drop`（**0**） | `rules.json` |
| **改變姿勢（站↔蹲）** | `time.stance`（**3**，v0.8 起） | `rules.json` |
| **改變面向** | **0** | `rules.json` |
| 敵人狀態轉換（§9.1） | 單位的 `transitionTime` | `actors.json` |
| 增援落地後的第一次行動延遲 | `time.deploy`（10） | `rules.json` |

**待機為什麼要收 10**：如果等待很便宜，玩家就能不斷「等一下下再看看」，永遠讓敵人先出手，白拿一個後手優勢。待機等同於移動一格，是為了讓「原地不動」跟「換個位置」在時間上等價——選哪個是戰術問題，不是划算問題。

**丟棄免費、準備收費，這個不對稱是刻意的。** 丟東西比撿東西快，而且緊急時減重不該被時間懲罰 —— 負重直接等於被追擊時的生存率。反過來，**準備如果免費，準備欄就只是多一次點擊**：玩家會在需要時免費換上想要的東西，等同於直接從背包使用。收 10 之後它與「現在手上拿的是哪把槍」完全對稱：**你身上現在準備好的是什麼，是一個要提前做、事後才發現對不對的決定。**

**面向免費是刻意的**：轉向的代價在能力限制（§7.5），不在時間。不要「順手」給轉向加上時間成本。

**v0.18 的三個搬裝備動作（§23）**：三個位置的東西**全部計入負重**，
所以搬動**不改變身上的總重** —— 改變的是「拿不拿得到」。
也因此它必須花時間：**在敵火下騰位置應該是件蠢事，在掩體後應該是可行的。**
花費依被搬動的那一把決定（v0.15 起寫在武器上；未寫則退回類別預設的輕 10／重 20）。

**姿勢從 v0.8 起收 3，也是刻意的。** 若站起來是免費的，玩家的掃視流程會變成「站起來看一圈、蹲回去」——**免費取得全方位資訊**，蹲姿的面向限制等於不存在，只多了兩次點擊。通則是：**任何免費的動作，只要能解除資訊限制，它就是一次免費掃視。** 收 3 之後掃一圈要花 6（起身 + 蹲下），約等於半次移動，「要不要花這個代價確認周圍」因此成為一個真正的決定。

**重武器不再「吃掉整個回合」**：v0.6 的重武器是 2 AP，等於開一槍就結束回合。v0.7 換算成 `fireTime: 20`，效果相同（別人動兩次你才動一次），但機制上更誠實——它慢，不是它「用完了什麼」。

### 5.3 玩家看得到什麼

HUD 顯示 **世界時刻 `T`**，按鈕上顯示的是**該動作的時間成本**，不是 AP 點數。移動預覽顯示整段路徑的總時間。

**不顯示任何敵人行動順序的資訊。** 沒有時間軸、沒有「下一個行動者」列表、沒有敵人頭上的倒數。玩家要靠觀察和推理去判斷誰快誰慢，這是資訊差的一部分。實作者不要「順手」加一個貼心的行動順序提示。

### 5.4 這對戰術的實際影響

- **快的敵人真的快**。衝鋒型 `moveTime: 7`，玩家走三格的時間它能走四格多。距離不再是安全感的可靠來源。
- **重武器的代價變成時間曝險**。開一槍 20，等於讓所有人比你多動一次。RR-4 的問題不是「打不到」，是「打完之後你還站在那裡」。
- **反應窗口是時間窗口，不是保證的一個動作**。§9.2 的兩段式察覺讓敵人先花 `transitionTime` 轉入警戒才開火——但那是一段時間，不是「玩家保證能動一次」。轉換時間短的單位（衝鋒型 5）可能在玩家的一個動作（10）之內就完成轉換並開火。這是設計，不是漏洞：對付快的東西要靠先手，不是靠反應。

### 5.5 系列動作（Sequence）

某些動作太重，不該用單一的大時間成本表現，因為那會讓單位在一段長時間裡「不存在」。系列動作把它拆成數個步驟：**時間照樣一步一步花掉，但效果只在整套走完的那一刻發生**。

- 單位開始序列時，`pendingSequence = { id, index }`。開始本身不花時間。
- 承諾期間，該單位**只能**執行 `SEQUENCE_STEP`（走下一步）或 `ABORT_SEQUENCE`（中止）。其他任何指令都是非法的。
- 每個 `SEQUENCE_STEP` 花掉該步驟的時間，`index++`。走完最後一步時，效果才發生，`pendingSequence` 清空。
- **中止**：`pendingSequence` 清空，效果不發生，**已經花掉的時間不退還**，中止動作本身不花時間。
- **單位死亡時序列連同單位一起消失**，效果不發生。

**RR-4 裝填**（`RR4_RELOAD`）兩步各 10，總計 20 —— 與單一動作的 `reloadTime` 相同。差別在於中間那 10 個時間單位裡，玩家是可以被打斷的。AR-9 的裝填是單一動作，沒有序列。

**UI 要求**：承諾中的單位必須在畫面上看得出正在蓄勢——橫幅顯示「裝填 RR-4：開栓退殼（1/2）　還要 10」，裝填鍵變成「續」並顯示下一步的時間，其餘動作鍵全部禁用。中止的入口放在點自己的詳細狀態卡片裡，並明寫「已花費的時間不退還」。

### 5.6 兩種中斷行為（v0.12）

每個序列標註被打斷之後會怎樣：

| 類型 | 中斷後 | 適用 |
|---|---|---|
| **可續行** `RESUMABLE` | **進度保留**，下次從中斷的步驟接續 | 機械性動作：重型武器裝填 |
| **須重來** `RESTART` | 進度歸零，已花費的時間不退還 | 生理性、精密性動作：治療、瞄準 |

**判準**：已經完成的物理狀態改變會不會自己消失。退殼退了就是退了，不會自己裝回去；包紮包到一半被打斷，那塊敷料就廢了。

**可續行的進度存在物件上，不是存在單位上。** RR-4 的裝填進度是 `Weapon.reloadProgress`，因此：

- 退殼完成後被打斷 → 該武器處於「已退殼、未裝填」狀態，**不可使用**（`canAttack` 直接擋下）
- 收起武器、換另一把、之後再換回來 → 進度仍在
- 移動到掩體後再繼續 → 從下一步接續

這產生一個很好的戰術狀態：**你開始裝填、挨了一輪、退回掩體、接著裝完。** 總時間沒變，改變的只是「投入的時間不會因為被打斷而白費」。

> 這會讓重型武器比先前好用一些。這是刻意的 —— 重武器應該是「有代價的選擇」，而不是「被打斷就白花二十時間」的懲罰。承諾的張力改由 `RESTART` 類的動作承擔。

**所有消耗品一律 `RESTART`。** 這一條讓「在掩體後包紮」與「在火線上包紮」變成兩件完全不同的事。

> v0.3 起取消斜向移動，原 §5.3「斜向切角」整節刪除。該節正文（兩側皆為阻擋物才禁止）與括號註記（禁止切角）互相矛盾；取消斜向後此問題自然消失。

---

## 6. 移動與地形

- **四方向移動**（上下左右），一次一格，每格花掉該單位的 `moveTime`（§5.2）。
- `WALL` 與 `HALF_COVER` 不可通行。
- 單位不可重疊。
- 屍體不阻擋移動，可以走上去。
- 地圖邊界視同 `WALL`。

> **所有「距離」一律指曼哈頓距離 `|dx| + |dy|`**：移動、射程、視野、噪音半徑、濺射半徑、AI 判斷全部同一套標準。取消斜向之後，走過去要幾步，距離就算幾格。

---

## 7. 視線、掩體與姿勢（本作的戰術核心）

這一節是整個遊戲的心臟，請務必照實作。

> **v0.8 起「看得見嗎」是三個獨立的檢查，不是不一致：**
>
> | 檢查 | 回答的問題 | 演算法 | 對稱？ | 在哪裡 |
> |---|---|---|---|---|
> | **距離**（§6） | 夠不夠得到 | 曼哈頓距離 | 是 | `grid.ts` |
> | **遮擋**（§7.2） | 中間有沒有東西擋著 | 幾何直線（對稱 Bresenham） | **是** | `los.ts` |
> | **面向**（§7.5） | 我有沒有朝那邊看 | 半平面 | **否** | `sight.ts` |
>
> 對稱性是**遮擋**的正確性條件；不對稱則是**面向**的全部意義 —— 背刺（§8.8）就是靠這個不對稱成立的。三者混在同一個函式裡的話全都會壞掉。
>
> **不要**把遮擋的射線改成沿正交路徑折線行走。折線有多條走法、無法唯一決定，會讓半身掩體的雙向阻擋規則產生不確定的結果。

### 7.1 視線演算法

使用**對稱**的視線判定。實作 Bresenham 直線，並要求「A 看得到 B」與「B 看得到 A」永遠一致（若兩方向結果不同則判定為不可見）。非對稱視線是這類遊戲最常見的 bug 來源。

### 7.2 遮蔽：看不看得見（射線，二元）

> **「遮蔽」與「掩蔽」是兩個互不相干的檢查，不要混在一起。**
>
> | | 回答的問題 | 演算法 | 結果 |
> |---|---|---|---|
> | **遮蔽**（§7.2） | 看不看得見 | 幾何射線 | 二元 |
> | **掩蔽**（§7.2b） | 好不好打中 | 目標鄰格掃描 | 三級 |
>
> 掩蔽刻意不走射線：現有模型裡射線通過 `WALL` 就直接阻擋，不存在「射線切到牆但仍看得見」的中間狀態，所以要讓牆提供掩蔽，來源必須是另一個檢查。

| 地形 | 視線效果 |
|---|---|
| `WALL` | 永遠阻擋視線 |
| `HALF_COVER` | 依兩端單位的姿勢決定，見下 |

**半身掩體規則：**

> 一條視線若通過某個 `HALF_COVER` 格，且**線段任一端的單位正緊鄰（8 鄰域）該掩體格並處於 `CROUCH` 狀態**，則該視線被阻擋。

這裡的「8 鄰域」是**幾何遮擋判定的一部分**，不是距離比較，因此不受 §6 的曼哈頓規則影響。

推論出來的實際效果（這正是我們要的）：

- **蹲在半身掩體後 → 你看不見外面，外面也看不見你。**（雙向阻擋）
- **站起來 → 你可以越過掩體開火，但在敵人行動的那段時間裡你是暴露的。**
- 因為改變姿勢免費，玩家每個動作之後的真正決策變成：**在下一個人動之前，我要站著還是蹲著？**

### 7.2b 掩蔽：好不好打中（鄰格掃描，三級）

對目標 T 與射手 S：

1. `dx = S.x − T.x`、`dy = S.y − T.y`
2. 找出 T 的正交鄰格中**朝向射手**的那些（最多兩格）：`dx > 0` 取東鄰、`dx < 0` 取西鄰、`dx == 0` 水平方向無候選；`dy` 同理
3. 數其中地形為 `WALL` 或 `HALF_COVER` 的格數。**地圖界外視同 `WALL`**

| 阻擋鄰格數 | 掩蔽等級 | 命中率修正 |
|---|---|---|
| 0 | 無掩蔽 | 0 |
| 1 | 部分掩蔽 | −0.25 |
| 2 | 良好掩蔽（牆角） | −0.40 |

`WALL` 與 `HALF_COVER` 在掩蔽上**一視同仁**。兩者的差別只存在於遮蔽：蹲在半身掩體後會整個消失，蹲在牆邊不會。

**為什麼是三級列舉，不是布林也不是純疊加：** 純布林讓縮在牆角與躲在單一掩體後完全相同，地圖設計少掉一個維度；純疊加沒有上界，掩蔽加蹲姿加射程衰減會把命中率壓到下限、雙方互相打不中、時間空轉——這是戰術遊戲變得拖沓的典型死法。三級列舉保留角落的價值，上界由結構本身保證。**不要**改成累加每一格鄰接掩體的實作。

**側翼是掩蔽的解法。** 掩蔽取決於掩體位於目標朝向射手的哪一側，因此射手換一個角度就能消除或降低目標的掩蔽。這是本機制最重要的戰術產出。

兩個預期中的推論（不是 bug）：

1. **正交直線對射時通常沒有掩蔽。** 若目標朝向射手那一側的鄰格是阻擋物，那格必然在射線上，結果是遮蔽（完全看不見）而非掩蔽。走廊裡的正面對射因此非常致命——這是設計意圖，也讓繞到正交線上成為一種進攻手段。
2. **掩蔽與蹲姿會疊加。** 上界由命中率下限保證（§8.1）。

### 7.3 姿勢的完整效果

| | 站 | 蹲 |
|---|---|---|
| 被命中率 | 基準 | **降低** −0.15 |
| 自己射擊命中率 | 基準 | **提高** +0.10（穩定度） |
| 視野**方向** | 全方位 | **只有面向的前方半平面**（§7.5） |
| 視野**範圍** | 基準 | **同基準**（v0.8 移除了 ×0.6 係數） |
| 射線上有半身掩體時 | 有掩蔽，可被射擊 | 完全看不見（§7.2） |

- 姿勢**不影響**傷害與移動成本。
- **姿勢改變花 `time.stance`（3）**（§5.2）。轉向仍然免費。
- 移動不會自動改變姿勢。蹲著也可以移動（成本相同）。

**蹲姿的代價從「看多遠」換成「看哪裡」。** v0.5 用視野 ×0.6 當代價，那是在面向還沒生效時的補丁。v0.8 移除它，理由有兩個：與 180 度視野疊在一起會把蹲姿削過頭；而且它懲罰的是「看多遠」，面向懲罰的是「看哪裡」—— 後者才是要玩家做的決定。

**蹲姿現在有兩個代價**：面向盲區（§7.5）與姿勢時間（§5.2）。兩者缺一不可 —— 只有盲區而姿勢免費的話，玩家會用「站起來看一圈、蹲回去」把盲區抵銷掉。

### 7.4 偵測

敵人在輪到自己時（以及移動的每一步後）檢查：若對玩家單位有視線且**曼哈頓距離** ≤ `sightRange`，則進入 `ALERT` 並更新 `lastKnownTarget`。

**v0.8 起面向會影響視野**，規則見 §7.5。偵測的完整條件是：曼哈頓距離 ≤ `sightRange`、目標在觀察者的視野方向內、且有視線。

實作上這三件事合成一個 `canSee(map, observer, target)`（`core/sight.ts`）。**它不對稱**，不要拿它當「互相看得見」用。

### 7.5 面向視野（v0.8）

| 姿勢 | 視野方向 |
|---|---|
| **站立** | 全方位（面向不影響任何規則） |
| **蹲下** | **面向的前方半平面**，含垂直於面向的那一條線 |

**所有敵人一律套用蹲姿的規則**（前方半平面），無論其姿勢為何。這是刻意的不對稱：敵人永遠有盲區，玩家只在蹲下時有（見 §9 的「已知的難度偏移」）。

判定式就是一個內積：`(target − observer) · DIR_VEC[facing] >= 0`。

**「含垂直線」是刻意的**，它自動長出正確的形狀：

- 面向北時，正東與正西的**相鄰格看得見**
- 後方三格（後、後左、後右）**是盲區**
- 整個後方半平面都看不見

不含那條線的話，蹲下就看不到自己身旁兩格，蹲在牆邊會瞎得很莫名其妙。反過來說，半平面本身已經夠了，**不需要**再補一條周邊視覺規則。

**這一層不動遮蔽。** `los.ts` 一行都沒改，仍然對稱。面向是疊在上面的第三個檢查。

---

## 8. 武器與戰鬥解算

### 8.1 命中解算管線

v0.5 起**已啟用擲骰**。開關在 `data/rules.json` 的 `combat.enableToHitRoll`，設為 `false` 可退回「一律必中」做對照測試。

#### 合法性檢查（先於解算）

攻擊必須同時滿足，否則為非法指令，UI 應事前禁用而非讓玩家打空：

1. 目標在射程內（**曼哈頓距離** ≤ `weapon.range`）
2. 攻擊者對目標有視線（§7）
3. 彈藥足夠，且該單位目前不在承諾中的系列動作裡（§5.5）

#### 命中率函式

```
命中率 = clamp(
    weapon.accuracy
  + attacker.aim
  + 射手姿勢修正          （蹲 +0.10）
  + 背刺加成              （+0.30，§8.8）
  - target.evasion
  - 目標姿勢修正          （蹲 −0.15）
  - 掩蔽修正              （部分 −0.25 / 良好 −0.40，§7.2b；**背刺時為 0**）
  - 射程衰減              （max(0, 曼哈頓距離 − optimalRange) × falloffPerTile）
, 下限, 1.0)
```

**命中率下限訂在 0.15 而非 0.05 是刻意的。** 掩蔽與蹲姿會疊加，最壞情況是良好掩蔽加蹲姿加斜向射程衰減。命中率觸底時的正確解法是換位置，但如果玩家當下換不了位置，5% 等於要他空轉二十次。0.15 讓僵局仍然會被打破，只是很慢。

所有修正項都在 `data/rules.json` 的 `combat.stance` 與 `combat.cover` 底下。

#### 解算流程（必須照這個順序實作）

```ts
export function resolveAttack(state, attackerId, targetPos, weapon): AttackResult {
  const chance = toHitChance(...);
  // 無論 chance 是多少，都必須抽一個亂數。
  // 這讓 MVP 與日後啟用擲骰時的 RNG 序列長度一致，重播不會錯位。
  const roll = rng.next(state);          // 0..1
  const hit = roll < chance;
  ...
}
```

**「即使必中也要抽亂數」是刻意的。**

#### 解算結果型別

```ts
interface AttackResult {
  hit: boolean;
  roll: number;          // 保留，供除錯與戰鬥紀錄顯示
  chance: number;        // 保留，同上
  impactPos: Vec2;       // 實際彈著點。命中時 = 目標格；MVP 階段永遠等於目標格
  damageByUnit: { unitId: string; amount: number }[];
}
```

#### 未命中路徑

未命中時：不造成傷害、照常花掉時間與彈藥、照常產生噪音、戰鬥紀錄與 UI 必須能正確顯示「未命中」。

驗收時會以測試強制命中率回傳 0，驗證整條未命中路徑走得通（見 §15）。**不要用 `if (hit)` 把未命中寫成什麼都不做的空分支。**

### 8.2 傷害計算

```
傷害擲值 = weapon.damage ± weapon.damageSpread      （均勻整數）
護甲擲值 = target.armor  ± target.armorSpread       （均勻整數，**每一發各自擲一次**）
實際傷害 = max(minDamage, 傷害擲值 − max(0, 護甲擲值 − weapon.penetration))
```

**護甲每一發都擲，不是單位生成時擲一次。** 這讓「防護穩定」與「品質參差」成為兩種可以區分的敵人特質——同樣平均 20 的護甲，`20 ± 2` 與 `20 ± 8` 打起來的感覺完全不同。生成時擲一次會把這個維度壓成一個數字。

**保底傷害是資料值**（`combat.minDamage`，目前為 `10`），不得寫死。它與生命值同單位：生命值規模變動時保底也要跟著調，否則等於實質取消保底。

**`penetration` 本次一律為 0**，只留欄位與公式。穿甲彈藥將來會做成**稀有消耗品**而非武器固定屬性——否則輕武器可以繞過換重武器的決策，裝甲型敵人的設計任務就失效了。

濺射武器：對中心格全額計算，對**曼哈頓半徑**內其他單位造成 `floor(傷害擲值 / 2)`，各自再擲一次護甲，同樣扣減、同樣保底。濺射會傷到玩家自己的單位，**不要**加入友軍傷害豁免。

### 8.3 噪音

任何開火動作會在 `noiseRadius`（曼哈頓）內產生噪音。所有在半徑內且處於 `IDLE` 的敵人轉為 `SEARCH`，並將開火位置設為 `lastKnownTarget`。噪音不受牆壁阻擋（刻意簡化）。

### 8.4 鎖定（v0.3）

射擊沒有專屬按鈕，一律透過地圖點擊執行（§12）。介面層維護一個「目前鎖定的目標」：

- 第一次點擊一個敵人 → **鎖定**。鎖定**不消耗任何資源**（不花時間、不花彈藥、不推進 `clock`）
- 第二次點擊**同一個**目標 → 開火，**且鎖定保留**
- 點擊**另一個**敵人 → 改變鎖定，不開火、不消耗任何資源
- **鎖定會一直保留**：若目標仍存活、仍在視線與射程內，下次輪到玩家時鎖定仍在，該目標的第一次點擊即為開火
- 目標失效（死亡、脫離視線或脫離射程）時解除鎖定，並清除瞄準線

鎖定是介面狀態，**不放進 `GameState`**（§3.1）。

### 8.5 武器資料（v0.15：七把）

放在 `data/weapons.json`。**AR-9 與 RR-4 的數值在 v0.15 完全未動**，只補上
`calibre` 與 `reloadMode` 兩個新欄位，並依 §19.2 的換算尺重設重量。

| | AR-9 | RR-4 | SG-12P 泵動 | SG-12S 削短 | DMR-7 精確 | LMG-5 輕機槍 | P-9 手槍 |
|---|---|---|---|---|---|---|---|
| `calibre` | 5.56 | 84mm | 12ga | 12ga | 7.62 | **5.56** | 9mm |
| `class` | LIGHT | HEAVY | LIGHT | LIGHT | LIGHT | **HEAVY** | LIGHT |
| `damage` | 30 | 120 | 55 ±8 | 70 ±10 | 55 ±8 | 28 ±5 | 22 ±4 |
| `range` | 8 | 12 | 5 | 3 | 16 | 9 | 5 |
| `optimalRange` | 5 | 8 | 2 | 1 | 14 | 5 | 3 |
| `falloffPerTile` | 0.08 | 0.10 | **0.20** | **0.30** | **0.05** | 0.09 | 0.12 |
| `accuracy` | 0.55 | 0.50 | 0.70 | 0.80 | 0.50 | 0.48 | 0.60 |
| `magazine` | 8 | 1 | 5 | 2 | 5 | 30 | 10 |
| `fireTime` | 10 | 20 | 10 | 10 | 14 | 10 | **6** |
| `reloadTime` | 10 | 20 | **6／發** | 20 | 14 | **30** | 6 |
| `reloadMode` | FULL | FULL | **INCREMENTAL** | FULL | FULL | FULL | FULL |
| `swapTime` | 10 | 20 | 10 | 8 | 12 | **20** | **5** |
| `action` | AUTO | BREECH | PUMP | **BREAK** | SEMI | AUTO | SEMI |
| `noiseRadius` | 6 | 14 | 10 | 12 | 14 | 10 | 5 |
| `modes` | 單／點／連 | 單 | 單 | **單／齊射** | 單 | 點／連 | 單／點 |
| `weight` | 7 | 20 | 7 | 5 | 11 | 15 | 2 |
| `penetration` | 0 | 0 | 0 | 0 | 0 | 0 | 0 |

> **衰減曲線是這批武器的主軸，不是傷害數字。**`optimalRange` 與 `falloffPerTile`
> 兩個欄位早就存在卻幾乎沒被用到；這一版讓它們承擔全部的角色分化。
> 以命中率下限 0.15 計（下列為武器基礎值，不含單發模式的 +0.10）：
>
> - **SG-12S** 1 格 0.80、2 格 0.50、3 格 0.20 —— 貼身之王，四格外等於沒有
> - **SG-12P** 2 格內 0.70，5 格觸底 —— 比削短型多一點餘裕
> - **DMR-7** 到 14 格完全不衰減，但基礎命中只有 0.50。它的長處不是準，是**遠處也一樣準**
> - **P-9** 3 格內 0.60，5 格 0.36

> **散佈不使用多發判定。**霰彈的散佈以「單次判定 + 高傷害 + 陡峭衰減」表現。
> 衰減曲線已經在做距離衰減，再疊一層多發判定等於雙重衰減，而且變異來源疊了兩個。
> 多發判定保留給射擊模式專用 —— 兩個機制維持正交。唯一的例外是削短型的齊射（§8.10）。

> **兩件不需要特別規則的事。**削短型齊射兩發 (70−20)×2 = 100，足以擊殺 90 血的重甲兵；
> 但重甲兵近戰 60±10、玩家只有 60 血 —— 貼上去就是兩下之內分勝負。
> 這個限制由既有數值自然產生。**手槍為什麼會被選**：步槍打空後裝填再開火要 20，
> 換手槍再開火只要 11 —— 手槍是「沒空裝填」的答案，加上極輕，
> 是想多背戰利品時收納格的首選。

> 射程是曼哈頓距離。射程 8 的武器在正前方可打 8 格，在斜 45 度方向只剩 4 格——這是取消斜向移動的預期結果，不是 bug。也因此**會出現「看得到但打不到」的敵人**：UI 不給它合法目標框，但仍然看得見它。

士兵初始狀態由**出擊前配裝**決定（§19）。預設配裝為手持 AR-9、收納 RR-4、
背包帶 24 發 5.56、2 發 84mm 與一支封合劑 —— 也就是 v0.14 以前那一組。

> **AR-9 彈匣 4 → 8 是被射擊模式逼出來的必要調整，不是平衡調整。** 連發一次消耗 3 發，若彈匣仍為 4，玩家會有一半的行動花在裝填上。8 發在混用模式下大約每三到四次行動裝填一次 —— **這個數字需要試玩驗證**。

### 8.9 射擊模式（v0.9）

三種模式**時間花費完全相同**（AR-9 開火 = 10），差別只在耗彈與命中。

| 模式 | 耗彈 | 判定次數 | 命中修正 |
|---|---|---|---|
| **單發** SINGLE | 1 | 1 | **+0.10** |
| **點放** BURST | 2 | 2 | 0 |
| **連發** AUTO | 3 | 3 | **−0.10** |

**每一發獨立判定（重要）**：連發**不是**「一次判定但命中率較高」，而是**三次各自獨立的完整判定** —— 各自擲命中、各自擲傷害、各自擲護甲。這讓連發同時具備傷害上限（三發全中即三倍）與高變異，「我需要它現在就死」因而成為一個真正的選項。

**預期的取捨**（以基礎命中 0.55 為例，期望命中數：單發 0.65、點放 1.10、連發 1.35）：

- **連發**期望傷害最高，但每發子彈的效率最低
- **單發**每發子彈的效率最高，適合彈藥吃緊時
- **重甲目標非常吃彈藥**：每發淨傷從約 30 掉到約 11，同樣要打掉它得燒三倍的子彈。這是換用重武器的另一個理由，與時間無關

**重武器沒有模式**：RR-4 只有單發。射擊模式是輕武器的專屬機制。

**自動降級（§8.9a）**：槍內子彈不足以支撐當前模式時，**自動降級**為最高的可負擔模式（連發不足則點放，再不足則單發）。模式按鈕與攻擊預覽必須以醒目樣式顯示**實際會使用的模式**。不要讓玩家在彈藥不足時無法開火，也**不要無聲降級**。玩家選定的模式不變，補彈之後就回到原本的火力。

### 8.10 增量裝填與齊射（v0.15）

**增量裝填**（`reloadMode: 'INCREMENTAL'`，目前只有 SG-12P）：
一次裝填動作只補一發，每發 6。從空到滿要五次行動，
但**在任何一發之後都可以直接開槍**。

> **這不是系列動作，不要用 `RESUMABLE` 實作。**
> 每一發都是一個完整結束的動作，沒有進度要保存，也沒有「承諾」可以被打斷。
> 這產生的手感正是泵動散彈槍該有的：**壓力下就填一發打一發，安全時才填滿。**

削短型剛好是它的反面（一次填兩發、單一動作、費時 20），
兩把槍因此**性格對立而不只是數值不同**：零承諾 vs 高承諾。

**齊射**（`VOLLEY`）沿用既有的射擊模式切換按鈕，與單發／點放／連發同一套操作：

| 模式 | 耗彈 | 判定 | 命中修正 |
|---|---|---|---|
| 單發 | 1 | 1 次 | +0.10 |
| 點放 | 2 | 2 次獨立 | 0 |
| **齊射** | **2** | **2 次獨立** | **0** |
| 連發 | 3 | 3 次獨立 | −0.10 |

> 齊射與連發的數值相同，差別在**意圖**：連發是「用彈藥換命中機會」，
> 齊射是「梭哈把它打死，還是留一發」—— 因為削短型的彈倉只有 2 發，齊射等於清空。

**可用模式清單資料化**：每把武器在 `weapons.json` 列出自己的 `modes`。
模式鍵只循環該武器有的模式；只有一種模式的武器，模式鍵是灰的。切換模式仍然不花時間。

> 自動降級（§8.9）也跟著改：彈藥撐不住所選模式時退到**這把槍自己**最省彈的模式。
> 不能寫死「退回單發」—— LMG-5 根本沒有單發，那會在介面上顯示一個它沒有的模式。

### 8.6 CombatEvent：規則層與回饋層的介面

`applyCommand` 除了回傳新的 `GameState`，另外回傳一份**決定性的事件清單**，描述這次指令產生了哪些事件：

```ts
export function applyCommand(state: GameState, cmd: Command): CommandResult;
export interface CommandResult { state: GameState; events: CombatEvent[] }
```

非法指令回傳 `{ state: 原物件, events: [] }`，因此 `result.state === state` 的 identity 比對照樣成立。

事件種類：`SHOT`（彈道）、`IMPACT`（命中傷害，含 `blocked` 被護甲吃掉的量與 `lethal`）、`MISS`、`KILL`、`NOISE`、`AI_STATE`、`AMMO_OUT`、`RELOAD`、`OBJECTIVE`、`DEPLOY`。

**硬性界線：**

1. 事件是純資料、可序列化，完全由狀態轉換推導而來，因此完全決定性。
2. **動畫的時間軸與播放狀態不進 `GameState`**，只活在 `render/effects.ts`。
3. `core/` 不知道有動畫這回事，也不引入任何瀏覽器 API。
4. 回饋層只是每幀被畫出來，**不阻塞輸入、不延後 `clock` 推進**。

### 8.8 背刺（v0.8）

攻擊一個**對攻擊者沒有視線**的目標時：

| 效果 | 值 |
|---|---|
| 命中率加成 | **+0.30** |
| 掩蔽修正 | **無視**（視同無掩蔽） |

**判定方式**：以目標的視野規則（§7.5，含射程、面向、姿勢、遮蔽）反向計算，目標是否看得見攻擊者。看不見即成立 —— 也就是 `!canSee(map, target, attacker.pos)`。

因此背刺有三種成立方式，都是同一條規則的結果：

1. **繞到背後**（落在目標的面向盲區）
2. **目標被牆擋住看不到你**（遮蔽）
3. **你在目標的視野距離之外開槍**（射程）—— 狙擊本來就是背刺

第 3 點是規則的自然推論，不是漏洞：`HULK` 視野只有 8，用射程 12 的 RR-4 從 10 格外打它，它確實不知道你在哪裡。

**對稱適用**：這條規則對玩家與敵人雙向生效。由於玩家站立時是全方位視野，**玩家只有在蹲下時才可能被背刺**。

**為什麼需要這條**：沒有它的話，「攻擊沒發現你的敵人」與「攻擊正在對射的敵人」在結算上完全相同，繞到背後就只是「先開槍」，不成其為玩法。這條規則之後也會直接支撐潛入契約與消音武器，不需要另外設計。

**介面要求**：背刺成立時攻擊預覽必須標示出來（「背刺 +30%・無視掩蔽」），否則玩家不會知道自己做對了什麼。本來有掩蔽的目標要顯示「部分掩蔽 已失效」而不是把掩蔽整個藏起來 —— 玩家要看得出是自己繞背才讓它失效的。

> 警戒中（`ALERT`）的敵人每次行動都會面向玩家（§9.1），所以**打不出背刺**。背刺是給 `IDLE` 與 `SEARCH` 的敵人用的 —— 你不可能背刺一個正在對你開槍的人。

### 8.7 亂數順序紀律

一次攻擊有三個擲值。順序固定，而且**無論該項是否生效一律照抽**：

1. 命中擲值
2. 傷害擲值
3. 主目標護甲擲值

即使 `enableToHitRoll` 為關閉、即使目標護甲為 0、即使目標格根本沒有單位、即使該發未命中，這三個亂數都必須照抽。濺射的每個額外受害者再各自多抽一次護甲（依 `state.units` 順序，仍然完全決定性）。

**理由**：亂數序列的長度必須與設定無關，否則切換開關或調整數值會使所有以種子重現的紀錄與測試失效。

---

## 9. 敵人

放在 `data/actors.json`。敵人也只能四方向移動。

| 原型 | HP | 護甲 | 移動 | 轉換 | 視野 | 攻擊 | 說明 |
|---|---|---|---|---|---|---|---|
| `RUNNER` 衝鋒型 | 25 | 0 ± 0 | **7** | **5** | 10 | 近戰，相鄰時費時 10，傷害 30 ± 5 | 比玩家快，逼玩家不能無限風箏 |
| `HULK` 裝甲型 | 90 | **20 ± 8** | **20** | 15 | 8 | 近戰，相鄰時費時 15，傷害 60 ± 10 | 輕武器每發只造成 10 點（保底），逼玩家換重武器 |
| `SHOOTER` 射手型 | 30 | 0 ± 0 | 10 | 8 | 12 | 遠程，射程 7，費時 12，傷害 20 ± 4 | 逼玩家利用掩體與姿勢 |

> 玩家的複製兵是 `move: 10`、`transition: 0`。**衝鋒型 7 就是「比玩家快」的全部實作**——不需要額外的規則，排程器自己會讓它多動。裝甲型 20 就是「笨重」：玩家走兩格它才走一格。
>
> v0.7 起 `attacksPerTurn` 與 `shotsThisTurn` 已刪除。射手型原本的「每回合上限 1 次」在沒有回合之後失去意義，攻擊頻率改由 `fireTime: 12` 決定——它比玩家的一個動作稍慢，所以自然不會連打。

> **注意單位**：HP、傷害、護甲是同一組可互相比較的數字，一起縮放。
> 射程、視野、噪音半徑、濺射半徑、彈匣、時間成本、`searchTimer`、地圖尺寸是**距離、次數或時間**，
> 與生命值不同單位，**不隨生命值規模縮放**。

敵人的攻擊各有 `accuracy`，並走與玩家**完全相同**的命中解算管線（含掩蔽、姿勢、射程衰減）：

| 敵人 | `accuracy` |
|---|---|
| RUNNER（近戰） | 0.70 |
| HULK（近戰） | 0.70 |
| SHOOTER（遠程） | 0.50 |

近戰命中率明顯較高是刻意的——這讓「不要讓衝鋒型貼上來」成為一條真正的壓力，而不只是掉血。

敵人不需要裝填、不需要換武器、不需要姿勢（永遠 `STAND`）。所有敵人的 `aim` 與 `evasion` 在 MVP 一律為 `0`，但欄位須存在於資料檔中。

### 9.0 面向（v0.8）

**敵人一律只看得見面向的前方半平面**（§7.5），不管姿勢。面向的變化規則：

| 情況 | 面向 |
|---|---|
| 移動時 | 跟隨移動方向 |
| `ALERT` 時 | 面向目標 |
| `IDLE` 時 | 維持初始面向 |
| 被噪音吵醒（`SEARCH`） | **不轉向**；等它開始朝噪音走，面向才跟著移動方向改變 |

地圖資料檔的敵人配置帶 **初始面向欄位**（§13.3），未指定時預設為南。

**感知與轉向的先後不可以顛倒。** 偵測用的是**這一次行動開始時**的面向，轉向是行動的附帶效果 —— 你只能轉向已經注意到的東西，不能先假設自己會轉對邊。顛倒過來寫的話（先朝玩家轉再看），敵人永遠看得見玩家，整個面向系統等於不存在。

因為 `ALERT` 的敵人每次行動都盯著玩家，**繞到它背後要走好幾步**，而且它一旦跟丟就轉入 `SEARCH`。這正是面向想要的產出。

### 9.1 AI 狀態機

```
IDLE
  原地不動（MVP 不做巡邏）。
  取得玩家視線 → ALERT
  聽到噪音 → SEARCH
ALERT
  有視線且在射程／相鄰 → 攻擊（一次輪到 = 開一槍）
  有視線但不在射程 → 朝玩家移動
  失去視線 → SEARCH，searchTimer = ai.searchTime（30）
SEARCH
  朝 lastKnownTarget 移動
  途中取得視線 → ALERT（不花轉換時間，見下）
  抵達 lastKnownTarget 或 searchTimer 歸零 → IDLE
  每次行動後 searchTimer -= 該動作的時間成本
```

**狀態轉換要花時間。** 進入警戒不是免費的心念一動——`IDLE → ALERT` 花掉該單位的 `transitionTime`（§5.2），這段時間就是 §9.2 的反應窗口。被噪音吵醒的 `IDLE → SEARCH` 同樣要花 `transitionTime`。

`SEARCH → ALERT` 與 `ALERT → SEARCH` 都是 **0**：已經在找你的人不需要再「驚醒」一次。這是刻意的，理由見 §9.2。

`searchTimer` 是時間量而不是次數：每個動作依自己的時間成本扣，所以慢的單位放棄得比較快，快的單位可以搜很多步。

### 9.2 落點評分（v0.10）

**總開關**：`ai.tacticalBehaviour`。關掉就完全回到 v0.9 的行為（貪婪逼近、不蹲、IDLE 不轉向、不喊口令）。這一版的效果只能靠對照判斷，所以沿用命中率擲骰開關的作法。

ALERT 時不再貪婪逼近，而是對**候選落點評分後選最高**。一次行動只移動一格，因此候選是四個正交鄰格 + 留在原地，最多五個。

| 項目 | 說明 |
|---|---|
| `approach` | 與目標的曼哈頓距離變化，接近為 +1、遠離為 −1、原地為 0 |
| `selfCover` | **該落點相對於玩家**的掩蔽等級（0 / 0.5 / 1），越高越好 |
| `targetExposure` | **從該落點看過去**玩家的掩蔽等級，越低越好（取 1 − 等級） |
| `canShoot` | 從該落點對玩家是否有視線且在射程內 |

`targetExposure` 是本次最重要的一項 —— 它只是把既有的掩蔽函式射手與目標對調，成本極低，但它會讓敵人**主動繞開玩家的掩體**。這也是本作核心戰術（側翼）第一次被敵人使用。

**權重必須依原型不同，否則三種敵人會退化成同一種打法**（放在 `data/actors.json`）：

| 原型 | 傾向 |
|---|---|
| `RUNNER` | `approach` 極高、其餘為 0 —— 不管掩體，直接衝 |
| `SHOOTER` | `selfCover` 與 `targetExposure` 高、`approach` 低 —— 保持距離、找掩體、繞側翼 |
| `HULK` | `approach` 中等、`selfCover` 低 —— 緩慢推進，靠護甲不靠掩體 |

**打得到就打，除非換一格能打得更好。** 敵人**永遠不會為了躲而放棄射線**：只有當某個鄰格「也還打得到」而且位置分數更高時，它才會先換位再打。這一步只比位置項（不含 `approach`），因為含了會震盪 —— `approach` 對相鄰兩格符號相反，A 想去 B、到了 B 又想回 A。只比位置項則不可能來回。

**要不要為了位置而先換位**由權重推導（`selfCover + targetExposure > approach`），不另開旗標 —— 權重是唯一的事實來源。

**決定性**：分數相同時依固定方向順序（北、東、南、西、原地）決勝，不得使用亂數。

### 9.3 姿勢與警戒巡視（v0.10）

**姿勢**

| 原型 | 行為 |
|---|---|
| `SHOOTER` | 所在位置有掩蔽、且**蹲下之後仍對玩家有視線**時，蹲下（花費 `time.stance`） |
| `RUNNER` / `HULK` | 永不蹲下 |

「蹲下之後仍有視線」這個但書是必要的：緊鄰半身掩體蹲下會**雙向阻擋**（§7.2），蹲進去等於把自己弄瞎，然後失去目標、轉入搜索、站起來、再蹲一次。玩家在同一個位置也不會蹲 —— 他會站著越過掩體開槍。

注意其代價：蹲下的敵人視野變成前方半平面，會產生盲區。**這是刻意保留的** —— 敵人變強的同時也給了玩家新的突破口。

**IDLE 的警戒巡視**

IDLE 敵人的行動在兩者之間交替：原地警戒（花 `time.wait`）與**警戒巡視**（原地轉 90 度，花 `ai.patrolTurnTime`），固定每兩次行動轉一次，方向固定（順時針）。

- **為什麼要花時間**：轉向本身是 0 成本，而 AI 不得選擇 0 成本動作（§5.4），否則排程器會無限迴圈。所以巡視是一個獨立的、有時間花費的動作。
- **為什麼要可預測**：固定周期讓玩家可以觀察、計時、抓空檔切入。**盲區從白拿變成需要時機的解謎** —— 這比隨機轉向有趣得多，也符合「隨機性放在局面、決策留給玩家」的原則。

**SEARCH 的收尾**

敵人抵達 `lastKnownTarget`（或搜索時間耗盡）後，先站定一次，再執行 `ai.searchWrapUpTurns` 次警戒巡視，才轉回 IDLE。這讓噪音引誘更有價值 —— 引開一個敵人之後，它會在那裡多待一陣子並環顧四周。

### 9.4 宣告下一個動作（v0.10）

敵人在**執行本次動作時**，同時宣告它**下一次**要做什麼，並且**必定執行**。

- AI 在行動時決定並記錄下一個動作（`Unit.declared`，屬於 `GameState`）
- 下次輪到它時，執行已宣告的動作，**不重新評估**
- 若宣告的動作在執行時已不合法，改為**原地等待**，並喊出失去目標的口令

**這是整套機制的回報所在**：玩家聽到「繞右邊，開火」之後移動破壞它的射線，那一發就整個浪費掉。**打斷敵人已宣告的攻擊，等於讓它損失一整個行動。**

代價是 AI 會依據稍舊的資訊行動，這是刻意的取捨 —— 可被預測、可被玩弄的敵人，比反應完美的敵人有趣得多。

**失效條件就是三件事：脫離視線、脫離射程、目標已死亡。**

> 開火的宣告綁的是「我要開火」，不是「我要打那一格」，所以執行時會重新瞄準到目標**現在**的位置。寫成「目標必須待在同一格」試過：玩家幾乎每個動作都在移動，於是敵人一槍都打不出來 —— 那不是拘束力，那是把敵人關掉。玩家要真的**破壞射線或拉開距離**才吃得掉這一發。

**轉入警戒的那一次是例外**：它喊的是「有動靜！」而不是下一個動作，而且不留下宣告。轉換本身就是那一次的動作，而世界在反應窗口（§9.2 的 `transitionTime`）裡會變 —— 現在宣告的東西到時候多半已經不對了。

### 9.5 敵人口令（v0.10）

敵人在行動時會**喊出口令**，宣告自己下一個動作要做什麼。玩家聽得到。這不是介面提示，是一個有規則的戰場機制：

- **用聽的，不是用看的** → 不需要視線，但有距離限制
- **只有進入戰鬥的敵人才會喊** → 沒發現玩家的敵人是安靜的
- **喊了就一定要做** → 宣告具有拘束力（§9.4）

**距離與遮蔽**：可聽範圍為曼哈頓 `ai.calloutRange`（12），**牆壁不阻擋**（與噪音機制一致，§8.3 的刻意簡化）。超出範圍則完全聽不到 —— **可聽與否的判定在規則層**，聽不到就根本不發事件，不能丟給渲染層決定。

| 狀態 | 是否喊口令 |
|---|---|
| `IDLE` | **不喊** |
| `SEARCH` / `ALERT` | 喊 |
| 執行 IDLE→ALERT 轉換的那一次 | 喊（「有動靜！」） |

**IDLE 敵人保持安靜是關鍵** —— 它保護了偷襲與將來的潛入契約，也讓「什麼時候變得吵起來」本身成為一個訊號。

**口令由 AI 的決策理由碼產生，不得事後推測** —— 否則調整 §9.2 的權重後，口令會開始說謊。理由碼順帶是很好的除錯工具：調權重時可以直接從口令看出每隻敵人為什麼那樣走。文字全部放在 `data/callouts.json`。

| 理由碼 | 口令 |
|---|---|
| `SPOT` | 有動靜！ |
| `ADVANCE` | 壓上去 |
| `FLANK`（左／右） | 繞左邊／繞右邊 |
| `TAKE_COVER` | 找掩護 |
| `FIRE` | 開火 |
| `CROUCH` | 就位 |
| `SEARCH_MOVE` | 那邊有聲音 |
| `PATROL` | 注意周圍 |
| `LOST` | 目標不見了 |

**這不是敵人之間的情報共享。** 口令只影響玩家聽到什麼，不影響 AI 行為。敵人之間仍然不通訊、不共享情報。請不要因為「他們在互相喊話」就順手實作情報共享。

**與將來的技能和裝備不衝突**：口令告訴玩家「敵人要做什麼」，但**不會告訴玩家「什麼時候」**。行動順序仍然保留給將來的技能，偵測安靜的敵人仍然保留給情報裝備。

### 9.6 敵人不會做的事（明確列出，避免過度發揮）

- 不會呼叫其他敵人、不會共享情報
- 不會撤退、不會治療、不會換位掩護
- 不會預判玩家的移動
- 不會刻意站到玩家的正交直線上
- 不會使用射擊模式、不會管理彈藥、不會搜刮
- 不做巡邏路線移動（IDLE 仍然不移動，只轉向）

**AI 只需要「不笨」，不需要「聰明」。** 請不要加入本節未列出的行為。

### 9.7 兩段式察覺

敵人從 `IDLE` 看到玩家時，**那一次輪到它只做「轉入警戒」這件事**，花掉 `transitionTime`，不攻擊也不移動。要到下一次輪到它才會開火。

> v0.7 起這不再是一條額外的例外規則，而是排程器的自然結果：轉換就是一個要花時間的動作。`Unit.justSpotted` 這個旗標已刪除，改用 `Unit.transitioning`（純粹給渲染層看的一格旗標，在該單位下次行動開頭清除）。

> **這條只適用於從 `IDLE` 轉入。** 已在 `SEARCH` 的敵人重新取得視線，**`SEARCH → ALERT` 的轉換成本為 0，可立即攻擊**。
>
> 若不加這條限制，玩家可以：開槍 → 敵人警戒 → 退出視線 → 敵人轉入搜索 → 再進入視線 → 敵人又「剛發現」而無法攻擊，變成一個無限安全的騷擾迴圈。要重新取得反應窗口，玩家必須躲滿整個 `searchTimer`、等敵人真的放棄回到 `IDLE`。

「剛發現、這次不會開火」的狀態必須**明確可見且與一般警戒可區分**——這段時間正是玩家用來蹲下、退回掩體或先開槍的反應窗口。若玩家看不出來，這條規則等於不存在。

> **反應窗口是一段時間，不是保證的一個動作。** 轉換時間短的單位（衝鋒型 `transition: 5`）可能在玩家的一個動作（10）之內就轉換完成並開火。對付快的東西要靠先手，不是靠反應。實作為琥珀色虛線框 + 單位頭上的「剛發現」字樣，與一般警戒的紅色閃爍實線框區隔。

AI 必須是決定性的。若有平手情況（多條等長路徑），以固定規則決勝（鄰居展開順序固定為 N, E, S, W），不要用亂數。

### 9.8 已知的難度偏移（累積記錄）

到 v0.9 為止累積了四項對玩家有利的不對稱。v0.10 修掉了前兩項：

| # | 不對稱 | 狀態 |
|---|---|---|
| 1 | 敵人 AI 不會利用掩體 | **v0.10 修掉**（§9.2） |
| 2 | IDLE 敵人的盲區是靜態的 | **v0.10 修掉**（§9.3 的警戒巡視） |
| 3 | 背刺加成主要由玩家取得（§8.8） | 仍在 |
| 4 | 彈匣 8 加上遍地彈藥（v0.9） | 仍在 |

調整數值時務必記得剩下兩項還在。**不要把「敵人有弱點」誤讀成「數值調得剛好」。**

> **v0.10 的實測結果與預期不同，這件事本身是最重要的發現。**
>
> `scripts/botrun.ts` 現在會在開關關閉與開啟兩種狀態下各跑一次（§8.3 的要求）。同一支笨機器人、同五個種子，兩邊幾乎沒有差別：成功 5/5 對 5/5，陣亡平均 2.4 對 2.2，耗彈 34.6 對 33.0，只有總耗時從 1925 上升到 2048（+6%）。
>
> 原因量得出來：**mission_01 上根本沒有掩體可用。** 十個敵人裡有九個，起始位置的四個鄰格加自己都沒有任何掩蔽；全圖 509 個可通行格中，對**東西向**射手提供掩蔽的只有 14%，對南北向的則有 28%。§13.1 要求半身掩體「成排配置」形成連續蹲行推進的路線 —— 那些排是水平的，提供的是對南北向射手的掩蔽，而這張地圖的動線是東西向。
>
> 也就是說：AI 照規格實作了，一格前瞻的落點評分也運作正常（`tactics.test.ts` 在有掩體的地形上驗得到繞側翼與蹲下），但**這張地圖的幾何讓它幾乎沒有東西可用**。這正是 §11.1 記錄「多樣化地圖是下一版」的理由 —— 現有的所有數值都是在同一組幾何上調出來的。

> 追擊時的目標格允許被佔據：搜索的 `lastKnownTarget` 往往就是玩家現在站的地方，若不允許，整條路徑會算不出來、敵人會原地發呆。真正的「不能走進去」由下一步的佔據檢查擋下（走到相鄰就停）。

---

## 10. 死亡、增援與屍體

### 10.1 玩家單位死亡

1. 在該格生成 `LootPile`（`kind: 'PLAYER_BODY'`），內含死者的**兩把槍、背包全部內容，以及一份 DNA**（§10.4）。
2. 該單位從 `units` 移除，`casualties++`。
3. 若 `roster` 為空 → `result = 'WIPED'`，任務結束。
4. 否則暫停，跳出增援選單：玩家從名冊選一位。
5. 新士兵出現在**距離死亡地點最近的、且該格未被佔據的 `DROP_POINT`**，`nextActAt = clock + time.deploy`（落地後要等一段時間才能行動），姿勢 `STAND`。
6. **v0.16：新士兵帶著他自己的配裝降落**（§21.4）。他在公司畫面上有什麼就帶什麼 ——
   若玩家沒有給他配裝，他就赤手空拳落地，那是玩家的責任。
   （v0.15 以前是「只配發一把 AR-9」，那條規則已被取代。）

> 死者身上的兩把槍與這一路搜刮的所有東西都留在屍體上。要不要冒險回去撿，是玩家自己的選擇。
> **v0.16 起那個選擇第一次真的有後果**：沒撿回來的槍是**永久**沒了（§21.5）。
>
> **v0.9 把這個選擇放大了**：屍體回收從「回去撿那把砲」變成「回去撿我這一路搜刮的所有東西」—— 玩家越貪，回去的動機越強，風險也越大（§4.4）。

### 10.2 屍體

- 屍體不阻擋移動與視線。
- **站在該格或正交相鄰格**即可搜刮（與 §11.1 的互動距離一致）。
- 屍體永遠不會消失。
- **收納欄（或主手）為空時，可以直接把武器拾取到那一格**，仍為 `time.pickup`（10）。
  這是常見情況，不該被收兩次費（v0.18 §2.2）。
- 彈藥與消耗品直接併入當前士兵的攜行量，不需要額外動作。
- 兩格都滿的時候，先用 §23 的搬裝備動作騰出位置。

### 10.3 敵人死亡

**v0.9 起敵人死亡也留下可搜刮的屍體**（`kind: 'ENEMY_BODY'`），內含彈藥與有機率出現的裝備。掉落表寫在 `data/actors.json` 各原型的 `loot` 欄位，**每一項依序各抽一次亂數**，抽值順序固定、與掉不掉得到無關。

### 10.4 搜刮（v0.9）

三種來源共用同一個 `LootPile` 型別與同一套兩段式點擊文法：

| 來源 | `kind` | 內容 |
|---|---|---|
| 地圖搜刮點 | `CACHE` | 地形類型 `LOOT`，內容寫在地圖檔的 `caches`（§13.4） |
| 敵人屍體 | `ENEMY_BODY` | 彈藥、有機率出現的裝備 |
| 己方屍體 | `PLAYER_BODY` | 該士兵的兩把槍、背包全部內容，以及**一份 DNA** |

**操作**（§12.17）：站在該格或相鄰格 → 第一次點擊顯示內容清單 → 第二次點擊某個項目拾取，花 `loot.takeTime`（10）。另提供「全部拿走」，同樣花 10，超重則盡可能拿。

**全部拿走在一件都拿不動時是非法的**，按鈕要灰掉。不擋的話，「一直按」會變成一個每次白花 10 時間卻什麼都沒拿到的無限迴圈（這是笨機器人探針實測抓到的）。

**DNA**：己方屍體上有一份 DNA 物品，可放入背包帶出。有重量但不大。局外層尚未存在，因此帶出的 DNA **只會列在結算畫面上**，本版不需要任何後續處理。

> 搜刮機制與 DNA 回收是同一套東西。這一版等於在不碰局外層的前提下，把局外層最重要那條迴路的地基蓋好。

---

## 11. 任務目標與結束條件

### 11.1 目標

- **主目標**：走到 `TERMINAL` 格**或其正交相鄰格**，花 `time.interact` 互動。
- **次要目標** ×2：走到 `SUPPLY` 格**或其正交相鄰格**，花 `time.interact` 互動。
- 目標可以任意順序完成，次要目標非必要。

> 互動距離為**曼哈頓距離 ≤ 1**（站在該格上或正交相鄰皆可）。斜向相鄰的曼哈頓距離是 2，不算相鄰。

### 11.2 撤離（v0.9 改動）

> **任何時候**回到初始空投點（或其相鄰格）並互動，即可撤離。**背包內的一切帶出**。

- 主目標已完成 → `result = 'SUCCESS'`
- 主目標未完成 → `result = 'ABORTED'`（合約失敗），**但戰利品照樣帶出**

**為什麼改**：戰利品一旦存在，「我東西撿夠了、主目標太硬、我走了」就是一個自然會浮現的決策 —— 而那正是最初設計止損按鈕時想要的東西，現在它有了實質內容。

這也符合本作的設計原則：**系統不禁止，只標價**。任務不是打開了就必須打完的副本，而是一個可以走人的工地。

> 戰役時間、合約期限那類約束屬於局外層，本版不處理。

### 11.3 止損

止損**只在當前士兵陣亡的那個當下提供**，出現在增援選單中（「改為止損撤出」）。

- 按下後跳出二次確認，顯示目前戰況損益（見 §12.5）
- 確認後 `result = 'ABORTED'`，任務結束
- 取消則退回增援選單，任務繼續

> 這是刻意的設計決定：止損必須付出一條人命才會浮現，而不是隨時可按的逃生鍵。HUD 上**沒有**止損按鈕。

> **止損與撤離是兩件不同的事**：撤離是走出去（東西帶回來），止損是不要了（戰場上的一切，含屍體與背包，全部損失）。兩者的 `result` 都是 `ABORTED`，差別在 `extracted` 有沒有內容 —— 結算標題必須分得出來。

### 11.4 結算畫面

無論何種結束方式，顯示：**任務總耗時**（結束時的 `clock`）、**本次使用的地圖名稱**、投入士兵數、陣亡數、完成的目標，以及**兩份清單**（含消耗品）：

1. **帶出的物品清單**，分類列出（彈藥、道具、值錢物品、DNA）
2. **遺留在戰場上的物品清單**（未回收的損失）

這兩份清單將來會是局外層的輸入，本版只需要正確顯示。附一個「重新開始」按鈕。

> HUD 上那一欄「我方遺留」只算**己方遺體**上的東西：沒開過的補給箱不是損失，那只是還沒去拿。結算的第二份清單則列出所有還在戰場上的東西 —— 兩者問的問題不一樣。

---

### 11.6 損益表（v0.20）

結算畫面最上面是一份損益表（§24.6）。**這是玩家最想知道的事** ——
這一趟是賺是賠，以及賠在哪裡。搜刮清單排在它後面。

### 11.5 結果如何套用回公司（v0.16）

結算畫面關掉之後，局外層把這一場的結果套用回 `MetaState`（§21.5）。規則只有一條：

> **只有「帶出來的東西」還在。其餘一律永久移除。**

| 情況 | 後果 |
|---|---|
| 撤離（走出撤離點） | 身上與背包的一切都帶回來，武器仍是同一個 `instanceId` |
| 止損 | **人回來，身上與背包的一切留在戰場上** |
| 陣亡 | 士兵永久移除，他身上的武器實例從軍械庫永久移除 |
| 名冊耗盡（WIPED） | 戰場上的一切全部損失 |

> 止損必須付代價，否則它會**嚴格優於**走到撤離點 —— 而那一段路正是本作的張力所在。


## 12. 觸控介面

### 12.1 硬性原則

> **手機沒有 hover。** 所有預覽資訊必須靠兩段式互動取得，不得依賴滑鼠懸停。

所有可點擊元素的觸控命中區不得小於 **48×48 CSS px**，且**相鄰按鈕的命中區不得互相重疊**。

地圖是**滿版**底層，HUD 與控制列半透明浮在上面。

### 12.2 統一的地圖點擊文法

這是介面的核心規則，適用於地圖上的**每一種**目標，沒有例外：

> **第一下：選取，並顯示「如果執行會發生什麼」。**
> **第二下：執行。**

**兩條不變式：**

1. **選取永遠不消耗任何資源。** 不花時間、不花彈藥、不推進 `clock`。因此點錯永遠無害，**不需要「取消」按鈕**。
2. **選取狀態是持續的、可見的。** 不使用任何遮蔽戰場的彈出面板。

| 目標 | 第一下顯示 | 第二下 |
|---|---|---|
| **敵人** | 雷射瞄準線、目標身上紅點準星、名稱與命中率 | 開火 |
| **遠處可通行格** | 路徑與所需總時間 | 依路徑移動（見 §12.4） |
| **屍體／搜刮點** | 可拿的東西清單 | 全部拿走 |
| **相鄰的終端／補給箱／撤離點** | 互動內容與時間花費 | 執行互動 |
| **自己** | 詳細狀態面板 | —（單次點擊即可） |
| **非法目標** | 無反應 | — |

> 只有「自己」與「屍體」用浮動小卡（兩者本來就需要一份清單，且都是低頻動作）。射擊、移動、互動的預覽全部畫在戰場上。

### 12.3 射擊

**沒有開火按鈕。** 射擊一律透過地圖點擊執行，鎖定的生命週期見 §8.4。

第一次點擊一個敵人時，持續顯示於戰場上：

- 從士兵拉一條**雷射瞄準線**到目標
- 目標身上顯示**小紅點準星**
- 目標附近以行內文字顯示**名稱**、**命中率**（MVP 固定 100%）、**預期傷害區間**與**目標護甲**

傷害一律以區間格式呈現（目前上下限相同，例如 `傷害 30–30`），護甲並排顯示。
這樣下一版改成 `傷害 25–35　裝甲 15–25` 時不需要重排版面。

> AR-9 鎖定裝甲型時會顯示 `傷害 10–10　裝甲 20` ——
> **開槍之前**玩家就看得出這把槍對它沒用。這是 §12.9 的教學能不能成立的前提。

標籤畫在目標**下方**：浮動傷害數字一律往上飄，兩者才不會疊在一起。

**唯一保留的確認對話框**：當濺射範圍會涵蓋玩家自己時，第二次點擊不直接開火，改為跳出明確的確認提示。重武器開火（費時 20，等於別人動兩次）**不需要**額外確認——兩段式點擊本身已足夠。

### 12.4 尋路移動

- 點擊遠處的可通行格 → 顯示四方向路徑與所需總時間
- 再點一次 → 沿路徑逐格移動
- 若目標格不可達，第一次點擊不顯示路徑（視為非法目標）
- 方向鍵仍然保留，供單步微調使用

自動移動在下列任一情況發生時**立即停止**：走完路徑、有新的敵人進入視線、玩家單位受到傷害、路徑被阻擋。（沒有「AP 耗盡」這回事了——路徑再長都走得完，代價是走完之後時鐘已經推得很遠。）

### 12.5 目標框：兩種訊息，兩種表現

| 視覺 | 意義 | 適用對象 |
|---|---|---|
| **靜態細框** | **這個目標我現在打得到**（視線、射程、彈藥皆成立） | 所有合法目標，**包含 IDLE 的敵人** |
| **點滅紅框** | **這個敵人不是 IDLE**（處於 ALERT 或 SEARCH） | 所有非 IDLE 敵人，無論打不打得到 |

兩者可以同時出現在同一個敵人身上。

> **硬性要求：框只表達狀態，不決定可否點擊。IDLE 的敵人必須能夠被選取與攻擊。**
> 偷襲毫無察覺的敵人是核心玩法。若因為 IDLE 敵人沒有點滅框而讓玩家以為不能攻擊，即為設計失敗。

附帶效益：把警戒狀態直接畫在敵人身上，會讓噪音機制變成可見的因果——玩家能看出「我剛剛那一槍驚動了誰」。

### 12.6 HUD（永遠可見，半透明浮動）

**HUD 只放「這一下要做什麼」的輸入。** 判準只有一條：**這個數字會改變我這個動作的選擇嗎？**

常駐（兩列）：

| 內容 | 為什麼常駐 |
|---|---|
| `HP 60/60` | 還能挨幾發 |
| `AR-9 8/8｜備 24` | 還能開幾槍、要不要裝填（槍內剩幾發只是一半的資訊） |
| `站・無人瞄準` / `蹲・部分掩蔽・2 人瞄得到` | 讓出行動權後會被怎麼打（§12.11 的硬性要求） |
| `負重 18/50`（被拖慢時追加 `・移動 12` 並轉警示色） | 走一格要多久、還能撿多少 |

**收進日誌面板（右上「誌」）**：世界時刻、名冊剩餘、場上敵數，以及**戰況損益**
`投入 2 名 ／ 陣亡 1 名 ／ 主目標 未完成 ／ 次要 1/2 ／ 我方遺留 1 項物資`。

這些回答的是「這一場打得怎麼樣」，不是「這一下要做什麼」—— 它們曾經在直向手機上
吃掉將近三成的畫面高度，而玩家並不會每個動作都去讀。搬進日誌之後離決策只差一個點擊，
而止損的二次確認本來就會把同一份損益再顯示一次（§11.3）。

> **「我方遺留」只算己方遺體上的東西。** 沒開過的補給箱不是損失，那只是還沒去拿。

**兩個常駐格的壓縮規則**（不是省字，是省掉不可用的資訊）：
>
> - 沒人瞄得到你的時候，掩蔽等級不是可用的資訊 —— 那時只顯示姿勢
> - 沒被負重拖慢的時候，移動時間就是基準值 —— 不用一直報

**畫面右上**是一排工具鍵：置中鏡頭（§12.15，只在鏡頭偏離時出現）、點擊移動開關（§12.16）、日誌開合。日誌面板頂端就是上面那份戰況帳目。

> **小卡的高度必須從實測的可觸區域算出來，不可以用 `50dvh` 這種比例。** 直向手機扣掉網址列之後可視高度只剩約 550px，比例值減掉方向盤與留白之後只剩二十幾像素，卡片會整個塌掉。
>
> 小卡（看自己、搜刮）**可以蓋住地圖**，用滿整個可觸區域。§12.8 的「士兵與四個鄰格不能被蓋住」管的是**常駐 UI**，不是讀完就關掉的清單卡片 —— 打開它的當下你在讀清單、按按鈕，不是在點地圖。

### 12.7 按鈕配置

**左下：移動區**

```
[ 待機 ]            ← 獨立在方向盤上方，間距 >= 一個按鈕高度

[ 　　 ] [  ↑  ] [ 蹲立 ]
[  ←  ] [  ↓  ] [  →  ]
```

- 左上角留空；**蹲立**放在右上角（隨時都要用，必須常駐好按）
- **待機是這一區唯一不可逆的動作**（費時 10 且把行動權讓給別人），不得與方向鍵相鄰

**右下：動作區**

```
[      ] [ 模式 ]   ← 射擊模式（v0.9）。點一下循環：單 → 點 → 連 → 單
[  包  ] [  用  ]   ← 背包（§12.20）／使用準備欄的東西（§12.19）
[  彈  ] [  換  ]   ← 裝填（承諾中會變成「續」）／換武器
```

**v0.12 起是兩欄三列，左上角留空**（與左下移動區同一個慣例）。排成一直行的話高度會吃掉 292px，可觸區域只剩 180px，地圖直向只看得到五格半 —— 比 v0.11 少了三分之一。兩欄三列是 172px，把那一百多像素還給地圖。

技能與背包合併成一顆：技能目前沒有內容，而背包是會反覆開的東西。

**模式鍵是獨立的一顆，不是每次開火都要選（§8.9）。** 射擊是全遊戲最高頻的動作，不可以每次開火都選一次模式 —— 模式是一個持續的設定，不是每發的選擇。

- 點一下循環切換，**不花時間**
- 按鈕上顯示當前模式與耗彈量，例如「連 3」
- 模式**記在武器上**：換到 RR-4 再換回 AR-9 時，維持先前選定的模式
- 持重武器時模式鍵為灰色（重武器只有單發）
- 因彈藥不足自動降級時，按鈕以虛線琥珀框標示 —— **不得無聲降級**

**射擊本身的操作完全不變**，仍是既有的兩段式點擊：點敵人 → 再點一次開火，以當前模式射擊。

**每個動作按鈕下方顯示的數字是該動作的時間成本**，不是 AP 點數。承諾在系列動作中時，除了「續」與「技能」（技能只是選單開合，不是動作），其餘動作鍵一律灰掉。

摺疊選單內的項目順序固定，不得依可用性重排。

**通用規則**

- **不可用的按鈕一律灰掉，不要隱藏。** 按鈕消失會導致其他按鈕位移，位移會破壞肌肉記憶並造成誤觸。
- 按鈕位置固定，任何情況下都不重排。

### 12.9 戰場回饋層

> **玩家不需要打開日誌，就能完整理解剛剛發生了什麼。**
> 日誌是事後查證的紀錄，不是理解戰況的管道。

必須在戰場上直接可見的事件：

| 事件 | 表現 |
|---|---|
| 命中 | 傷害數字從目標身上浮起 |
| 未命中 | 彈道畫成偏移的虛線 + 「未命中」文字，與下一項截然不同 |
| **命中但被護甲擋下** | 顯示實際造成的傷害，旁邊以較小字體顯示「擋下 N」 |
| 擊殺 | 更大的數字 + 「擊殺」，與一般命中可區分 |
| 開火 | 從射手到目標的短暫彈道 |
| 彈藥見底 | 「空倉」提示，不要只在 HUD 上默默歸零 |
| 敵人警戒狀態改變 | 目標框變化 + 該敵人身上一次性的狀態提示 |
| 噪音 | 開火時短暫顯示波及半徑（曼哈頓 ⇒ 畫成菱形） |

**三種結果必須可區分（硬性要求，優先度高於本節其他項目）：**

| 結果 | 玩家應該讀到的意思 | 判準 |
|---|---|---|
| **未命中** | 運氣不好，再開一槍 | 沒有 `IMPACT` 事件 |
| **命中但被擋下大半** | **這把槍對這個敵人沒用，該換武器了** | `IMPACT.blocked >= IMPACT.amount` |
| **命中** | 有效 | `IMPACT.blocked < IMPACT.amount` |

三者必須用**不同的顏色**，被擋下的主數字還要比有效命中**小一號**。

**理由**：裝甲型敵人在設計上的唯一任務，就是教會玩家「該換重武器了」。如果玩家分不出「我沒打中」和「我打中了但沒用」，這個教學就不會發生，裝甲型只會被當成一個很煩的高血量敵人。

**表現要求：**

- 浮動數字從目標上方浮起並淡出，約 0.6～0.8 秒
- **不得阻塞輸入**：動畫播放中玩家仍可繼續操作，動畫不得延後 `clock` 推進
- 同一目標短時間內多次受擊時，數字要錯開堆疊；每一階要拉開「主數字 + 副標」的總高，否則兩行字會咬在一起
- 深色描邊 + 亮色字，在淺色地形上也看得清
- 一律以 Canvas 繪製，不新增 DOM 元素

日誌保留在右上，內容與現況相同，但驗收標準是：**全程不開日誌，也能完整理解戰況。**

### 12.10 掩蔽必須看得見

當目標因為掩蔽而難以命中時，戰場上要標示出**是哪些格子造成的**——把那一格或兩格阻擋鄰格框起來。

攻擊預覽同時顯示：實際命中率、傷害區間、護甲區間，以及掩蔽的**等級與幅度**（例如「部分掩蔽 −25%」），不要只給一個變小的數字。

**理由**：側翼是掩蔽的解法（§7.2b）。玩家必須看得出擋住自己的是哪一塊，才會想到繞過去。看不見成因的懲罰只會被當成運氣不好。

### 12.11 玩家自身的防禦狀態

HUD 要顯示玩家當前的防禦狀態：**無掩蔽 / 部分掩蔽 / 良好掩蔽**、是否蹲伏、以及有幾個敵人瞄得到。

玩家必須能在把行動權讓出去之前，知道自己接下來會以什麼狀態承受攻擊。這是「在下一個人動之前要站著還是蹲著」這個核心決策的資訊基礎。

掩蔽取決於射手的方位，所以「玩家有沒有掩蔽」沒有單一答案。**本作採用最差情況**：在所有現在看得到玩家的敵人之中，取掩蔽最低的那一個。玩家因此永遠不會被自己的 HUD 騙——顯示良好掩蔽就代表每個看得到你的人都難打。這個選擇會在任務開始時寫進戰鬥紀錄。

### 12.12 敵方行動演出

v0.7 沒有「敵人回合」了，但演出需求不變：**當行動權不在玩家手上時**，UI 反覆送出 `ADVANCE`，直到當前行動者又變回玩家。規則解算完全不變，改的只是渲染層從一次沖掉改為排隊播放。`core/` 不因此改動，`GameState` 中仍不得含有任何動畫時間軸或播放狀態。

- **玩家視線內**的敵人動作 → 逐一演出，每個動作之間有間隔（`presentation.enemyStepMs`，預設 200ms）
- **視線外**的敵人動作 → 瞬間結算，不產生任何延遲
- **點畫面任意處可跳過**，直接結算到行動權回到玩家；跳過後的狀態與完整播放**完全相同**
- 播放期間不鎖住輸入
- 若這一段完全沒有可見動作，**完全沒有停頓**

否則十隻敵人在地圖另一頭走路時，玩家要盯著空白畫面等。

> 橫幅寫「敵方行動中」而**不寫是誰、也不寫還有幾個**。§5.3 已經說明：不得洩漏行動順序。

### 12.13 地形不受視野限制（v0.8）

**已探索過的地形維持繪製**（壓暗即可），**只有單位受當前視野限制**。

蹲下時視野只有 180 度，若地形也跟著消失，玩家一蹲下就有半張地圖不見、完全失去空間感。地形是空間感的骨架，敵人才是資訊差。

### 12.14 蹲姿的方向鍵：先轉向、再移動（v0.8）

- **站立時**：方向鍵直接移動，面向跟隨移動方向改變（此時面向不影響任何規則）
- **蹲下時**：方向鍵若與當前面向不同 → **先轉向**（不移動、不花時間）；再按一次才移動

這是刻意的防呆：蹲姿的視野由面向決定，玩家想調整視野時最不該發生的事就是意外離開掩體。

**轉向永遠合法**，即使那個方向是牆 —— 面向牆壁蹲著也是一種選擇。撞牆是在第二下（真的要走）才被擋下。

**按鍵提示是硬性要求**：蹲下時方向鍵必須以外框或其他明確方式區分「按下會移動」與「按下會轉向」。同一顆鍵有兩種意思，玩家必須在按下去之前就分得出來。

### 12.15 鏡頭平移（v0.8）

**拖曳**

- 在地圖區域拖曳可平移鏡頭
- **位移門檻 10 CSS px**：超過視為拖曳，否則視為點擊
- 平移範圍夾制在可觸區域邊界（維持 §12.8 的既有行為）

位移門檻不是可選的：即使關掉點擊移動（§12.16），地圖點擊仍有鎖定、屍體、互動等功能，沒有門檻就會把拖曳誤判成點擊。

**置中**

- 鏡頭偏離時顯示一顆**置中按鈕**；鏡頭置中時隱藏
- **玩家執行任何動作後，鏡頭自動置中回士兵**

**演出時鏡頭強制帶回（重要）**

演出視線內的敵人動作時，**鏡頭強制移動到該行動單位**，並取消玩家的手動平移。

若不做這件事，玩家拖到別處觀看時，v0.6 建立的逐一演出等於沒有播 —— 而回饋可讀性是花了整整一版建立的東西，不能被自由鏡頭悄悄破壞。**拖曳只在玩家自己決策的期間有效。**

### 12.17 搜刮介面與負重顯示（v0.9）

**搜刮**沿用兩段式點擊文法（§12.2）：

| 目標 | 第一下 | 第二下 |
|---|---|---|
| 屍體／搜刮點 | 內容清單（含「全部拿走」） | 全部拿走 |

清單上每一項都是一顆按鈕，顯示名稱、數量與重量。武器有三顆：換為手持／換為收納／塞進背包（塞進背包的武器**不能使用**）。拿不動的按鈕灰掉並寫明原因。

**搜刮面板不會在拿了一件之後關掉** —— 搜刮通常要連拿好幾樣。

**負重**在 HUD 常駐一格：`負重 18/50・移動 10`。跨過分級門檻時該格轉為警示色，玩家才看得出「再撿就會變慢」。

**彈藥**在 HUD 的武器格顯示成 `AR-9 8/8｜備 24` —— 槍內剩幾發只是一半的資訊。

### 12.19 準備欄與使用（v0.12）

**背包裡沒有「使用」，只有「準備」。** 非武器的物品從背包選取後進入**準備欄**，回到戰鬥主畫面用「用」鍵才真正執行。

- 準備欄只有一格。要換就得再準備一次
- 準備花 `time.prepare`（10）。理由見 §5.2
- HUD 上要一眼看得到準備欄裡是什麼（`備 戰地封合劑`）
- 「用」鍵在準備欄為空時為灰色
- 使用中橫幅顯示當前步驟與**剩餘時間**（「使用戰地封合劑：止血（1/2）　還要 40」），並提供明確的中止操作

### 12.20 背包介面與丟棄（v0.12）

**開啟與檢視背包不花時間** —— 那是玩家自己的東西，屬於資訊而非行動。

顯示：物品清單（名稱、數量、單件重量）、**當前總重量、容量上限、目前所在的負重級距與對應的移動時間**、以及準備欄的內容。負重級距在 v0.9 是隱形的，玩家只能從移動變慢間接感覺到 —— 這一版要把它說出來，包含「再撿多少就會掉到下一級」。

可執行的操作只有三個：檢視（0）、準備（10）、**丟棄（0）**。

**丟棄是重要功能，不是附屬品。** 負重會拖慢移動，而移動速度在被追擊時直接等於生存率。玩家必須能在戰鬥中決定「這批戰利品不值得我用命換」。丟棄的物品落在腳下的格子，成為可搜刮的堆（腳下已經有一堆就併進去）。背包裡的武器也可以丟。

**版面**：背包是**滿版面板**，連下方的按鈕群一起蓋掉，只留 HUD。打開它的當下你在讀清單而不是在打，看得完整比看得到底下的地圖重要得多 —— 退出只靠右上的「關閉」，所以那顆鍵一定要在，而且要 ≥48×48。日誌面板同理。

**二次確認**：準備與丟棄要按兩次。用「同一顆鍵按兩次」而不是另開對話框 —— 手機上單手可達，而且不會把已經滿版的背包再蓋一層。按別的鍵就取消。

### 12.18 敵人口令的呈現（v0.10）

- **看得見的敵人**：口令以浮動文字顯示在該敵人頭上
- **聽得見但看不見的敵人**：**只顯示方向與內容，不顯示確切位置** —— 在玩家周圍以方位牌呈現，例如「東北方：繞右邊」

第二點是刻意的。**聽覺不該洩漏精確座標**，否則 §7.5 的面向視野系統會被架空。

呈現要求：

- 文字必須有描邊與底襯。依資產調查的結論，無描邊的資訊元素在地形變亮時會直接消失
- 多個敵人同時喊話時標示要能錯開，**不可重疊成一團** —— 位置在繪製當下才知道（鏡頭會動），所以避讓也只能在繪製時做，而且要連 AI 狀態的小字一起讓
- 一個單位同時只留最新的一句；同時最多顯示五張牌，超過丟最舊的。看不見的敵人是瞬間結算的（§12.12），一整批口令會在同一幀落地，不設上限會排成一道字牆
- 不得遮蔽目標框、雷射瞄準線或掩蔽高亮
- 純 Canvas 繪製，不新增 DOM 元素
- **已宣告的動作屬於 `GameState`**（因為宣告具有拘束力，是規則狀態），但動畫與顯示狀態不進入 `GameState`

### 12.16 點擊地圖移動可開關（v0.8）

一個設定開關控制「點擊地圖進行尋路移動」，**預設開啟**，放在畫面右上（與日誌同區），不需要獨立的設定畫面。

關閉時，點擊地圖仍然可以：鎖定敵人、檢視屍體、與相鄰目標物互動、檢視自己。**只有「點空格尋路移動」被停用。**

### 12.8 鏡頭

**邊界夾制**：士兵原則上置中，但接近地圖邊緣時停止捲動，不再嚴格置中。界外區域畫成岩層紋理並標出可作戰範圍的邊界。

> **夾制的邊界是「可觸區域」，不是視窗。** HUD 與控制列浮在地圖上，若照視窗邊界夾制，士兵走到地圖下緣時會被壓到控制列底下——而 §12.3 之後射擊與互動全靠點地圖，被蓋住就等於完全不能操作。因此垂直方向夾在 HUD 底部與控制列頂部之間，士兵置中於「看得到也點得到」的那一段。代價是地圖上下緣外會露出一條界外區，而那條剛好被 UI 蓋住，不佔可視面積。水平方向沒有常駐 UI（控制列只佔最底下一條），維持照視窗夾制。
>
> **硬性要求**：士兵在地圖上任何位置時，**他自己與四個正交鄰格都必須完整落在可觸區域內**，否則移動與相鄰互動會變成點不到。

---

### 12.24 背包畫面裡的裝備管理（v0.18）

**沒有新增介面** —— 裝備管理就長在既有的滿版背包畫面裡（v0.17 §4.1：
檢視不花時間，所以可以滿版）。畫面上看得到三個位置各自裝著什麼，並可在其間移動。

但**移動裝備會花時間**，而背包是滿版介面 —— 誤觸的代價是整場的時間。
所以每一顆搬裝備的按鈕都：

1. **顯示時間花費**（與所有會花時間的按鈕一致）
2. **需要二次確認**（同一顆鍵按兩次，與丟棄／準備同一套文法）
3. 執行後**收起面板**，讓玩家回到戰場看清楚代價（與準備一致）

負重與對應的移動時間即時更新。搬動不會改變總重（三個位置都計入），
所以真正會動到負重的仍然只有拾取與丟棄。

### 12.23 選單通則（v0.17）

> **凡是不消耗遊戲時間的介面，都可以蓋住整張地圖。**
> **凡是會消耗遊戲時間的操作，必須保持地圖可見。**

判準很乾淨：不花時間就代表**盤面是靜止的**，玩家不需要一邊看地圖一邊決策。

| 可全螢幕（不消耗時間） | 必須保持地圖可見（消耗時間或需要空間判斷） |
|---|---|
| 背包、掠奪、日誌、士兵資訊、設定 | 射擊預覽與確認 |
| 配裝（局外層） | 移動路徑預覽 |
| | 使用道具的執行過程 |

右側那一欄本來就畫在戰場上（§12.2 的兩段式文法），不走面板那一層。

**掠奪選單的自動關閉條件**：容器被掠奪後不會消失，所以不能用「主體失效」當條件。
改為兩個明確的條件，**滿足任一即關閉**：

1. 該容器已被拿空
2. 玩家與該容器的曼哈頓距離超過門檻（`ui.json` 的 `lootCloseDistance`，預設 1）

### 12.22 公司畫面（v0.16）

滿版直向，與合約清單同一套視覺。三個分頁 + 兩個子畫面：

| 分頁／子畫面 | 內容 |
|---|---|
| 名冊 | 每人一張卡：編號-世代、HP、**服役紀錄**、當前配裝摘要、負重級距 |
| 軍械庫 | 每一把槍的實例 id、口徑、機構、彈倉，以及**現在在誰身上** |
| 補給站 | 0 元的士兵／武器／彈藥／消耗品，畫面上明確標示為暫時性測試機能 |
| 逐人配裝 | 從名冊進入。武器是單選清單（含「不帶」），彈藥與消耗品是 ＋／− 從共用庫存拿與還 |
| 派遣 | 選完合約之後進入。列出名冊，挑一名首發 |

- 配裝畫面上**別人手上的槍照樣列出來，但標明「選了就搶過來」** ——
  同一把槍只能給一個人是規則，不是介面限制，所以介面要說清楚而不是隱藏。
- 彈藥列同時顯示**已分配量**與**庫存剩餘量**：分配資源時兩個數字都要看得見。
- 重置公司的入口在公司畫面底部，**同一顆鍵按兩次**確認（與背包的丟棄同一套文法）。

### 12.21 配裝畫面與模式鍵的資料化行為（v0.15）

**配裝畫面**（完整規範見 §19.5）：合約清單與任務之間的一個滿版直向畫面。
它與合約清單共用同一套視覺與兩段式文法，唯一的差別是它**沒有兩段式** ——
配裝的每一次調整都是即時可逆的，只有「出擊」是不可逆的那一下。

**模式鍵**：只循環該武器 `modes` 裡列出的模式；只有一種模式的武器，模式鍵是灰的。
彈藥不足時的自動降級仍然照舊顯示實際會用的模式（§8.9），
但降級的下界改為**這把槍自己最省彈的模式** —— 不能寫死單發。

**「彈」鍵**：增量裝填的武器（§8.10）在裝填未滿時，紀錄會標示「還可以繼續裝」。
它不是系列動作，所以按鍵不會進入承諾狀態，其他按鈕照樣可用。

## 13. 地圖格式

`data/maps/mission_01.json`：

```json
{
  "id": "mission_01",
  "name": "廢棄水處理廠",
  "width": 32,
  "height": 24,
  "legend": {
    ".": "FLOOR",
    "#": "WALL",
    "+": "HALF_COVER",
    "D": "DROP_POINT",
    "T": "TERMINAL",
    "S": "SUPPLY",
    "L": "LOOT"
  },
  "tiles": ["################################", "..."],
  "startDropPoint": { "x": 1, "y": 1 },
  "enemies": [
    { "archetype": "RUNNER",  "pos": { "x": 11, "y": 9 }, "facing": "W" }
  ],
  "caches": [
    { "pos": { "x": 9, "y": 4 }, "label": "彈藥箱", "items": [{ "defId": "AMMO_RIFLE", "qty": 12 }] }
  ]
}
```

### 13.1 四張地圖是三個對照實驗加一個基準（v0.11）

**這四張圖不是為了內容變多。** 三張新圖各自回答一個不同的問題，不要把它們做成三張「差不多但長得不一樣」的地圖。

| id | 名稱 | 要驗證的假設 |
|---|---|---|
| `mission_01` | 廢棄水處理廠 | **對照基準。**既有的所有數值都是在它上面調出來的，保留不動 |
| `mission_02` | 輸送管廊 | **狹窄空間。**正交直線對射是否致命；側翼繞行在窄空間中是否仍然可行 |
| `mission_03` | 乾涸沉澱池 | **開闊地。**當穿越開闊地有安全但繞遠的選項時，玩家會不會選它 |
| `mission_04` | 舊倉儲區 | **掩體密集、多路線。**`targetExposure` 繞側翼是否真的會發生；固守單一掩體是否被懲罰 |

> **`mission_04` 的掩體是縱向的。** v0.10 的實測發現 `mission_01` 的半身掩體是水平成排的，提供的是對南北向射手的掩蔽，而那張圖的動線是東西向 —— 於是 AI 幾乎沒有掩體可用。`mission_04` 改成縱向貨架列，掩蔽對東西向的交火生效，這才測得到 `targetExposure`。方向性掩蔽覆蓋率記在 `docs/map-stats.md`。

**共同要求**（驗證器會全部檢查，§13.5）：

- 尺寸 32×24 上下
- 一個主目標、兩個次要目標、至少 3 個 `DROP_POINT`
- 敵人 8～10 隻，三種原型都要出現，**每一隻都要指定初始面向**
- 至少 2 個搜刮點
- 主目標離起始空投點夠遠
- `TERMINAL` 放在離起點最遠的一端；兩個 `SUPPLY` 放在偏離主路線的側翼
- **不移除任何敵人也走得到所有目標** —— 一格寬的走廊裡一隻敵人就是一道牆，唯一路線被塞死是脆弱的設計

**各圖自己的設計語彙**：

- `mission_01`：半身掩體成排配置，形成可連續蹲行推進的路線
- `mission_02`：全部填滿再挖通道。**主幹管廊兩格寬**，一格寬只留給檢修爬道；每隔一段就有檢修室或路口。掩體稀少且多為單側
- `mission_03`：池區裡有**三條角度不同的掩體穿越路線**，都明顯比直線遠；抄對角線最短但整段暴露。敵人以 SHOOTER 為主

> **v0.13 為什麼重做這兩張。**
>
> `mission_02` 初版是一格寬的走廊。實測是**空轉**：機器人 3/5 成功、平均只開九槍、耗時是基準的六倍。原因是幾何 —— 一格寬 + 曼哈頓距離 + 蹲姿 180 度視野，有效視距被壓到趨近於零。一格寬同時讓「側翼繞行」根本無法成立，而那正是這張圖要驗的東西。**兩格寬的主幹讓遭遇戰有發生的空間，也讓繞側翼做得到。**
>
> `mission_03` 初版是「大面積開闊 + 孤立的點狀掩體」。實測是屠宰場：1/5 成功、四人名冊平均死 3.4 個。問題不是難度而是算術 —— **開闊地上雙方都沒有掩蔽修正，交火退化成純粹的傷害競賽，而玩家是一個人打八個。** 所以「開闊」重新定義為：
>
> **有趣的開闊不是「沒有掩體」，是「有掩體，但穿過去的路很長」。**
>
> 玩家的決策應該是**繞遠路安全通過，還是抄近路暴露一段** —— 初版沒有這個決策，只有一條死路。
- `mission_04`：縱向貨架列構成平行的南北推進路線，列間的缺口刻意錯開，橫向移動要走 Z 字

### 13.2 隨機選圖（v0.11）

任務開始時從四張地圖中選一張。

- 選圖使用**既有的可播種亂數**（`core/rng.ts`），不得使用 `Math.random()`
- **抽在最前面**：`createInitialState` 一建好 rng 就先抽選圖，後面所有的擲值順序才不會因為「有沒有指定地圖」而錯開
- 相同種子必然選到相同的圖
- 網址參數 `?map=<id>` 可指定地圖，方便針對單張圖除錯；`?seed=` 的既有行為不變

選圖是**規則的一部分**，不是介面的一部分。不需要選圖畫面，也不需要任何局外層的關卡選擇機制。

### 13.3 敵人的初始面向（v0.8）

`enemies[].facing` 是**初始面向**，八向字串（`"N"` / `"E"` / `"S"` / `"W"` / …）。**未指定時預設為南。**

敵人一律只看得見面向的前方半平面（§7.5），所以這一欄等於在畫「每個守衛在看哪邊」。**沒有面向的守衛不是守衛，是靶。**

地圖設計上要刻意留幾個背對主要進路的敵人 —— 那是玩家該學會背刺（§8.8）的地方。

### 13.4 搜刮點（v0.9）

新的地形類型 `LOOT`（圖例 `L`），可通行、不擋視線。內容寫在地圖檔的 `caches`，座標必須落在 `LOOT` 地形上（`build_map.mjs` 會驗）。

擺法的原則：**沿路線但要繞一下**。放在主路線上等於白送，放在死角則沒人會去。

### 13.5 地圖驗證工具（v0.11）

`scripts/build_maps.mjs` 同時是產生器與驗證器。**任一張地圖驗證失敗即建置失敗**，納入 CI 與型別檢查、測試並列。

現在的用途是防止手刻地圖出現結構性錯誤；**將來的用途是程序化區塊拼接的約束條件** —— 沿用專案一路以來的作法：現在建立鉤子，將來直接使用。門檻全部放在 `rules.json → mapRules`。

**檢查項**

| 類別 | 檢查 |
|---|---|
| 連通性 | 所有空投點、目標、搜刮點從起始空投點四方向可達；沒有走不到的可通行區域 |
| 連通性 | **不移除敵人也走得到所有目標** |
| 空投點 | 數量 ≥ 下限；兩兩間距落在區間內；未被敵人初始位置佔據 |
| 掩體 | 半身掩體佔可通行面積的比例落在區間內 |
| 掩體 | 最短路徑上的連續無掩蔽段 ≤ 上限；**必經**的連續無掩蔽段 ≤ 較嚴的上限 |
| 任務長度 | **預估完成路徑時間**落在區間內（v0.13） |
| 掩蔽方向 | **東西向與南北向的掩蔽覆蓋率差距 ≤ 門檻**（v0.13） |
| 敵人 | 每隻都指定面向；位置可通行、不重疊、可達；三種原型都有 |
| 目標 | 恰有一個主目標；次要目標數量符合設定；主目標離起點超過下限 |

> **「必經」與「最短路徑」是兩個數字。** §13.5 的措辭是「不存在完全無掩體的長距離**必經**路段」—— 最短路徑上的暴露不算必經，因為玩家可以繞。所以必經那一項用二分搜尋 + 「不允許連續暴露超過 k」的 BFS 算出來（狀態是「格子 + 目前連續暴露長度」）。兩者差距大，代表這張圖給了「繞路換安全」的選擇。

> **預估完成路徑時間**（v0.13）：從起始空投點出發、依序抵達所有必要目標、再返回撤離點的最短四方向路徑總長，乘以基礎移動時間。目標數量很少，所以直接窮舉全部順序取最短。這是**下限估計** —— 實際耗時一定更高，因為有交火、繞路、搜刮、裝填 —— 但它是靜態的，不必跑機器人就能算，所以進得了 CI。
>
> **方向性掩蔽差距**（v0.13）：掩體列的走向等於在決定**哪個軸向的交火是安全的**。若主目標在北方，玩家整趟推進都在南北軸上交火 —— 那張圖對他是 19% 還是 51% 的掩蔽，完全是作者無意間決定的。門檻設 20 個百分點。

> 掩體密度的區間**刻意寬**：開闊地圖與掩體密集圖是兩個對照組，密度天差地遠。這個區間抓的是「整張圖沒有掩體」或「幾乎不能走」這種結構性錯誤，不是設計約束。刻意設計成開闊的地圖可以在自己的檔案裡覆寫暴露上限 —— 那是一句**寫下來的設計宣告**，不是漏檢。

**產出**：`docs/map-stats.md`，每張圖的尺寸、可通行面積、掩體密度、**方向性掩蔽覆蓋率**、空投點間距、暴露長度、敵人組成、主目標距離。這份摘要是比較四張圖、並歸納出「什麼叫好地圖」的依據，由 CI 自動更新。

---

### 13.6 統計值會寫進地圖 JSON（v0.14）

`npm run map:build` 除了驗證，還會把算出來的統計值一併寫進每張地圖的 JSON
（`stats` 區塊）。**合約清單的地形標籤與難度評級都由這些值推導**（§18.2），
所以它們與地圖永遠同步：改了地圖、跑了 build，標籤就跟著變。

v0.14 為此新增一個統計值：

| 統計值 | 定義 | 為什麼要它 |
|---|---|---|
| `openness` 開闊度 | 可通行格周圍八格中**不是牆**的比例 | 量的是**建築的寬窄**。刻意不把半掩體算成阻擋 —— 那是 `coverDensity` 的事；若一起算，掩體密集的倉儲區會被判成「狹窄」，而那正好是它最不狹窄的原因 |

沒有 `stats` 的地圖（例如測試裡臨時組的）不進合約清單，也不該進。

## 14. 亂數與決定論

MVP 的戰鬥結果幾乎是決定性的（命中率固定 1.0、無傷害浮動、AI 決定性），但**每次攻擊仍會消耗一個亂數**（§8.1），以固定亂數序列的長度。

`rng.ts` 必須實作為可播種的產生器，且其內部狀態（含已抽取次數）應納入 `GameState`，使狀態序列化後能完整還原。

`GameState.rngSeed` 應可從網址參數覆寫（例如 `?seed=12345`），方便重現 bug。

---

## 15. 完成定義（驗收清單）

在 320px 寬的手機瀏覽器上，以下每一項都必須成立：

**移動**
- [ ] 士兵與敵人皆無法斜向移動
- [ ] 射程、視野、噪音、濺射全部使用曼哈頓距離，專案中已無殘留的切比雪夫距離判定
- [ ] 斜 45 度方向的可及範圍約為正前方的一半
- [ ] 視線遮擋仍為幾何直線判定，既有的視線測試維持通過
- [ ] 存在「看得見但超出射程」的敵人時，不顯示合法目標框，但該敵人仍然可見
- [ ] 可以用方向鍵移動士兵，`nextActAt` 正確推進，時間到就換別人動
- [ ] 點遠處空格顯示路徑與總時間，再點一次會沿路徑移動
- [ ] 自動移動在敵人進入視線時立即停止

**行動排程器（v0.7）**
- [ ] `GameState` 沒有 `turn`、`phase`、`enemyQueue`；`Unit` 沒有 `ap`／`maxAp`
- [ ] 主迴圈永遠選 `nextActAt` 最小的存活單位，`clock` 推進到它
- [ ] 同時刻的先後：玩家優先，其次陣列索引，完全決定性
- [ ] 所有時間成本都讀自 `data/`，程式碼裡沒有寫死的時間值
- [ ] 零成本動作（姿勢、面向）執行後行動權仍在自己身上，且 AI 不會選它而卡死
- [ ] 衝鋒型（move 7）確實比玩家（move 10）動得更頻繁
- [ ] 畫面上**沒有**任何敵人行動順序的提示
- [ ] `GameState` 仍可 `JSON.stringify` 後還原，重播結果相同

**系列動作（v0.7）**
- [ ] RR-4 裝填為兩步序列，總花費 20
- [ ] 序列中的單位在畫面上看得出正在蓄勢
- [ ] 承諾期間除了「續」與「中止」，其餘動作皆為非法
- [ ] 玩家可中止序列，時間不退還，效果不發生
- [ ] 單位死亡時序列作廢
- [ ] AR-9 的裝填仍是單一動作，沒有序列

**視線與掩體**
- [ ] 蹲在半身掩體後時，掩體另一側的敵人看不見我，我也看不見他們
- [ ] 站起來後可以越過同一個掩體射擊該敵人
- [ ] 掩蔽是獨立的鄰格掃描，**未修改既有的射線遮蔽函式**
- [ ] `WALL` 與 `HALF_COVER` 提供相同的掩蔽效果
- [ ] 朝射手側一格阻擋物 = 部分掩蔽 −25%，兩格 = 良好掩蔽 −40%
- [ ] 地圖界外視同 `WALL`，貼邊界站立可獲得掩蔽
- [ ] 射手移到另一側後，掩蔽等級改變、命中率隨之變動
- [ ] 正交直線對射時，朝向射手的鄰格若為阻擋物，結果是遮蔽而非掩蔽
- [ ] 蹲姿與掩蔽可同時生效，且命中率不低於 0.15

**兩段式察覺**
- [ ] 敵人從 IDLE 轉入 ALERT 的那一次行動不會攻擊，而且花掉 `transitionTime`
- [ ] 已在 SEARCH 的敵人重新取得視線後可立即攻擊（轉換成本 0）
- [ ] 「剛發現、這次不開火」在畫面上明確可見，且與一般警戒可區分
- [ ] 反覆進出視線無法製造無限安全的騷擾迴圈

**敵方行動演出**
- [ ] 視線內的動作逐一演出，視線外瞬間結算
- [ ] 可點畫面跳過，跳過後結果與完整播放完全相同
- [ ] 播放期間可以輸入
- [ ] 這一段無可見動作時沒有停頓

**面向視野（v0.8）**
- [ ] 站立時視野為全方位，不受面向影響
- [ ] 蹲下時只看得見前方半平面，含垂直於面向的那條線
- [ ] 蹲下面向北時，正東與正西的相鄰格可見
- [ ] 蹲下時後方三格（後、後左、後右）為盲區，整個後方半平面都看不見
- [ ] 敵人一律套用前方半平面規則，不管姿勢
- [ ] 地圖資料檔可指定敵人初始面向，未指定時為南
- [ ] 敵人移動時面向跟隨移動方向，`ALERT` 時面向目標，`IDLE` 不轉向
- [ ] 偵測用的是行動開始時的面向（不是先轉頭再看）
- [ ] 已探索地形維持繪製，只有單位受視野限制
- [ ] 遮蔽函式仍然對稱，且**一行都沒改**

**姿勢**
- [ ] 蹲下提高自身命中率、降低被命中率
- [ ] 姿勢改變花費 3，轉向維持 0
- [ ] v0.5 的蹲姿視野 ×0.6 係數已移除
- [ ] 站立時方向鍵直接移動，面向跟隨
- [ ] 蹲下時按非面向方向會先轉向，再按一次才移動
- [ ] 蹲下時方向鍵以外框區分「會移動」與「會轉向」
- [ ] 轉向永遠合法（含面向牆壁），撞牆是在第二下才被擋

**背刺（v0.8）**
- [ ] 對沒有視線的目標攻擊時命中率 +0.30 且無視掩蔽
- [ ] 規則對玩家與敵人雙向生效
- [ ] 玩家站立時不會被背刺
- [ ] 攻擊預覽會標示背刺成立，且原有掩蔽顯示為「已失效」而不是消失
- [ ] `ALERT` 中的敵人打不出背刺（它每次行動都面向玩家）

**鏡頭與開關（v0.8）**
- [ ] 可拖曳平移，位移門檻 10 CSS px
- [ ] 拖曳不會誤觸發鎖定、互動或移動
- [ ] 鏡頭偏離時顯示置中按鈕，置中時隱藏
- [ ] 玩家動作後鏡頭自動置中
- [ ] 演出視線內的敵人動作時，鏡頭強制帶到該單位
- [ ] 點擊地圖移動可開關，預設開啟
- [ ] 關閉時，點擊地圖的鎖定／屍體／互動／看自己都不受影響

**命中與傷害**
- [ ] 命中率擲骰可由資料檔開關控制
- [ ] 關掉開關並把 spread 歸零時，行為與 v0.4 完全等價
- [ ] 傷害與護甲皆為 `base ± spread`，護甲每發各擲一次
- [ ] `penetration` 欄位存在且全為 0，公式已就位
- [ ] 三個擲值一律照抽、順序固定；切換開關不改變亂數序列長度

**射擊**
- [ ] 沒有開火按鈕
- [ ] 點敵人一次出現雷射線、準星、名稱與命中率，且**不消耗任何資源**
- [ ] 再點同一個敵人即開火，且鎖定保留
- [ ] 下次輪到自己時再點同一目標，一下就開火
- [ ] 點另一個敵人只改變鎖定，不開火
- [ ] 濺射會波及自己時跳出確認；其餘情況不跳任何對話框
- [ ] 輕武器開一槍費時 10、重武器 20：打一發重武器的時間，輕武器打得了兩發
- [ ] 對裝甲型使用輕武器每發只造成 1 點傷害；使用重武器造成 10 點
- [ ] 換為重武器費時 20，換完之後別人已經動過了
- [ ] 開火會讓噪音半徑內的閒置敵人轉為搜索狀態並朝開火點移動

**目標框**
- [ ] IDLE 的敵人可以被選取並攻擊
- [ ] 合法目標有靜態細框；非 IDLE 敵人有點滅框；兩者可同時出現

**互動與任務**
- [ ] 站在終端**相鄰格**即可互動，不需站上去
- [ ] 屍體、終端、補給箱全部遵循「點一下顯示、點兩下執行」
- [ ] 衝鋒型敵人移動速度比玩家快，無法靠後退持續拉開距離
- [ ] 士兵死亡後屍體與重武器留在原地，新士兵只帶著步槍從最近空投點出現
- [ ] 可以走回屍體處花 `time.pickup` 撿回重武器
- [ ] 名冊耗盡時任務判定為 WIPED
- [ ] 止損只在當前士兵陣亡時提供，二次確認後正確結算
- [ ] 完成主目標後回到初始空投點可以成功撤離

**介面**
- [ ] 左下為兩列六格配置，左上角留空，蹲立在右上角
- [ ] 待機獨立於方向盤上方，兩者間距 ≥ 一個按鈕高度
- [ ] 右下是模式、技能、裝填、換武器四顆（v0.9）
- [ ] 技能鍵可展開收合，選單內容為空
- [ ] 日誌開合在右上
- [ ] 不可用按鈕為灰色而非隱藏，且位置不因狀態改變而位移
- [ ] 所有觸控命中區 ≥ 48×48 CSS px 且互不重疊
- [ ] 士兵在地圖任何位置（含四角與四邊）時，自己與四個正交鄰格都沒有被 HUD 或控制列蓋住

**戰場回饋層**
- [ ] 全程不開日誌可以完整理解戰況
- [ ] 未命中、命中被擋下、一般命中三者在畫面上明確可區分（顏色不同、字級不同）
- [ ] 用 AR-9 射擊裝甲型時，玩家能一眼看出「打中了但幾乎沒用」
- [ ] 動畫播放期間仍可輸入，`clock` 推進不被延後
- [ ] 同一目標連續受擊時數字不重疊
- [ ] 在 320px 寬視窗、淺色地形上，浮動數字仍然清晰可讀
- [ ] `GameState` 中不含任何動畫時間軸或播放狀態

**數值規模**
- [ ] 相同種子加相同指令序列，事件序列與放大前完全等價（只有生命值單位的數字大十倍）
- [ ] 射程、視野、噪音、濺射、時間成本、地圖尺寸全部未改變（彈匣在 v0.9 因射擊模式調到 8）
- [ ] 保底傷害由資料檔控制，目前值為 10
- [ ] 程式碼中已無寫死的戰鬥數值

**彈藥（v0.9）**
- [ ] 背包有彈藥總量，裝填時從背包扣除
- [ ] 背包彈藥不足時只補部分
- [ ] 背包彈藥耗盡後無法裝填
- [ ] 不追蹤彈匣個數，也沒有上膛殘彈

**射擊模式（v0.9）**
- [ ] 三種模式時間花費完全相同
- [ ] 連發為三次完整獨立判定，各自擲命中、傷害、護甲
- [ ] 連發可能出現三發全中或全部落空
- [ ] 右下動作區有獨立的模式按鈕，點一下循環切換，不花時間
- [ ] 模式按鈕顯示當前模式與耗彈量
- [ ] 開火操作維持兩段式點擊，不需要每次選模式
- [ ] 模式記在武器上，換槍再換回來時維持先前的設定
- [ ] 攻擊預覽只顯示當前模式的數值
- [ ] 槍內子彈不足時自動降級，且按鈕與預覽明確顯示實際會用的模式
- [ ] RR-4 沒有模式選項，模式按鈕在持重武器時為灰色

**背包與負重（v0.9）**
- [ ] 背包超過重量上限時無法拾取
- [ ] 負重跨過分級門檻時移動時間增加，且玩家看得出來
- [ ] 裝備中與收納中的武器不佔背包
- [ ] 背包中的武器不能直接使用
- [ ] 士兵死亡時背包全部內容留在屍體上

**搜刮（v0.9）**
- [ ] 地圖上有搜刮點，可搜出彈藥與其他物品
- [ ] 敵人死亡後留下可搜刮的屍體
- [ ] 己方屍體含兩把槍、背包內容與一份 DNA
- [ ] 搜刮花費 10 時間，遵循兩段式點擊文法
- [ ] 「全部拿走」在超重時盡可能拿；一件都拿不動時為非法

**撤離（v0.9）**
- [ ] 任何時候回到初始空投點都可以撤離
- [ ] 主目標未完成時判定為 ABORTED，但戰利品照樣帶出
- [ ] 止損按鈕行為不變，放棄後戰場上的一切損失
- [ ] 結算畫面列出帶出與遺留的物品，且分得出「撤離」與「止損」

**敵人戰術 AI（v0.10）**
- [ ] 關閉 `ai.tacticalBehaviour` 時，行為與 v0.9 完全一致
- [ ] 兩種狀態下的自動機器人數據皆已記錄
- [ ] ALERT 敵人會移動到有掩蔽的位置，而不是直線衝向玩家
- [ ] 玩家固守同一個掩體時，敵人會繞到掩蔽失效的方向
- [ ] 敵人不會為了躲而放棄射線：換過去的落點一定也打得到
- [ ] RUNNER 仍然直接衝鋒；SHOOTER 偏好有掩蔽且看得到玩家的位置；HULK 緩慢推進不繞路
- [ ] 落點選擇完全決定性，不含亂數
- [ ] SHOOTER 在有掩蔽處會蹲下，但**不會蹲到把自己弄瞎**
- [ ] RUNNER 與 HULK 永不蹲下
- [ ] IDLE 敵人每兩次行動轉向 90 度，方向固定，且花掉時間不造成無限迴圈
- [ ] SEARCH 抵達目標點後會巡視兩次才轉回 IDLE

**敵人口令（v0.10）**
- [ ] 敵人在行動時宣告下一個動作，下次輪到時**必定執行該動作**，不重新評估
- [ ] 宣告的動作在執行時已不合法（脫離視線或射程）時，改為原地等待
- [ ] 玩家可以破壞敵人的射線，使其浪費一整個行動
- [ ] IDLE 敵人不喊口令
- [ ] 可聽範圍為曼哈頓 12，牆壁不阻擋；超出範圍完全聽不到
- [ ] 看得見的敵人：口令顯示在頭上
- [ ] 聽得見但看不見的敵人：只顯示方向與內容，不顯示確切位置
- [ ] 口令來自 AI 的理由碼；調整權重時口令會跟著改變
- [ ] 敵人之間仍未共享情報
- [ ] 文字有描邊與底襯；多個敵人同時喊話時標示錯開
- [ ] 已宣告的動作屬於 `GameState`，但動畫與顯示狀態不進入 `GameState`

**翻越掩體（v0.19）**
- [ ] 翻越一次移動兩格，落在掩體對面；**任何單位都無法停留在半身掩體格上**
- [ ] 翻越後姿勢強制為站立，蹲姿翻越亦然
- [ ] `WALL` 不可翻越；對面格不存在、不可通行或被佔據時為非法
- [ ] **未新增任何按鈕**；蹲下時沿用先轉向再執行的規則
- [ ] 方向鍵顯示「翻越」與其時間花費，三種狀態可區分
- [ ] 尋路移動會使用翻越，路徑預覽的總時間正確
- [ ] 敵人會翻越，且納入落點評分；**衝鋒型明顯比射手型更常翻越**
- [ ] 視線、掩蔽、姿勢規則完全不變；地圖驗證器仍全數通過

**經濟層（v0.20）**
- [ ] `credits` 存在並持久化；所有價格、報酬倍率、售出折扣皆在資料檔
- [ ] 士兵約為 C 級合約報酬的一半；**最便宜的遺產武器仍貴過一名士兵**
- [ ] 遺產武器為變動的少量現貨（1～3 件），非完整型錄，買走就沒了
- [ ] 現貨更新綁定**完成的合約數**，不綁定真實時間
- [ ] 合約清單顯示報酬與次要目標獎金
- [ ] **主目標完成才給主要報酬**；`ABORTED` 時仍可獲得次要目標獎金
- [ ] 可出售武器實例、雜物、彈藥、消耗品、DNA；有人拿著的槍不能賣
- [ ] 信用點可為負，沒有硬性失敗；負債時收到三級的董事會信件
- [ ] **信用點為負時仍可取得基礎複製人**
- [ ] 結算畫面呈現完整損益，含陣亡與遺留裝備的估值，**且擺在最上面**
- [ ] `schemaVersion` 已遞增，舊存檔提示重置而非崩潰

**自動補給（v0.18 附錄）**
- [ ] 彈藥基準寫在武器上，消耗品基準在資料檔
- [ ] **只從既有庫存拿**，不夠就補多少算多少
- [ ] **不會讓士兵掉到更慢的負重級距**
- [ ] 全員補給依名冊順序，沒有配槍的人不補
- [ ] 公司畫面有全員補給、配裝畫面有單人補給，按鍵上寫得出差多少
- [ ] 回到公司時自動跑一次，且該開關**不影響既有存檔的格式**

**任務中的裝備管理（v0.18）**
- [ ] 背包 → 收納欄、收納欄 → 背包、主手 → 背包三個動作皆可執行
- [ ] 時間花費依**被移動的那一把**（輕 10／重 20，或武器自己的 `swapTime`）
- [ ] 收納欄為空時可從屍體直接拾取到收納欄，只花 10
- [ ] 丟棄仍為 0；從屍體取得的彈藥與消耗品直接併入攜行量
- [ ] 裝備管理位於既有的滿版背包畫面內，**未新增介面**
- [ ] 每次移動裝備都顯示時間花費並需二次確認
- [ ] §23.3 的七個步驟全部可行，**且回收的武器實例識別碼與原本那一把相同**
- [ ] 替補士兵可以完整接手前一個人的裝備

**呈現層與介面（v0.17）**
- [ ] 移動有格間位移動畫，敵我速度為獨立旋鈕且皆在資料檔
- [ ] 兩個旋鈕設為 0 時等同瞬間行為
- [ ] **動畫播放中收到新輸入時，當前這一步立刻完成並開始下一步**；連按方向鍵不黏
- [ ] 攝影機跟隨當前行動中的單位，不分敵我
- [ ] 目標已在畫面內且未太靠邊時，**不平移也不等待**
- [ ] 平移期間目標不行動；可跳過整段演出，跳過後結果與完整播放完全相同
- [ ] 背包、掠奪、日誌、士兵資訊已改為全螢幕
- [ ] 射擊預覽、移動預覽、道具使用過程仍保持地圖可見
- [ ] 掠奪選單在容器被拿空、或玩家離開相鄰範圍時自動關閉
- [ ] **`core/` 完全未改動**；`GameState` 中無任何動畫進度、攝影機位置或選單狀態
- [ ] 決定論測試通過，機器人基準與 v0.16 完全一致
- [ ] 已回報預設旋鈕與 0 旋鈕下的真實牆鐘時間對照

**局外層（v0.16）**
- [ ] `MetaState` 與 `GameState` 分離，任務期間不讀寫 `MetaState`
- [ ] 任務仍是 (派遣快照, 種子, 輸入) 的純函數
- [ ] `instanceCounter` 一併持久化，重新載入後不會產生重複的實例識別碼
- [ ] 名冊中每一名士兵各自持有配裝
- [ ] **同一把武器實例不可能同時出現在兩名士兵身上**
- [ ] 彈藥與消耗品為共用庫存，逐人分配，顯示剩餘量
- [ ] 武器不足以裝備所有士兵時，介面能正確呈現
- [ ] 出擊時手動挑首發；陣亡後手動挑替補，替補帶著自己的配裝降落
- [ ] 沒有配裝的士兵可以被派出（赤手空拳）
- [ ] 陣亡的士兵與留在戰場上的武器實例永久移除
- [ ] 帶出的武器實例加入軍械庫，**且仍是同一個 `instanceId`**
- [ ] 關閉瀏覽器再開啟，狀態完整保留；服役紀錄跨任務累積
- [ ] 補給站可取得士兵、武器、彈藥、消耗品，價格皆為 0 並標示為暫時性
- [ ] `schemaVersion` 不符時提示重置而非崩潰或遷移；`?reset=1` 可用
- [ ] `?map=` 與 `?seed=` 行為不變；機器人基準仍可執行
- [ ] 戰術層規則與數值完全不變

**配裝與武器（v0.15）**
- [ ] 口徑為獨立資料實體，武器以引用方式指定；新增武器只要指定既有口徑就相容
- [ ] AR-9 與 LMG-5 共用 5.56，SG-12P 與 SG-12S 共用 12ga
- [ ] 每發彈藥有重量並計入負重
- [ ] **AR-9 與 RR-4 的數值完全未變**（重量欄位除外）
- [ ] 五把新武器已加入，數值全部來自資料檔，`penetration` 皆為 0
- [ ] SG-12S 在 1 格極準、4 格外幾乎打不到；DMR-7 在 14 格外仍可有效射擊
- [ ] 泵動霰彈槍為增量裝填、一次一發、可隨時停下開槍，且**不是**系列動作
- [ ] 削短霰彈槍有齊射，沿用既有模式鍵，吃 2 發、2 次獨立判定
- [ ] 可用模式清單資料化；只有一種模式的武器，模式鍵是灰的
- [ ] 手持與收納的武器計入負重；門檻為 55／78／100
- [ ] 所有重量依 §19.2 的換算尺（1 單位 = 0.5 公斤）
- [ ] 流程為 合約清單 → 配裝 → 任務；可從配裝退回清單
- [ ] 即時顯示總重、上限、負重級距與移動時間
- [ ] 帶槍不帶對應彈藥時**警告但允許出擊**；超重時阻擋
- [ ] 有預設配裝與「沿用上次」
- [ ] `?map=` 除錯路徑仍可略過配裝畫面（使用預設配裝）
- [ ] 機器人基準以預設配裝跑出可比較的數據

**彈藥型別與機構型式（v0.15 附錄 B）**
- [ ] 彈藥的識別單位為 `AmmoType`，引用 `calibreId`
- [ ] 背包、配裝、裝填、搜刮、屍體、結算清單全部以彈藥型別 id 為鍵
- [ ] 五種彈藥型別已建立，重量與先前一致；預留欄位存在且為預設值
- [ ] 七把武器皆已填入 `action`，且該欄位不參與任何判斷
- [ ] 設定文件已改為兩層版本（遺產／土製），含 84mm 為遺產彈藥
- [ ] 玩家可見的行為完全不變，機器人基準數據與加入本附錄前一致

**合約清單（v0.14）**
- [ ] 開啟遊戲進入合約清單，而非直接開始任務
- [ ] 一次三份、地圖不重複、選擇後進入對應地圖
- [ ] 結算畫面可返回合約清單，清單重新產生
- [ ] `?map=<id>` 仍可略過清單直接指定地圖（除錯與機器人基準用）
- [ ] 相同 `?seed=` 產生相同清單，連續開幾次都相同
- [ ] 地形標籤由統計值推導：**改地圖 → 跑 map:build → 標籤自動跟著改**
- [ ] 難度評級由統計值推導，四張圖分得開
- [ ] 顯示主目標與次要目標數量
- [ ] **不顯示任何報酬金額**
- [ ] 四張地圖各有一份手寫簡報，文體符合世界觀文件 §12
- [ ] 簡報文字與地圖資料分離，存放於獨立資料檔
- [ ] 320px 寬可用、可捲動、兩段式選擇、可點擊元素 ≥ 48×48
- [ ] 戰術層規則行為完全不變，`core/` 仍無瀏覽器 API 與直接的 `Math.random()`

**地圖收攏（v0.13）**
- [ ] mission_02 主要路線至少兩格寬，支線可為一格；長通道之間有路口或房間
- [ ] mission_03 開闊區內有兩到三條角度不同的掩體穿越路線，且明顯長於直線
- [ ] 四張圖的機器人耗時皆落在 1500～3000
- [ ] mission_02 的機器人耗彈不低於基準圖的一半
- [ ] mission_03 的機器人成功率 ≥ 3/5
- [ ] 四張圖皆通過預估完成路徑長度檢查
- [ ] 四張圖的東西向與南北向掩蔽覆蓋率差距 ≤ 20
- [ ] 四張圖的性格仍然肉眼可區分

**地圖與選圖（v0.11）**
- [ ] 三張新地圖符合 §13.1 各自的設計意圖，肉眼可區分，不是同一種佈局的變體
- [ ] 四張地圖全部通過 §13.5 的驗證
- [ ] 每張圖都可以完整通關（自動機器人四張圖各跑一次的數據已記錄）
- [ ] 任務開始時隨機選圖，使用可播種亂數；相同種子選到相同的圖
- [ ] `?map=<id>` 可指定地圖；`?seed=` 的既有行為不變
- [ ] 所有 §13.5 的檢查項皆已實作，驗證失敗會使 CI 建置失敗
- [ ] `docs/map-stats.md` 已產出並提交，且在 CI 中自動更新
- [ ] 結算畫面顯示本次使用的地圖名稱

**系列動作的兩種中斷行為（v0.12）**
- [ ] 序列可標註為 `RESUMABLE` 或 `RESTART`
- [ ] RR-4 裝填為 `RESUMABLE`，中斷後保留進度，下次從中斷處接續
- [ ] 半裝填狀態的 RR-4 不可使用
- [ ] 收起半裝填的 RR-4、換槍、再換回來，進度仍在（進度存在武器上）
- [ ] 消耗品為 `RESTART`，中斷後進度歸零、時間不退還

**準備與使用（v0.12）**
- [ ] 背包中沒有「使用」，只有「準備」
- [ ] 準備欄只有一格，準備花費 10 時間
- [ ] HUD 上一眼看得到準備欄中是什麼
- [ ] 使用按鈕在準備欄為空時為灰色
- [ ] 使用中顯示當前步驟與剩餘時間，可主動中止

**戰地封合劑（v0.12）**
- [ ] 總花費 40 時間，分為兩個步驟，恢復 25 HP 且不超過上限
- [ ] 在敵人火力下使用會被打斷，且進度全失
- [ ] 士兵初始攜帶 1 個；可從搜刮點、敵人屍體、己方屍體取得
- [ ] 消耗品為資料驅動，新增品項只需改資料檔

**背包介面與丟棄（v0.12）**
- [ ] 開啟與檢視不花時間
- [ ] 顯示總重量、容量上限、當前負重級距與對應的移動時間、以及「再撿多少會掉級」
- [ ] 可以丟棄物品，丟棄不花時間，落在腳下且可再被撿起
- [ ] 準備與丟棄需二次確認
- [ ] 背包為滿版面板，蓋掉按鈕群，靠「關閉」退出
- [ ] 所有可點擊元素 ≥ 48×48 CSS px

**規則層**
- [ ] `core/` 有單元測試涵蓋：視線對稱性、時間推進、傷害護甲計算、四方向移動
- [ ] 連發的三次判定各自照抽三個擲值，順序固定；即使目標已倒下也照抽
- [ ] **有一個測試把命中率覆寫為回傳 0，驗證未命中路徑完整可用**：不扣血、照花時間與彈藥、照樣產生噪音、戰鬥紀錄顯示未命中、UI 不崩潰
- [ ] 有一個測試驗證：相同種子 + 相同指令序列 → 相同最終狀態
- [ ] 全程沒有直接呼叫 `Math.random()`
- [ ] `core/` 未引入任何 DOM / Canvas / 瀏覽器 API

---

## 16. 已設計但**這一版不要實作**的系統

以下內容僅供架構參考，避免把路擋死。**不要實作。**

- **DNA 與世代**：屍體可抽取 DNA，繼承前一代約 70% 的經驗，逐代遞減形成天花板。士兵編號採「編號-世代」形式（如 `K-441 Gen.7`）。
- **屍體的兩種去處**：抽 DNA 續血脈，或製成溫室蔬菜換現金。互斥。
- **合成系統**：不同資源組合 + DNA 產出不同數值的士兵，無角色設計，純數值。產出應為分佈而非查表，避免配方被社群一週解出。
- **副業與時價**：發電、溫室等被動收入，短期大量出貨會使價格崩跌。**價格回升應綁定「完成的任務數」而非真實時間**，以免養成掛機 check-in 習慣。
- **大型合約**：各打各的、純計分，按完成的主／次目標貢獻分數，分數需依難度加權以避免刷最便宜的目標。
- **屍體池**：他人陣亡士兵的 DNA 進入伺服器池，補員時作為「回收樣本」提供。取代拍賣行，低人口下仍成立。
- **技能系統**：v0.3 已預留技能鍵與摺疊選單的鉤子，但選單內容為空，不要放任何技能進去。
- **調性**：企業諷刺喜劇，越荒謬越好。人格與趣味全部由文字（合約簡報、裝備說明、企業通知、服役紀錄）承擔，零美術成本。

**架構上唯一需要現在預留的**：`core/` 的純粹性與決定性（§3.1），因為日後合約計分需要在伺服器端重跑同一套規則。其餘一律不預留。

---

## 17. 待調整數值一覽

以下數字是為了讓遊戲能跑起來而填的**預設值**，全部預期會在試玩後修改。請把它們集中在 `data/` 的 JSON 中，不要寫死在程式碼裡。

> **兩組不同單位，不要一起縮放。**
> **生命值單位**（HP、傷害、護甲、保底傷害）是一組可互相比較的數字，要縮放就一起縮放。
> **距離與時間**（射程、最佳射程、視野、噪音半徑、濺射半徑、彈匣、時間成本、`searchTimer`、地圖尺寸、空投點數量）是另一回事，**不隨生命值規模縮放**——放大會直接破壞遊戲。

| 數值 | 目前值 | 調整方向的判斷依據 |
|---|---|---|
| 時間基準 | 移動一格 = 10 | **整張表的分母。**所有時間成本都是它的倍數；改它等於改全部，正常情況下不要動 |
| 玩家移動時間 | 10 | 調它等於同時調「玩家相對每一種敵人的快慢」，牽動最大 |
| 士兵 HP | 60 | 雙方都要能被兩三發打死，才會出現「每一發都可能決勝」的感覺 |
| 敵人 HP | 衝鋒 25 / 射手 30 / 裝甲 90 | 衝鋒型一發必死；裝甲型對 RR-4 是「一發賭命」、對 AR-9 要約 5～9 發 |
| 保底傷害 | 10 | 與生命值同單位。生命值規模變動時要一起調，否則等於實質取消保底 |
| AR-9 / RR-4 基礎命中 | 0.55 / 0.50 | **修正項必須蓋過基礎值**，位置才會成為交火的主題。0.55 − 0.40（良好掩蔽）= 0.15，剛好觸底 |
| 敵人基礎命中 | 近戰 0.70 / 遠程 0.50 | 近戰高是刻意的，讓「不要被貼上來」成為真正的壓力 |
| 敵方行動演出間隔 | 200ms | `presentation.enemyStepMs`。只影響渲染層 |
| HULK 護甲 | 20 | 要讓輕武器「打得動但很蠢」，不要真的免疫 |
| RUNNER 移動時間 | 7 | 必須嚴格小於玩家的 10，否則風箏流成立 |
| HULK 移動時間 | 20 | 笨重感的來源：玩家走兩格它才走一格 |
| SHOOTER 開火時間 | 12 | 取代了原本的「每回合上限 1 次」。略慢於玩家的一個動作，所以不會連打 |
| 敵人狀態轉換時間 | 衝鋒 5 / 射手 8 / 裝甲 15 | **反應窗口的實際長度。**衝鋒型的 5 短於玩家一個動作，所以對它反應不過來是設計 |
| 待機時間 | 10 | 與移動一格等價。若比移動便宜，「等一下再看看」會變成無代價的後手優勢 |
| 增援落地延遲 | 10 | 死亡懲罰的一部分：新兵落地時已經慢了一手 |
| 搜索持續時間 | 30 | 敵人放棄追擊前願意花的時間。越長越難甩掉 |
| RR-4 裝填步數 | 2 步各 10 | 拆步不改總時間，改的是「中途可以被打斷」。步數是曝險長度的旋鈕 |
| 空投點數量／間距 | 3 個 | **這是死亡懲罰的主旋鈕。**越稀疏死一次越痛 |
| 名冊人數 | 4 | 決定一場任務的容錯上限 |
| 重武器噪音半徑 | 14 | 要讓「開這一砲會引來誰」成為真的顧慮 |
| AR-9 / RR-4 傷害 | 30 / 120 | 與 HP、護甲、保底同一組單位，要一起縮放 |
| 命中率總開關 | 已啟用 | `combat.enableToHitRoll`，設 false 可退回一律必中做對照 |
| 命中率下限 | 0.15 | 掩蔽與蹲姿會疊加；太低會讓玩家覺得被規則耍、只能空轉 |
| 射手蹲姿加成 | +0.10 | |
| 目標蹲姿減免 | −0.15 | |
| 部分／良好掩蔽 | −0.25 / −0.40 | 三級列舉，不要改成每格累加 |
| 姿勢改變時間 | 3 | **蹲姿的第二個代價。**免費就等於免費掃視一圈（§5.2）。太貴則會讓玩家懶得蹲，掩蔽系統跟著失效 |
| 背刺命中加成 | +0.30 | 要大到「繞過去值得」，但不能大到「不繞就別打」。0.55 + 0.30 = 0.85，接近上限但沒觸頂 |
| 背刺是否無視掩蔽 | 是 | `combat.backstab.ignoreCover`。掩蔽的意義是「對著你打的人很難打中」，而背刺的前提正是它沒在對著你 |
| 敵人初始面向 | 見 `build_map.mjs` | **這是地圖設計的一部分，不是平衡數字。**背對主要進路的守衛就是背刺教學點 |
| 拖曳位移門檻 | 10 CSS px | 太小會把點擊誤判成拖曳，太大則拖起來很鈍 |
| AR-9 彈匣 | **8** | **這是節奏的主旋鈕。**v0.6 起「煞車必須是彈藥」；v0.9 因連發吃 3 發而調到 8。太小則一半的行動在裝填，太大則彈藥不再是煞車 |
| 初始攜行彈量 | 步槍彈 24 / 火箭彈 2 | 決定「一趟能打多久」。太多的話搜刮就沒有意義 |
| 模式耗彈與命中修正 | 1/2/3 發，+0.10/0/−0.10 | 三者的期望命中是 0.65／1.10／1.35。修正幅度決定「效率 vs 爆發」的傾斜 |
| 重量換算尺 | **1 單位 = 0.5 公斤** | v0.15 的正式定義。所有重量由現實裝備換算取整（§19.2）。新增任何物品都依此換算，**不要臨時決定數字** |
| 攜行上限 | 100 | **這是貪心的價格。**含手持與收納的武器（v0.15） |
| 負重分級 | ≤55→10 / ≤78→12 / ≤100→14 | 標準：背兩把武器、足夠彈藥與一支恢復藥進場後不得影響移動速度。預設配裝 41.6 落在第一級，餘裕 13.4 |
| 新武器數值 | 見 §8.5 的七把表 | **角色分化靠 `optimalRange` 與 `falloffPerTile`，不靠傷害。**若換了槍感覺差不多，代表衰減級距不夠拉開 |
| 口徑單發重量 | 5.56/9mm 0.024、7.62 0.048、12ga 0.09、84mm 6 | 由現實單發重量換算。**輕武器彈藥幾乎不構成壓力是物理事實**，不是設計失誤；真正咬人的只有 84mm |
| 攜行彈藥的增量 | `loadout.ammoStep` | 介面用，鍵是**彈藥型別 id**。彈藥不逐發點擊；84mm 每發 6 重，所以它一發一發加 |
| 彈藥型別 | `data/ammo.json` | 附錄 B。修正欄位（傷害／衰減／濺射／允許機構）v0.15 一律為預設值 |
| 機構型式 | `weapons.json` 的 `action` | 附錄 B。v0.15 不參與任何判斷，日後決定彈種相容性 |
| 口徑的本地生產難度 | `calibres[].localProduction` | 設定用。84mm 為 `NONE` —— 遺產彈藥，用一發少一發 |
| 物品重量 | 步槍彈 0.5／火箭彈 3／DNA 1／武器 6～12 | 與分級表同一套單位，改一個就要重看另一個 |
| 搜刮花費 | 10 | 與移動一格相同。太便宜就變成無腦全撿，太貴則沒人想繞路 |
| AI 落點權重 | 見 `actors.json` 的 `ai` | **這是三種敵人「打起來不一樣」的全部來源。**調它等於改打法，不是改強度 |
| 戰術 AI 總開關 | `ai.tacticalBehaviour` | 關掉即回到 v0.9 行為。效果只能靠對照判斷，所以這個開關要一直留著 |
| 警戒巡視花費 | `ai.patrolTurnTime`（10） | 決定盲區轉一圈要多久，也就是玩家抓空檔的窗口長度 |
| SEARCH 收尾次數 | `ai.searchWrapUpTurns`（2） | 噪音引誘的價值：引開之後它會在那裡待多久 |
| 口令可聽範圍 | `ai.calloutRange`（12） | 太短則機制形同不存在，太長則整張地圖都在對你喊話 |
| 空投點間距區間 | `mapRules.dropSpacing` | 太近則死亡懲罰消失，太遠則回收屍體不可行 |
| 掩體密度區間 | `mapRules.coverDensity` | **刻意寬**：四張圖是對照組。這個區間抓的是結構性錯誤，不是設計約束 |
| 暴露長度上限 | `mapRules.maxExposedRun` / `maxForcedExposure` | 「必經」那一項才是真的門檻；最短路徑那一項只是提醒 |
| 主目標最短距離 | `mapRules.minMainDistance` | 太短則整場任務沒有推進感 |
| 封合劑：時間／恢復量／重量 | 40／25／5（`items.json`） | **慢是最重要的一條**：40 = 四次移動，讓它無法在交火中進行。恢復量太少沒人想花，太多則死亡失去份量 |
| 封合劑出現機率 | `actors.json` 的 `loot` | 恢復必須稀有。它不是標準配備，士兵只帶一個 |
| 準備物品的時間 | `time.prepare`（10） | 免費的話準備欄就只是多一次點擊。太貴則玩家綁手綁腳到不想用道具 |
| 任務長度目標區間 | 機器人耗時 1500～3000 | 換算：2048 時間單位約 205 次行動，真人每次含判讀 4～6 秒 = 14～20 分鐘。**主旋鈕是目標配置，不是畫布大小** —— 縮小地圖會同時壓縮戰術空間 |
| 預估耗時區間 | `mapRules.estRunTime`（700～1400） | 靜態下限估計，對應上面那個實測區間 |
| 方向性掩蔽差距上限 | `mapRules.dirCoverGap`（20） | 太寬則「哪個軸向安全」變成作者無意間決定的；太嚴則地圖設計會過度受限 |
| 翻越時間 | `time.vault`（20） | **不是更快的移動，是穿越屏障的手段。**若成排掩體不再構成屏障，先調高它，不要動掩體配置 |
| AI 的翻越懲罰係數 | `ai.vaultPenalty`（1.5） | 乘上多花的時間倍數，所以調高 `time.vault` 對敵人也生效 |
| 基準報酬 `P` | `economy.json` 的 `baseReward`（400） | **整張價格表的分母。**調它等於調全部 |
| 各級合約倍率 | `rewardByRating`（C 1.0／B 1.4／A 1.9／S 2.6） | 風險與報酬的斜率。太平則玩家永遠挑最簡單的 |
| 次要目標獎金 | `secondaryBonus`（0.15） | 決定「主目標太硬就走」值不值得 |
| 售出折扣 | `sellDiscount`（0.45） | 買賣價差就是「東西流過你手上」的成本 |
| 士兵／武器／彈藥價格 | `economy.json` 的 `prices` | **人便宜、好槍貴。**最便宜的遺產武器必須貴過一名士兵 |
| 遺產武器現貨 | `legacyStock`（1～3 件，每 1 份合約換一批） | 太頻繁則稀缺感消失，太稀疏則玩家永遠只有土製槍 |
| 負債級距門檻 | `debtTiers`（0／−600／−1800） | 決定董事會第幾封信在什麼時候來 |
| 起始資金 | `meta.start.credits`（400） | 約一份 C 級報酬 —— 夠買一個人或補一輪彈藥，不夠買一把遺產武器 |
| 自動補給基準 | `weapons.json` 的 `resupplyMagazines`（多數 3、RR-4 與 LMG-5 為 2） | 太高則補一次就掉級或吃光庫存；太低則等於沒補。RR-4 的 2 是設定上的限制：84mm 不可生產 |
| 回公司時自動補給 | `ui.json` 的 `autoResupplyOnReturn`（true） | 關掉就只剩手動按鍵 |
| 搬裝備的時間 | 該武器的 `swapTime`（輕 10／重 20） | **在敵火下騰位置應該是件蠢事，在掩體後應該是可行的。**太便宜則屍體回收沒有代價，太貴則替補接手裝備變得不可行 |
| 玩家／敵人移動動畫 | `ui.json` 的 `animation.playerMoveMs`（80）／`enemyMoveMs`（180） | **純呈現，設 0 等同瞬間完成。**敵人慢是刻意的：它的動作是要給玩家看的 |
| 敵人行動時的鏡頭 | `animation.followActingUnit`（**OFF**） | 試玩回報追尾會暈（§22.2.1）。`SNAP` 是 v0.16 的行為，`PAN` 是 v0.17 原本的設計 |
| 攝影機平移時間 | `animation.panMs`（220） | 平移期間該單位不行動，所以這個值直接乘上「需要平移的次數」 |
| 攝影機邊緣門檻 | `animation.edgeMargin`（2 格） | **太小則幾乎不平移，太大則每隻敵人都要等一次平移。**這是 §22.2 與 §22.3 之間的實際旋鈕 |
| 掠奪選單距離門檻 | `ui.json` 的 `lootCloseDistance`（1） | 走離相鄰範圍就關。太大則走遠了選單還開著，太小則站位一動就被關掉 |
| 新公司起始狀態 | `meta.start`（4 人、4 把槍、少量彈藥） | **刻意不讓四個人都配得滿** —— 第一次進入配裝畫面就應該遇到「東西不夠分」的處境 |
| 補給站內容與批量 | `meta.supply` | **暫時性的佔位**（全部 0 元）。經濟層之後只需要填入價格，不必重做介面 |
| 存檔格式版本 | `meta.schemaVersion` | 版本不符一律提示重置，**不做遷移** |
| 任務紀錄保留筆數 | `meta.missionLogSize`（20） | 只是公司畫面上的歷史，不影響規則 |
| 合約清單張數 | `contract-rules.json` 的 `listSize`（3） | 太少則「選擇」變成「二選一」，太多則玩家在清單上花的時間超過任務本身 |
| 地形標籤門檻 | `contract-rules.json` 的 `tags[].value` | 一條標籤在四張圖上全中或全不中，就等於沒有這條標籤。調的是**它分不分得開**，不是它準不準 |
| 標籤顯示上限 | `contract-rules.json` 的 `maxTags`（4） | 卡片上放得下的資訊量。超過的依 `priority` 砍掉，所以通用標籤要給大的 priority |
| 難度公式權重 | `contract-rules.json` 的 `difficulty.terms` | 每一項是 `(統計值 − base) × weight`。它讀的是**地形說了什麼**，不是機器人跑出什麼 —— 見 §18.3 |
| 難度級距 | `difficulty.bands`（5.5／8／10.5） | 目前四張圖剛好各落一級。全部擠在同一級就等於沒有評級 |
| 敵人掉落機率 | 見 `actors.json` 的 `loot` | 決定「打完這場划不划算」。必掉的彈藥是保底，稀有裝備是誘因 |
| 傷害 spread | AR-9 ±5 / RR-4 ±20 | |
| 護甲 spread | HULK ±8 | 每發各擲一次；調它等於調「這個敵人的防護穩不穩定」 |
| `accuracy` / `falloffPerTile` | 見 §8.5 | 啟用擲骰後才有意義；設定射程衰減曲線用 |

> **v0.3 的已知副作用，本次不處理**：四方向移動會使地圖的有效距離變長（原本斜向 3 步的路徑現在要 6 步），任務總耗時將上升；曼哈頓射程也會讓現有射程數值在斜向感覺明顯變短。地圖尺寸、空投點間距與射程數值全部是本節的旋鈕，留待試玩後一起調。結算畫面照常記錄總耗時，作為後續調整的依據。

---

## 18. 合約清單（局外層的第一塊）

> **刻意不含**：持久化、經濟（不顯示任何報酬金額）、名冊管理、配裝。
> 這一版只做「選哪一張圖去打」這個決策，以及承載它的畫面。

### 18.1 流程

```
開啟遊戲
   ↓
合約清單畫面  ←──────────┐
   ↓ 選擇一份合約          │
任務（既有的戰鬥）          │
   ↓                      │
結算畫面                   │
   ↓「返回合約清單」────────┘
```

- 一次提供 **3 份**合約，從四張地圖中**不重複**抽取。
- 抽法是部分洗牌 —— 連順序都是抽的，否則清單永遠照 mission_01…04 排，
  玩家會學會「第一張最簡單」。
- 清單專用一個亂數產生器，與任務的亂數**完全分開**。「返回合約清單」時
  **繼續**從同一個產生器抽，所以每次都是新的一批，而整條序列仍由最初的種子決定：
  同一個 `?seed=` 重玩，第一批、第二批、第三批合約都會一模一樣。
- 每份合約的**任務種子在產生清單時就抽好**：玩家看到的那張卡片，
  與他按下去會玩到的那一場，是同一件事。
- `?map=<id>` 仍然略過整個清單直接開圖 —— 除錯與機器人基準都走這條路。
- 結算畫面的「返回合約清單」與「重新開始」是兩件事：
  後者重打**同一份合約、同一個種子**，前者換一批。

### 18.2 地形標籤必須由數據產生，不能手寫

> **標籤由驗證器已經在算的統計值推導，不得手寫。**

手寫的標籤會在地圖被修改之後開始說謊，而地圖一定會被修改 ——
v0.13 才剛把 mission_02 與 mission_03 整張換掉。統計值不會說謊，
它跟著 `npm run map:build` 一起重算，CI 會比對（§13.6）。

門檻對照表放在 `data/contract-rules.json`：

| 標籤 | 讀哪個統計值 |
|---|---|
| 掩蔽充足／掩蔽稀少 | `coverDensity` |
| 空間狹窄／開闊地形 | `openness`（v0.14 新增，見 §13.6） |
| 存在無掩護路段 | `directRun` |
| 有必經的無掩護段 | `forcedRun` |
| 縱深大 | `estRun` |
| 敵方以遠程為主 | `shooterRatio` |
| 存在重裝目標 | `hulks` |
| 現地補給稀少 | `caches` |

一張卡片最多顯示 `maxTags` 個，超過的依 `priority` 砍掉。
**排序用 priority 而不是「觸發了幾條」**：像「存在重裝目標」這種目前四張圖
全都會觸發的標籤，priority 給到 90，只有在卡片還有空位時才擠得進去。

### 18.3 難度評級

同樣由統計值推導，公式在 `contract-rules.json`：

```
score = Σ (統計值 − base) × weight
```

再對照 `bands` 落到 C／B／A／S。

> **這個分數讀的是「地形說了什麼」，不是「機器人跑出什麼」。那是兩件事。**
>
> 例：mission_02 的掩體密度只有 4%，地形上這叫危險，評級落在 A；
> 但它的走廊窄到雙方多半根本看不到對方，機器人反而 5/5 全通。
> 評級照實反映前者，因為那是玩家在**接合約的當下唯一能知道的東西**。
> 委託方不會附一份實測報告。

### 18.4 合約簡報

**這是本作的敘事載體。**主線與所有角色感都由合約簡報、公文與服役紀錄承擔，
不做過場動畫。這個畫面就是那些文字第一次有地方放。

文體嚴格依世界觀文件 §12：

> **用最無趣的公文語言，講最荒謬的事。不要吐槽，不要眨眼，不要讓文字知道自己好笑。**

- 會計與稽核語言優先：資產減損、繼續經營、期後事項、內部控制
- 人命一律以財產語彙表達
- 公文格式要正確：旨、說明、辦法、附件
- **絕不解釋笑點**

每張地圖一份，文字放在 `data/contracts.json`，與地圖資料分離，方便日後大量擴充。
每份包含委託方、作業事由（一律寫成「代為維護公司財產」之類的法律虛構）、
目標說明，以及一段與地形性格呼應、但**不重複標籤內容**的描述。

### 18.5 介面

- 直向、320px 寬可用，整份清單可捲動
- 每份合約一張卡片；收合狀態就看得到編號、評級、作業地點、標籤與目標數
- **兩段式**，與地圖操作同一套文法：點卡片＝展開簡報，再點＝確認出擊
- 一次只展開一張
- 所有可點擊元素 ≥ 48×48 CSS px
- **不顯示任何報酬金額**（沒有貨幣系統，顯示金額等於說謊）

### 18.6 這個畫面將來會承載

1. 合約報酬與經濟（E 階段）
2. **主線推進** —— 一條主線就是一串有編號的合約，簡報文字逐漸變調
3. 潛入契約 —— 承接條件、士兵強度下限、被發現後的規則差異
4. 聯合大型合約 —— 共用計數器與貢獻計分
5. 持久化（B 階段）—— 清單改為跨場次保留，接了又放棄要有後果

---

## 19. 出擊前配裝（v0.15）

> **刻意不做**：持久化與經濟。所有武器彈藥隨時可選，沒有稀缺性與價格。
> 這一階段可接受，但**數值不要調成假設玩家永遠拿得到最好的組合**。
> 穿甲值也一律維持 0 —— DMR-7 與 LMG-5 將來會有，但那與稀有穿甲彈一起處理。

流程：

```
合約清單 → 選擇合約 → 【配裝】 → 任務 → 結算 → 返回合約清單
```

`?map=<id>` 略過清單與配裝，一律使用預設配裝 —— 除錯與機器人基準走這條路。

### 19.1 口徑系統

口徑是**獨立的資料實體**（`rules.json` 的 `calibres`），武器以 `calibre` 引用它。
這樣新增武器只要指定口徑，就自動與既有彈藥相容。

| 口徑 | 名稱 | 每發重量 | 誰在吃 |
|---|---|---|---|
| `5.56` | 5.56×45mm | 0.024 | AR-9、**LMG-5** |
| `7.62` | 7.62×51mm | 0.048 | DMR-7 |
| `12ga` | 12 號徑 | 0.09 | SG-12P、**SG-12S** |
| `9mm` | 9×19mm | 0.024 | P-9 |
| `84mm` | 84mm 無後座力 | **6** | RR-4 |

> **共用是刻意的設計。**七把槍如果各吃各的，配裝就沒有取捨；分開之後，
> **取捨會落在「餵幾種彈藥」而不是「哪把槍比較強」** —— 那才是這個畫面真正的決策。
> 命名參考現實中的 NATO 制式，讓玩家憑常識就能推測相容性。

### 19.2 重量換算尺

> **1 遊戲重量單位 = 0.5 公斤。**

所有重量由現實裝備換算並取整。**之後新增任何物品都依此換算，不要臨時決定數字。**

| 遊戲物品 | 現實參考 | 現實重量 | 遊戲單位 |
|---|---|---|---|
| AR-9 制式步槍 | M4A1（含彈匣） | 3.4 kg | 7 |
| SG-12P 泵動霰彈槍 | 雷明頓 870 | 3.6 kg | 7 |
| SG-12S 削短霰彈槍 | 削短雙管 | 2.7 kg | 5 |
| DMR-7 精確射手步槍 | HK417 / M110（含瞄具） | 5.5 kg | 11 |
| LMG-5 輕機槍 | M249 SAW（空槍） | 7.5 kg | 15 |
| P-9 手槍 | Glock 17（含彈匣） | 1.0 kg | 2 |
| RR-4 無後座力砲 | Carl Gustaf M3 | 10 kg | 20 |
| 戰地封合劑 | 個人急救包等級 | 1.0 kg | 2 |

> 84mm 採一般 HEAT 彈的重量（3.0 kg → 6）。導引彈藥（約 6.8 kg）本作不採用。

### 19.3 負重門檻

**標準**：背兩把武器、足夠彈藥與一支恢復藥進場後，不得影響移動速度。

**v0.15 起手持與收納的武器計入負重。**以前不計，於是「多帶一把槍」是完全免費的，
配裝畫面就不會有取捨。

預設配裝的總重：

```
AR-9        7
RR-4       20
5.56 ×24    0.576
84mm ×2    12
封合劑       2
──────────────
合計       41.576
```

| 級距 | 移動時間 |
|---|---|
| ≤ 55 | 10（不受影響） |
| ≤ 78 | 12 |
| ≤ 100 | 14 |
| 攜行上限 | 100 |

預設配裝落在第一級，尚有 13.4 單位（約 6.7 公斤）的自由空間。
**戰利品是把玩家推過門檻的主要壓力來源** —— 這是刻意的，貪心與速度的取捨。

> **配裝的真正取捨不在彈藥，而在重裝備。**
> 以本作的彈藥數量級，輕武器彈藥的重量幾乎不構成壓力（24 發 5.56 只有 0.58）。
> **這是物理事實，不是設計失誤** —— 現實中要背到有感得帶數百發。
> 所以本階段配裝的實際決策是：**我要在開始搜刮之前，先花掉多少載重量？**
> 帶 RR-4 加兩發火箭就佔掉 32；帶 SG-12S 加 P-9 只佔 7，留下大量空間給戰利品。
>
> 若日後希望彈藥數量本身成為約束，正確的作法**不是**加重彈藥重量（那會違背現實比例），
> 而是加入**每口徑的攜行槽位上限**（相當於彈袋數量）。本次不實作，記錄備查。

### 19.4 配裝的單位是「一對」

玩家真正在選的是**組合**，不是單一武器：

| 組合 | 適用的合約標籤 |
|---|---|
| SG-12S + DMR-7 | 近遠互補，適合縱深大的圖 |
| AR-9 + RR-4 | 通用 + 反裝甲，「存在重裝目標」 |
| LMG-5 + P-9 | 火力 + 應急，但共吃 5.56 的彈池很快見底 |
| SG-12P + AR-9 | 狹窄空間，「空間狹窄」標籤 |

**這代表七把槍就足以撐起有意思的配裝，不需要更多。**

### 19.5 介面

- 直向、320px 寬可用，可捲動；所有可點擊元素 ≥ 48×48 CSS px
- 可選主手、收納（皆可留空）、各口徑攜行彈藥、消耗品數量
- **合約的評級與地形標籤留在畫面上端** —— 玩家調整配裝時必須同時看得見
  他是為了什麼在調。這是這個畫面存在的全部理由
- 必須即時顯示總重、上限、當前負重級距與該級距的移動時間；跨過門檻要有明確提示
- 彈藥以合理的增量調整（`loadout.ammoStep`），不要逐發點擊
- 提供預設配裝與「沿用上次」。沒有持久化，「上次」只在本次遊玩期間有效

**兩個警告，只有一個是阻擋：**

| 情況 | 處理 |
|---|---|
| 帶了武器卻沒帶對應口徑的彈藥 | 明確警告，**但允許出擊** |
| 總重超過攜行上限 | **阻擋** |

> 第一項刻意只警告不阻擋 —— 依本作一路以來的原則：**系統不禁止，只標價。**
> 帶了 DMR 卻不帶 7.62 是玩家的選擇，遊戲只負責讓他知道。

### 19.6 試玩時要注意的事

1. **衰減曲線是否真的讓武器感覺不同** —— 換了槍若感覺差不多，代表級距不夠拉開
2. **增量裝填的手感** —— 壓力下填一發打一發是否成立
3. **手槍是否真的會被帶** —— 若從不被選，換裝與開火時間還要再降
4. **戰利品是否真的會把你推過門檻** —— 壓力應來自戰利品與重裝備，不是輕武器彈藥
5. **合約標籤是否真的影響配裝決定** —— 這是這一版的核心目的

### 19.7 給下一步的紀錄

1. **持久化（B 階段）**—— 有了它，配裝才有稀缺性：帶出去的彈藥用掉就是少了
2. **穿甲** —— DMR-7 與 LMG-5 應該有穿甲值，與稀有穿甲彈一起處理
3. **經濟** —— 武器與彈藥的價格，配裝才會真正變成取捨
4. **名冊差異化** —— 目前四人數值相同，「派誰去」還不是決策
5. **地形統計加入平均可視距離** —— 現行難度評級抓不到「這張圖根本打不起來」的情況

---

## 19 附錄 A：武器是實例，不是型號引用（v0.15）

> **本次不新增任何玩法。**只是把資料模型改成日後接得上的形狀。

### A.1 為什麼要現在做

專案有一條長期路線：武器將來會有詞條而變得獨一無二，並且會在伺服器端流通 ——
玩家陣亡遺落的槍，經拾荒者回收後重新洗進獎勵池，可能出現在另一個玩家的任務裡。

那條路線的最低前提是：**一把槍必須是一個「實例」，不是一個型號引用。**
若武器只是「玩家擁有 AR-9 這個型號」，那就沒有任何東西可以被追蹤、
被標記、被賦予來歷 —— 追蹤到的只是一個名字。

現在加欄位幾乎零成本；等背包、屍體、戰利品、配裝、存檔全部長好之後再改，
會動到每一處。這與命中率鉤子、`penetration` 欄位、`CombatEvent` 事件流
是同一種作法：**先留形狀、後填內容。**

### A.2 型號與實例

見 §4 的 `WeaponType` / `WeaponInstance`。所有持有處都持有實例：
`Unit.equipped`、`Unit.stowed`、背包中的武器、屍體上的武器、
配裝畫面的選擇結果、結算畫面的「帶出」與「遺留」。

**搬動一把槍（換手、掉落、撿起）保留它的 `instanceId`** ——
換了識別碼追蹤就斷了。只有「造一把一樣的新槍」才會拿新號（`duplicateWeapon`）。

`instanceId` **必須是決定性的**：走的是與物品、屍體同一個流水號
（`GameState.nextEntitySerial`，本身在可序列化狀態裡），
不得使用時間戳或 `crypto.randomUUID()`。否則
「相同種子 + 相同指令序列 ⇒ 相同結果」這條硬性要求（§3.1）就破了。

### A.3 詞條與來歷（只有形狀）

`affixes` 與 `provenance` 一律為空，v0.15 不實作任何產生、掉落或顯示邏輯。

有效數值的計算路徑必須經過套用點：

```
基礎型號數值 → 套用詞條 → 得到實際數值
```

實作在 `core/weapon.ts` 的 `resolveStats()`，是唯一的套用點；
改動詞條請走 `withAffixes()`，它會重新套用一次，所以實例上的數值不會與詞條脫節。

> 因為詞條為空，**實際數值必然等於型號數值** ——
> `tests/weapon-instance.test.ts` 有一條專門守這件事。
> 這樣日後真的填入詞條時，就知道套用點確實在生效而不是被繞過去了。

`provenance.actor` **只存公司或勢力名稱，不得存放帳號或使用者識別。**
這是日後開放多人時的隱私邊界，現在先立下來。

### A.4 設定上的依據（**已由附錄 B 更正，見 §20**）

> 這一節原本寫「這顆星球上的每一把槍都是撤離之前製造的，總數固定且只會減少」，
> 以及「玩家永遠不能製造武器」。**那兩句推論過頭了，請勿採用** ——
> 若所有槍都不可補充，這顆星球上早該沒有槍了。
> 正確的兩層版本見 §20.1，以及 `docs/setting-bible.md` §4。
>
> 更正**不影響 A.1–A.3 的任何一條**：實例、決定性識別碼、詞條套用點
> 的理由全部成立，而且在兩層版本下更成立 —— 需要被追蹤的正是那些不可補充的遺產武器。

### A.5 明確不做

- 不實作任何詞條效果、產生或掉落
- 不實作來歷的填寫或顯示
- 不實作伺服器、物品池、跨玩家流通
- 不改變任何武器數值或玩法
- 不改變「離開任務後遺落的東西即永久失去」的現行規則

---

## 20 附錄 B：更正附錄 A 的設定推論，並預留彈藥型別（v0.15）

> **一項更正（設定，非機制）＋ 一項資料結構鉤子。不含任何玩法變更。**
> 土製武器與霰彈彈種本次不實作。

### 20.1 更正：武器分兩層

附錄 A §A.4 寫的「每一把槍都是撤離之前製造的，總數固定且只會減少」
**推論過頭了**：若所有槍都不可補充，這顆星球上早該沒有槍了，
但設定上到處都是武裝勢力。

> **撤離帶走的是精密製造，不是一切製造。**
>
> **簡易槍械與大部分彈藥仍然可以在本地生產與維護；精密武器不行。**

| | 遺產武器 | 土製武器 |
|---|---|---|
| 來源 | 撤離前製造 | 現在仍可生產 |
| 補充 | **不可能** | 可以 |
| 經濟性質 | 不可替代的資本 | 消耗品 |
| 現有對應 | AR-9、DMR-7、LMG-5、RR-4 | 拉栓手槍與步槍、削短霰彈槍等 |
| 典型形制 | 半自動、自動、精密光學、氣動機構 | 拉栓、中折、單發 |

- 遺產武器的總數固定且只會減少；土製武器不會。
- 稀有度自我解釋：好槍稀有不是因為掉落率，是因為世界上就剩那幾把。
- **大部分在場上廝殺的複製人，手上拿的其實都是土製槍。**

**經濟層約束的正確版本**：玩家不能製造**遺產武器**，只能取得、交易、修復；
土製武器則可以生產。合成系統可以長士兵，也可以做土製槍，但長不出一把精密武器。
換一種講法：**能製造的東西不保值；保值的東西不能製造。**

**彈藥同樣分層**：12 號徑與粗製手槍彈容易本地生產；5.56 與 7.62 困難但可行；
**84mm 不可能生產，屬遺產彈藥** —— 每開一砲，世界上就永遠少一發。
記在 `rules.json` 的 `calibres[].localProduction`，v0.15 不參與任何判斷。

### 20.2 彈藥型別與口徑分離

> **彈藥的識別單位是「彈藥型別」，型別引用口徑 —— 不是拿口徑當彈藥。**

未來同一種口徑會有多種彈種：

| 12 號徑彈種 | 定位 |
|---|---|
| 鋼珠彈 | 現在的預設：高傷害、陡峭衰減、單一目標 |
| 獨頭彈 | 單發高傷害、衰減平緩、射程拉長 |
| 鋼索切段 | 命中目標與其相鄰單位，每個目標傷害低 |

同理，5.56 之後會有穿甲彈，7.62 會有精密比賽彈。

**這是結構性的改變，不是加一個欄位。**若背包彈藥實作成「口徑 → 數量」，
日後背包、配裝、裝填、搜刮、屍體、結算清單每一處都要改 ——
與附錄 A 的武器實例是同一類問題。

型別定義在 `data/ammo.json`（見 §4 的 `AmmoType`）。
**型別 id 同時就是它在背包裡的 `defId`**，所以搜刮表、地圖補給箱、
初始配裝一律引用型別 id，`makeItem()` 不必知道彈藥是特別的。

修正欄位（`damageModifier`／`falloffModifier`／`splashRadius`／`allowedActions`）
一律為預設值，v0.15 不參與任何判斷。

> 「這把槍餵得到哪些型別」的判斷集中在 `inventory.ts` 的 `feedableTypes()` 一處。
> v0.15 就是「同口徑的全部」；日後還要看機構型式，改那一個函式即可。
> 同理，**「裝哪一種彈」日後會是玩家的決定** —— 那時 `takeAmmoFor()` 要吃一個型別參數。

### 20.3 機構型式

`WeaponType.action`：`BREAK` / `PUMP` / `BOLT` / `SEMI` / `AUTO` / `BREECH`。

| 武器 | action |
|---|---|
| AR-9 | AUTO |
| RR-4 | BREECH |
| SG-12P | PUMP |
| SG-12S | BREAK |
| DMR-7 | SEMI |
| LMG-5 | AUTO |
| P-9 | SEMI |

**v0.15 不使用這個欄位做任何判斷。**它存在的目的是日後的相容性規則：

> 彈藥的相容性不只看口徑，還要看機構型式。破爛的中折式沒有複雜機構
> 可以被打壞，所以能塞任何東西進去；泵動槍的抽殼機構會被鋼索切段毀掉。

**這讓「粗糙」成為一種能力，而不只是比較爛** —— 那是土製武器存在的意義。

### 20.4 v0.15 的實際內容不變

每個口徑只有一種型別，數值與附錄前完全相同：

| 彈藥型別 | 口徑 | 每發重量 |
|---|---|---|
| `standard_5.56` | 5.56 | 0.024 |
| `standard_7.62` | 7.62 | 0.048 |
| `buckshot_12ga` | 12ga | 0.09 |
| `standard_9mm` | 9mm | 0.024 |
| `heat_84mm` | 84mm | 6.0 |

介面上，一個口徑只有一種彈種時顯示口徑名（與 v0.14 以前完全一樣），
有多種時才把彈種名接上去 —— **玩家不必為了還不存在的東西多讀一行字。**

### 20.5 給下一步的紀錄

日後的「土製武器與彈種」批次會用到本附錄預留的兩個欄位：

1. 一批土製武器：拉栓手槍、拉栓步槍、中折霰彈槍等，粗糙、便宜、可生產
2. 12 號徑的三種彈種，其中鋼索切段**僅限 `BREAK` 機構**
3. 詞條系統啟用，土製武器的詞條偏向「粗勇」而非「精密」

那一批的設計重點是：**讓粗糙成為一種能力，而不只是比較爛。**

---

## 21. 局外層：持久化、逐人配裝與公司（v0.16）

> **分水嶺版本。**這一版之後，專案從「單場戰鬥模擬」變成 roguelike。
> **明確不做**：經濟（補給站全部 0 元）、合成、DNA 世代、技能、
> 跨任務傷勢累積、合約清單的跨場次保留。

### 21.1 `MetaState` 與 `GameState` 分離

`MetaState` 是局外層的持久狀態，**不得混入 `GameState`**。任務仍必須是純函數：

```
mission(派遣快照, 種子, 玩家輸入) → 任務結果
```

- 任務開始時，從 `MetaState` 取一份**派遣快照**（`makeDeployment`）
- **任務期間完全不讀寫 `MetaState`** —— 連替補也是從快照裡取
- 任務結束後由局外層把結果套用回去（`missionResultOf` → `applyMissionResult`）

專案的四條硬性要求全部維持：`core/` 純函式、決定論、無 `Math.random()`、數值在資料檔。

> **實例識別碼的計數器必須一起持久化**（`MetaState.instanceCounter`）。
> 附錄 A 要求 `instanceId` 由決定性來源產生；若那個來源的狀態不存檔，
> 重新載入頁面之後就會產生重複的識別碼，而武器實例的整個追蹤機制會從根部壞掉。

### 21.2 `MetaState`

見 §4 的型別定義。要點：

- `roster: Soldier[]` —— 每人各自持有 `Loadout` 與 `ServiceRecord`
- `armoury: WeaponInstance[]` —— 公司持有的**所有**武器；「未指派」是算出來的，不是另一份清單
- `ammoStock` / `consumableStock` —— **共用庫存**，鍵是彈藥型別 id 與消耗品 defId
- `Loadout.equippedWeaponId` 引用的是**實例**

### 21.3 逐人配裝：從「選裝備」變成「分配資源」

這是這一版最重要的體驗變化：

- **武器是實例，同一把槍只能給一個人。**你有三把槍、四個人，就有一個人沒槍。
- **彈藥與消耗品是共用庫存，逐人分配。**你有 60 發 5.56 和三個人，你要決定怎麼分。

玩家面對的不再是「哪把槍比較強」，而是**「我要把有限的東西分給誰」**。

由此自然長出兩種配裝原型，**兩種都不需要任何額外設計**：

| 原型 | 內容 |
|---|---|
| **主攻** | 重裝、彈藥充足、載重量吃緊 |
| **回收** | 只帶手槍與封合劑，**刻意把載重量留空**，任務是去把前一個人的東西撿回來 |

第二種完全是規則自己長出來的。

### 21.4 派遣

```
公司畫面 → 合約清單 → 選合約 → 派遣（挑首發） → 任務
       ↑                                          ↓
       └────────── 結算 → 套用回 MetaState ────────┘
```

- 出擊時**手動挑選首發士兵**
- 陣亡後**手動挑選替補**，替補**帶著他自己的配裝**在最近的空投點降落（取代 §10.1 第 6 點）
- 名冊中所有士兵都可以被投入，沒有人數上限；名冊耗盡 → `WIPED`
- 沒有配裝的士兵可以被派出，他會赤手空拳落地

### 21.5 任務結果如何套用

見 §11.5。核心：**只有帶出來的東西還在。**

> **生還者的生命值全額恢復是佔位規則。**跨任務的傷勢累積需要治療手段，
> 而治療在本作設定中意味著「更換」—— 那屬於經濟與合成層。

### 21.5.1 自動補給（v0.18 附錄）

撤離帶回來的彈藥併回**共用庫存**，士兵的攜行量歸零（§5.1）。
那個模型是刻意的 —— 但它的副作用是每一場之後都要手動把彈藥一發一發按回去。

**補到基準**（`resupplySoldier` / `resupplyAll`）：

- 彈藥基準寫在**武器**上：`resupplyMagazines × magazine`。
  AR-9 的 3 個彈倉剛好是 24 發，也就是 v0.15 以來的預設攜行量。
  RR-4 只給 2 —— **84mm 是遺產彈藥，不可本地生產**（附錄 B §1.3），
  自動補給不該把它當消耗品灑出去。
- 消耗品基準在 `rules.json` 的 `meta.resupply.consumables`。

兩條界線：

1. **只從公司既有的庫存拿，不會無中生有。**庫存不夠就補多少算多少 ——
   稀缺性沒有被拿掉，被拿掉的只有按鈕次數。
2. **不會讓他變慢。**補到目前那一級負重的上限就停。
   自動的東西不可以悄悄改變他的移動速度。

全員補給**依名冊順序**，所以庫存不夠時誰先拿到是決定性的。
沒有配槍的人不補 —— 沒有槍就沒有要餵的東西。

介面：公司畫面（名冊分頁）有「全員補給」，每個人的配裝畫面有「補給至基準」，
按鍵上直接寫出「現在有多少／基準多少／庫存夠不夠」。

任務結束回到公司時自動跑一次，由 `ui.json` 的 `autoResupplyOnReturn` 控制。
**它放在介面設定而不是存檔裡** —— 改它不會讓既有存檔失效。

### 21.6 補給站（v0.20 起有價格）

可取得：新的複製人、土製武器、**遺產武器的現貨**、彈藥、消耗品；也可以出售。
價格與現貨規則見 §24。

> v0.16～v0.18 附錄 這一節是「全部 0 元的暫時佔位」。
> 當初做成商店的形狀而不是一顆「新增」按鈕，就是為了讓經濟層只需要填入價格 ——
> **v0.20 兌現了那個賭注：介面結構沒有重做，只是每一列多了一個數字。**
>
> 最終形態仍然是**合成（長士兵、做土製槍）＋交易（取得遺產武器）**，
> 見 `docs/game-design.md` §5。

### 21.7 存檔、版本與重置

- 存於 `localStorage`；**每次回到公司畫面時儲存，任務進行中不儲存**
- `schemaVersion` 必填。載入時版本不符：**不要遷移，也不要崩潰**，
  顯示明確訊息並提供「重置並開始新公司」
- 公司畫面有明顯的重置入口（同一顆鍵按兩次確認）；`?reset=1` 直接清除
- `?map=` 與 `?seed=` 的既有除錯行為維持不變。**`?map=` 同時略過整個局外層**，
  用測試用的派遣快照直接開打 —— 機器人基準走這條路

> 專案迭代很快，存檔格式會被之後每一版打破。
> **遷移程式碼的維護成本遠高於重置。**

### 21.8 難度預期

**這一版之後遊戲會變得明顯嚴苛。**
開闊圖賠掉三個人，在沒有損失時只是一個數字；
在會賠掉三個真的士兵、以及他們身上的槍時是另一回事。

**本次不調整任何戰術層數值來補償。**先看實際變成什麼樣，數值調整留待下一輪。

### 21.9 給下一步的紀錄

1. **難度重調** —— 戰術層數值大概需要一輪調整
2. **經濟** —— 補給站填入價格，戰利品與 DNA 才有用途；
   「一個士兵值幾場任務的收入」這個數字終於要決定了
3. **名冊差異化** —— 目前所有士兵數值相同，「派誰去」還不是決策
4. **合成與世代** —— 補給站的最終形態
5. **跨任務傷勢** —— 與經濟層的醫療費用一起處理
6. **合約跨場次保留** —— 「接了又放棄」才會有後果

---

## 22. 移動演出與攝影機跟隨（v0.17）

> **純呈現層與介面。不改動任何規則、數值或 `core/`。**
>
> - `GameState` 中不得出現任何動畫進度、攝影機位置或選單狀態
> - 決定論測試必須完全不受影響
> - **跳過或關閉動畫後的最終狀態，必須與播放完整動畫的結果完全相同**
>
> 旋鈕因此**不放在 `rules.json`**，而是 `data/ui.json`，由 `ui/config.ts` 讀 ——
> 規則層對「攝影機平移要花幾毫秒」沒有意見，也不能有意見。

### 22.1 格間移動演出

單位移動一格時以位移動畫呈現。旋鈕敵我分開（`ui.json` 的 `animation`）：

| 旋鈕 | 預設 | 意義 |
|---|---|---|
| `playerMoveMs` | 80 | 玩家移動一格 |
| `enemyMoveMs` | 180 | 敵人移動一格 |

**`0` 代表瞬間完成**，等同 v0.16 的行為。兩個旋鈕都必須支援 0，作為對照與退路。

> 敵人預設慢於玩家是刻意的：
> **敵人的動作是要給玩家看的，玩家自己的動作是要讓他快點走完的。**

**最重要的一條是可被打斷：**

> **動畫播放中收到新的輸入 → 當前這一步立刻瞬移完成 → 立即開始處理下一步。**

這是移動動畫毀掉 roguelike 手感最常見的方式。
**慢慢走的時候有演出，趕路的時候不擋人。**

實作上，`dispatch()` 的第一行就是 `motion.finishAll()` ——
在任何指令被解算之前，畫面先追上邏輯。
動畫純粹是呈現：單位的邏輯位置在動作結算的當下就已經改變。

### 22.2 攝影機跟隨（**預設關閉**，見 22.2.1）

`ui.json` 的 `animation.followActingUnit`：

| 值 | 行為 |
|---|---|
| **`OFF`（預設）** | 鏡頭完全不跟敵人跑，一直待在玩家身上 |
| `SNAP` | 瞬間跳到行動中的單位（v0.16 以前的行為） |
| `PAN` | 平滑平移過去（v0.17 原本的設計，即以下各條） |

#### 22.2.1 為什麼預設是 OFF

實際試玩的回報：**一輪十隻敵人，鏡頭在不同單位之間來回追尾會讓人暈到想吐。**

這一條推翻了 §22.3「平移期間目標暫停」的前提 —— 那條規則要解決的是
「玩家看不到動作的開始」，但它的代價是玩家的生理不適，而那個代價不可接受。

> **看得到敵人在做什麼，代價不該是玩家的生理不適。**

敵人的動作**仍然有位移動畫**（`enemyMoveMs`），只是鏡頭不再跟著跑：
在畫面內的敵人照樣看得見它在移動，畫面外的本來就靠口令（§12.18）與紀錄交代。
兩種切回去的路都留著，但預設不是它們。

以下 §22.2 與 §22.3 的規則**只在 `PAN` 模式下生效**。

攝影機跟隨**當前行動中的單位，不分敵我**。

**但只在必要時才平移**（`edgeMargin`，預設 2 格）：

- 目標在可視範圍外 → 平移
- 目標太靠近畫面邊緣 → 平移
- **目標已經舒服地位於畫面內 → 不平移，也不等待**

> 這一條與 §22.3 是一組的。加上「平移期間目標暫停」之後，
> 若每一隻敵人都要「平移過去 → 等待 → 行動」，十隻敵人的一輪會非常久。
> 大部分情況下目標本來就在畫面內，應該零延遲。

玩家自己的動作仍沿用 v0.8 的既有行為（動作後自動置中、可拖曳平移、置中按鈕）。

> 一個實作上的細節：鏡頭對準的是**畫面上**的位置而不是邏輯位置。
> 否則玩家自己走一格時，格子瞬間置中而人還在滑，看起來會像倒退嚕。

### 22.3 平移期間目標暫停

順序固定為：

```
攝影機平移 → 到位 → 該單位行動
```

平移期間該單位**不得開始其動作**。否則玩家永遠看不到動作的開始，只看得到結果 ——
那正是 v0.4 建立回饋層時要解決的同一個問題。

跳過沿用 v0.6 的機制：點畫面任意處跳過整段演出，直接結算到輪到玩家為止。
**跳過後的結果必須與完整播放完全相同** —— 跳過時不播動畫、不平移，只結算。

### 22.4 對任務長度的影響

動畫時間會直接延長真人的實際遊玩時間，而 v0.13 好不容易把四張地圖
收斂到 1500～3000 時間單位（換算真人約 15～25 分鐘）。

`scripts/pace.mjs` 用同一張圖、同一個種子、同一套政策跑三次，
只有動畫旋鈕不同，量的就是**真實牆鐘時間的差距**。
這支腳本與機器人基準（`scripts/botrun.ts`，完全不碰畫面）是兩回事：
**一支量規則，一支量畫面。**

實測（三次的遊戲結果完全相同：時刻 1659、陣亡 3、SUCCESS）：

| 旋鈕 | 真實耗時 |
|---|---|
| 全部 0（等同 v0.16） | 64.9 秒 |
| 預設（80／180／220） | 67.9 秒（+2.9） |
| 刻意調慢（300／500／600） | 103.0 秒（+38.0） |

> **預設值幾乎不花錢，原因是動畫長度塞得進既有的節奏間隔裡**
> （`playerMoveStepMs` 110 > 80、`enemyStepMs` 200 > 180），只是把原本的空檔填滿；
> 真正會加時間的只有攝影機平移的等待。
>
> **風險線因此很明確**：把 `enemyMoveMs` 調過 `enemyStepMs`、
> 或 `playerMoveMs` 調過 `playerMoveStepMs`，就會開始真的變慢。

---

## 23. 任務中的裝備管理（v0.18）

> **補上一個缺失的動詞。不新增系統，不改動數值。**

### 23.1 為什麼

實測發現：**空投下來的替補士兵沒辦法接手前一個人的裝備。**

他走到屍體旁邊，屍體上躺著一把無後座力砲 —— 但他自己的主手與收納欄都滿了，
而遊戲裡沒有任何動作可以騰出位置。

規格 §10.2 與 v0.9 §3.1 其實都預期這件事可行
（「撿到的第三把武器可以放進背包，要先換到收納欄」），
**只是這個「換到收納欄」的動作從來沒有被實作。**

> **這使本作的核心決策整個失效。**
> 「要不要冒險走回去撿自己的屍體」從第一輪設計到現在是這款遊戲最重要的單一決策；
> 而在此之前，玩家走過去之後做不了任何事。

### 23.2 三個位置與三個新動作

| 位置 | 可否使用 | 計入負重 |
|---|---|---|
| 主手 | 可以開火 | 是 |
| 收納欄 | 不可使用，但可用換武器動作切到主手 | 是 |
| 背包 | 不可使用 | 是 |

| 動作 | 時間 | 狀態 |
|---|---|---|
| 主手 ↔ 收納欄 | 該武器的 `swapTime` | 既有，不變 |
| **背包 → 收納欄** | 同上 | v0.18 新增 |
| **收納欄 → 背包** | 同上 | v0.18 新增 |
| **主手 → 背包** | 同上 | v0.18 新增 |
| 丟棄（任一位置） | 0 | 既有（v0.12） |
| 從屍體或容器拾取 | `time.pickup`（10） | 既有 |

指令是 `MOVE_GEAR { from, to, itemId? }`。目的地已經有東西時就**對調**
（收納欄換出來的那把進背包）—— 比「先清空再搬」少按一次，而且淨重量一樣不變。

> **不必檢查重量。**三個位置的東西全部計入負重（v0.15 §19.3），
> 所以搬動不改變身上的總重，只改變「拿不拿得到」。

搬裝備與換槍一樣會**中斷手上的序列** —— 你正在裝填的那把槍被收走了。

### 23.3 這一版真正要驗證的情境

1. 士兵 A 帶著 AR-9 與 RR-4 陣亡，屍體上有兩把槍與整個背包
2. 士兵 B 從最近的空投點降落，帶著自己的配裝
3. B 走到屍體旁邊
4. B 把自己的槍移進背包，從屍體拾取 RR-4 放進收納欄
5. B 一併取走屍體上的彈藥與消耗品
6. B 帶著這些東西撤離
7. **結算後，RR-4 回到軍械庫，且仍是同一個 `instanceId`**

第 7 點是關鍵：**回收的必須是原本那一把槍，不是一把同型號的新槍。**
`scripts/gear.mjs` 在真的瀏覽器上跑這七步。

> 實作時發現的一個坑：`applyMissionResult` 原本靠 `extracted` 的**順序**
> 決定生還者手上留哪兩把，而背包內容排在武器前面 ——
> 於是背包裡的手槍搶走主手，玩家冒著命撿回來的那把砲反而變成未指派。
> 改為由 `MissionResult` 明確帶上撤離當下的主手與收納欄實例 id。

---

## 24. 經濟層（v0.20）

> **取代 v0.16 §21.6 的 0 元補給站佔位。**
> 明確不做：合成、DNA 世代、經驗值、副業與時價、個人裝甲與其修補、
> 跨任務傷勢與醫療費用、技能。戰術層規則與數值完全不變。

### 24.1 貨幣

單位是**信用點**。設定上的荒謬要維持：
**發行機構已經不存在了，但所有帳目仍以它計價。**這件事不需要解釋，
只要讓它出現在每一張表上。

### 24.2 價格結構：人便宜，好槍貴

依世界觀設定：**生物可以複製，精密製造不行。**

| 類別 | 供給 | 價格 |
|---|---|---|
| 複製人士兵 | 無限 | 0.5 P |
| 土製武器與彈藥 | 無限 | 0.1～0.2 P |
| **遺產武器** | **有限、不定期補貨** | **1～3 P** |
| 消耗品 | 無限 | 0.1 P |

`P` 是一份 C 級（例行）合約的報酬。**這是本作的一句話：人可以再長，好槍不行。**

設計意圖：

- 零陣亡完成一份合約 → 純利約 P
- 陣亡兩人 → 大致打平
- 全滅加上武器損失 → 相當於欠下兩份以上合約的收入

> **「打得好」與「打得爛」的差別，就是「賺錢」與「白做工」的差別。**

武器的遺產／土製由 `weapons.json` 的 `origin` 決定，分界線是**自動循環**
（附錄 B §20.3）：泵動與中折的兩把霰彈槍是土製的，其餘都是撤離前留下的。

### 24.3 遺產武器的稀缺必須是結構性的

補給站的遺產武器**不是型錄，是一份會變動的少量現貨清單**：

- 同時最多 1～3 件
- **不保證你想要的型號會出現**
- 買走就從清單上消失

這反映「只有流通、沒有生產」。玩家因此會傾向**保住手上那把**，而不是死了再買一把。

> **現貨的更新綁定「完成的合約數」，不得綁定真實時間。**
> 這沿用副業時價的同一條原則：不要養出「每四小時回來看一次」的習慣。

### 24.4 收入

**合約報酬**（顯示在合約清單上，與難度評級並列）：

- 依難度評級縮放（倍率在 `economy.json`）
- **主目標完成才給主要報酬**；次要目標各自另計較小的獎金
- 主目標未完成而撤離 → 只拿次要目標的獎金

> 這讓「東西撿夠了、主目標太硬、我走了」變成算得出來的決定：
> **你放棄的是主目標的報酬，換到的是活著回來與背包裡的東西。**

**出售**：值錢的雜物、多餘的武器實例、多餘的彈藥與消耗品、DNA。
售價 = 買價 × 折扣。**有人拿著的槍不能賣** —— 先在配裝畫面收回來。

> DNA 可賣，但介面上要標示**日後另有用途** ——
> 依「系統不禁止，只標價」的原則，玩家有權賣掉自己的血脈，但要知道自己在做什麼。

### 24.5 債務：沒有硬性失敗

**允許信用點為負。沒有破產結束遊戲。**
依本作原則 —— 系統不禁止，只標價 —— **破產不是終局，是一個持續變糟的處境。**

信用點為負時，回到公司畫面會收到**董事會信件**，語氣隨負債程度惡化（三級）。
文體嚴格依世界觀 §12。**這一版信件本身就是後果，不附帶任何實際懲罰。**

**保底**：即使信用點為負，玩家永遠可以取得最基礎的複製人士兵。
設定上的理由已經存在 —— 那條血脈是某位失蹤的高階主管，
樣本庫龐大到現在他的臉是這顆星球上最常見的一張。便宜、量大、平庸、隨時有貨。
實作上這是防止卡死的保底，但它完全由設定支撐，**不需要標示為佔位**。

### 24.6 結算損益表

結算畫面改為一份損益表，**擺在最上面**：

```
合約報酬              +XXX
次要目標獎金           +XX
帶出的戰利品（估值）    +XX
────────────────────────
陣亡士兵（重置成本）    −XX
遺留的武器（永久性減損） −XXX
消耗的彈藥與物資        −XX
────────────────────────
本次合約損益           ±XXX
```

**這張表就是這一版的重點。**玩家必須看得出自己這一趟是賺是賠，以及**賠在哪裡**。

> 要分清楚兩件事：**只有合約報酬與獎金真的入帳。**
> 帶回來的戰利品要拿去補給站賣才變成錢 —— 那是玩家的下一個決定，不是自動發生的事。
> 其餘各欄是估值，用來回答「這一趟實際上花掉了多少」。

文體同樣依世界觀規範 —— **用會計語言講死人。**

### 24.7 這一版之後才會成立的兩件事

1. **屍體回收的正確答案** —— 在此之前「要不要回去撿」沒有可計算的答案，現在有了
2. **個人裝甲** —— 修補費用是自然的支出項目

---

## 23. 翻越掩體（v0.19）

> **新增一個移動動作，但不新增任何按鈕。**
> 不改動既有的視線、掩蔽、姿勢規則，也不改動任何既有數值。

### 23.1 三條決定成敗的規則

**一、翻越是跨過去，不是站上去。**

一次移動**兩格**：從當前位置，越過相鄰的半身掩體格，落在掩體正對面那一格。

> **任何單位都不得停留在半身掩體格上。**
>
> 若單位可以站在掩體上，就必須回答「站在半身掩體上的人算不算有掩蔽」
> 「他看不看得過去」—— 那會弄壞整組視線與掩蔽規則（§7）。跨過去完全迴避這個問題。

**二、落地必定是站姿。**即使翻越前是蹲姿。

> **這是翻越真正的代價，比時間成本重要。**
> 你落在開闊處，而且掩體現在在你錯的那一側 —— 如果對面有人，你剛剛跳進他懷裡。
> 這一條讓翻越自我節制，不需要靠很高的時間成本去壓它。

**三、敵人也必須會翻越。**

> 否則掩體列會變成**單向膜**：玩家可以穿過去，敵人只能繞路。
> 那會直接架空 v0.10 的側翼繞行機制。

翻越候選沿用既有的落點評分項（`approach`／`selfCover`／`targetExposure`／`canShoot`），
另加一個代表時間成本的懲罰。**原型差異不必另外寫**：
翻過去之後掩體在你身後，`selfCover` 自然歸零，
所以射手型（`selfCover` 權重 6）本來就不會想翻，衝鋒型（只看 `approach`）會。

> 懲罰係數乘上「多花了幾倍的時間」，**不是固定值** ——
> §23.4 說「若成排掩體不再構成屏障，先調高翻越時間」，
> 若懲罰與時間脫鉤，那個旋鈕對敵人就完全沒有作用。

### 23.2 合法性與操作

同時滿足才合法：面向方向的相鄰格是 `HALF_COVER`；該掩體格的正對面一格
存在、可通行、且未被單位佔據；執行者不在系列動作進行中。**`WALL` 永遠不可翻越。**

**沒有新增按鈕。**按方向鍵撞向半身掩體本來是一個非法輸入、什麼都不會發生，
把翻越接上這個輸入即可。蹲姿沿用 v0.8 §12.14 的「先轉向、再執行」。

方向鍵因此有**三種狀態**，而且要分得出來：會移動、會轉向、**會翻越**。
翻越那一顆直接把花費寫在按鍵上 —— 它比較貴，按下去之前就該看得到。

尋路移動把翻越當成一條**有成本的邊**。路徑預覽的總時間逐段加總，
不能再用「步數 × 移動時間」。

> 實作上這逼著把 `findPath` 從 BFS 換成成本感知搜尋 ——
> 翻越是「兩格但比較貴」的邊，BFS 會把它當成兩格的捷徑而永遠優先。
> 同成本時取先發現的節點，讓所有邊等價時的展開順序與 BFS 一致。

### 23.3 數值

| 項目 | 值 |
|---|---|
| 翻越時間 | `time.vault`（**20**） |
| 落地姿勢 | 強制站立 |
| AI 的翻越懲罰係數 | `ai.vaultPenalty`（1.5，乘上多花的時間倍數） |

**訂 20 的理由**：一次移動兩格但花兩倍時間，所以它**不是「更快的移動」，
是「穿越屏障的手段」**。繞到成排掩體的末端可能要六步（60），翻越是 20 ——
明顯的捷徑，但代價是落地暴露。

> 若實測發現成排掩體不再構成屏障，**先調高這個數字，不要動掩體配置。**
